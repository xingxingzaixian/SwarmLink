# SwarmLink 架构设计书

**主题**：无中心内网 P2P 聊天与文件分享工具 — 架构评审、目标架构与演进规划
**版本**：v1.0
**日期**：2026-09-20
**输入**：《内网P2P聊天与文件分享工具 — 技术研究及设计方案 v1.0》
**定位**：在 v1.0 技术方案之上，补齐架构契约、修正不可扩展的设计点、给出长期演进路径
**技术栈**：Go 1.23+ / Wails v3.0.0-beta.24 / Vue3 + Pinia

**修订记录**

| 版本     | 变更                                                                                                                                                                                                                                                                                                                                   |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v1.0     | 初版：架构评审 + 目标架构 + 协议 v2 + 数据模型 v2 + 8 条 ADR + 演进路线图                                                                                                                                                                                                                                                              |
| **v1.1** | **确认规模约束「全局节点数 ≤ 200」**。据此新增 **3.7 连接拓扑**、**ADR-009（按需连接）**、**ADR-010（全量目录同步）**；移除分层发现、软中继、增量目录同步三项复杂度；同步修订 0.5 / 1.1 / 2.1 / 4.5 / 4.6 / 7.2 / 7.4 / 8.1 / 8.7 / 8.8 / 9.4 / 9.5 与附录 A/C                                                                         |
| **v1.2** | **确认拓扑约束「3–5 个网段、手动维护种子、不考虑运维负担」**。据此新增 **4.5.1 跨网段发现**、**ADR-011（多种子 + 单跳目录拉取）**；确立「跨网段能力进 M1 而非 v1.1」这条**形状约束**；新增**两条部署前置条件**（网段间三层可达、地址段不得重叠）；同步修订 0.5 / 3.7 / 4.5 / 5 数据模型 / 7.2 / 7.4 / 8.1 / 8.5 / 8.8 / 9.5 与附录 A/C |

---

## 0. 结论摘要（先看这一页）

### 0.1 一句话判断

现有方案的**技术调研是合格的**（UDP 广播的坑、Wails v3 绑定、modernc vs mattn、位图续传都选对了），但**架构是缺位的**：模块之间没有依赖规则、没有跨模块契约、没有为 P0 功能预留设计，且存在 5 个会导致"做完了但要重做"的缺口。

### 0.2 五个必修缺口（详见第 1 章）

| #   | 缺口                                                             | 级别 | 后果                                       | 修复成本                            |
| --- | ---------------------------------------------------------------- | ---- | ------------------------------------------ | ----------------------------------- |
| G1  | **群聊是 P0，但零设计**（无 group 模型、无扇出策略、无成员同步） | P0   | 功能承诺无法兑现，返工数据模型 + 协议 + UI | 高（现在补最便宜）                  |
| G2  | **HELLO 不做身份认证**，NodeID 可被任意冒充                      | P0   | 身份体系形同虚设，E2E 加密失去意义         | 低（已有 Ed25519，加挑战-应答即可） |
| G3  | **接收侧文件名未清洗**，对端可发 `../../.ssh/authorized_keys`    | P0   | 目录穿越写任意路径，安全事故               | 极低                                |
| G4  | **CHAT 消息不可靠**：无 msg_id、无 ACK、无重发、无去重           | P0   | 断线即丢消息，聊天工具核心体验缺陷         | 中                                  |
| G5  | **双向拨号未定胜负规则**，两端同时互拨产生重复连接               | P0   | 连接撕裂、消息重复、状态机混乱             | 极低                                |

### 0.3 核心架构建议

**从"分层直接调用"改为「六边形架构 + 事件驱动核心」。**

理由：这个系统的本质是 **4 个 I/O 适配器（UDP / TCP / SQLite / Wails）围绕一个纯领域核心**。现设计的 `transport.Manager.onChatReceived()` 直接调 `store.SaveMessage()`，是把存储层焊进了网络层——这个方向一旦固定，后续每加一个功能（群聊、加密、历史检索、统计）都会继续往网线上挂逻辑。改成事件驱动后，传输层只发事件、不认存储层，依赖方向单向向内。

配套三条红线（详见 3.2）：领域层零 I/O、跨模块只走端口接口或事件、适配器可被内存实现替换。

**第二条核心决策：把「在线状态」和「传输连接」解耦（ADR-009，详见 3.7）。**

既然全局只有 200 个节点，一个自然的诱惑是"让所有节点互相保持长连接"——**不要这样做**。全网 19,900 条连接会带来约 1,300 pkt/s 的纯保活流量，且任何一次网络抖动都会变成 199 条连接同时重建。正确做法是：**在线状态由 UDP announce 维护（无连接、成本趋零），TCP 连接按需拨号、空闲回收，单节点常驻连接数控制在 10 条以内。**

**第三条核心决策：跨网段只加一条单播路径，不加任何角色（ADR-011，详见 4.5.1）。**

3–5 个网段 = 3–5 个互不可见的"广播岛"。这里最容易走歪的地方是**给种子加特权**（转发消息、维护权威目录）——那会长出中心节点，侵蚀核心卖点。本方案的种子**只是配置里的一串地址**，与普通节点跑完全相同的目录交换协议：每网段配 2 颗、共 6–10 条 IP，手工维护。代价是两条**部署前置条件**（P-1 网段间三层可达、P-2 地址段不重叠）—— 它们是网络侧的既定事实，**代码救不了，必须在部署前确认**，所以 M1 就要把 `subnet` 字段放进 ANNOUNCE 以便自检。

**第四条决策（时机）：跨网段进 M1，不进 v1.1。**

它不是一个"以后要加的功能"，而是**发现协议报文的形状约束**。若 M1 按纯广播语义写死目录交换，v1.1 加种子时要改协议；现在做只多 2 天，还顺带把 `subnet` 检测与"种子身份第一跳即可验签"落地。

### 0.4 长期演进一句话

v1.0 把**单聊 + 群聊 + 单文件续传 + 跨网段发现**做扎实并留下接缝 → v1.1 补 **E2E 加密 + 群聊扩容** → v1.2 补**并发传输 + 目录传输**（此处架构需从"单连接单任务"升级为"会话调度"）→ v2.0 才谈**插件化与多网络**。**不要为了"以后可能要"提前上微服务/消息队列/中继节点**——本项目全局不超过 200 个节点、3–5 个网段，提前上只会拖垮小团队。

> **注意"跨网段"的位置变了**：v1.1 曾经是"补跨网段"，现在**提前到 v1.0**。原因见 4.5.1——跨网段不是一层功能，而是**发现协议报文的形状约束**。如果 M1 的目录交换报文只按"广播响应"设计（例如依赖 UDP 源 IP 推断对端），v1.1 加种子时就要改协议；而把它做成"同一套请求-响应跑在单播上"，M1 只需多写一个定时器（约 2 天）。**这是整份文档里"提前做最便宜"的一处判断。**

### 0.5 规模与拓扑约束（已确认：**全局 ≤ 200 节点、3–5 个网段、种子手动维护**）

**这两条约束合起来砍掉了四块复杂度**，是全文档最强的"边界"。

| 维度           | 确认值                   | 架构结论                                                                   |
| -------------- | ------------------------ | -------------------------------------------------------------------------- |
| **全局节点数** | **≤ 200**                | 全量节点目录同步可行（12.8 KB）；**不需要分层/分区发现**；**不需要软中继** |
| **网段数**     | **3–5**                  | 网段内广播，网段间**种子单播拉取**；种子清单**手动维护**（共 6–10 条 IP）  |
| 种子分发机制   | **不需要**               | 手工配种子完全可控 → "种子分发 / 只读引导节点"**永久移除**                 |
| 节点间连接     | **不维护全互联持久连接** | 在线状态走 UDP announce，TCP **按需拨号 + 空闲回收**（3.7 / ADR-009）      |
| 单文件大小     | ≤ 50 GB                  | 无本质变化（位图 12.5 KB，可忽略）                                         |
| 群成员上限     | 20（v1.0）               | 现实天花板 = 200（全局节点数）；> 20 切 Gossip，**永远不需要软中继**       |
| 消息频率       | ≤ 10 msg/s/节点          | 无本质变化（WAL 可承受）                                                   |

**被这两条约束直接划掉的四件事**：

1. ~~分层 / 分区发现协议~~ —— 200 节点用"广播 + 全量目录交换"足够，不需要拓扑分层
2. ~~软中继（群聊方案 C）~~ —— 触发条件是"群 > 200 人"，而全局只有 200 节点，场景不存在
3. ~~全互联持久连接~~ —— 会引入约 1,300 pkt/s 的纯保活流量与 SYN 风暴风险，改为按需拨号
4. ~~种子分发机制 / 只读引导节点~~ —— 3–5 个网段手工配种子完全可控，**架构上永久不需要引导节点**

**跨网段方案冻结为「网段内广播 + 网段间种子单播拉取」**（详见 4.5.1 / ADR-011）：每个网段配 **≥ 2 个**种子，全部种子共用同一份清单。

**两条部署前置条件**（不增加代码量，但**必须写进安装文档并由部署方保证**；不满足则本方案不成立）：

| #       | 前置条件                                                             | 不满足的后果                                                                        | 自检方法                                                                |
| ------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| **P-1** | 任意两网段的种子 IP 之间**可 TCP 直连**（三层路由可达，非 NAT 隔离） | 跨网段发现与消息全部失效，此时**只能引入中继**——而中继是本文档刻意回避的设计        | 启动时对种子做一次 TCP 探测，全通即达标                                 |
| **P-2** | 各网段使用**不重叠的地址段**（不得都是 `192.168.1.0/24`）            | 目录中出现 `node_id` 不同、IP 相同的条目 → **跨网段寻址崩溃，且现象隐蔽、极难排查** | ANNOUNCE 携带 `subnet` 字段；种子侧检测到同一 subnet 来自两个网段即告警 |

> P-2 不是理论风险。**"多个厂区/门店各自一个 192.168.1.0/24，再靠 VPN 互联"是内网组网里最常见的形态之一**——如果你们正属于这种，必须在部署前统一改址或加 NAT 映射层，否则本方案的第二条前提要重新设计。

---

## 1. 架构评审：现设计的问题清单

评审标准不是"写得好不好"，而是**这处设计在 6 个月后会不会阻碍我们加功能**。

### 1.1 G1 — 群聊（P0）在架构上不存在

**现状**：功能表把"支持群聊"标为 P0，但第三章模块划分、第四章协议、4.5 数据模型全篇没有 group 概念。协议消息类型表里 CHAT 是点对点语义，`conversations` 表主键是 `peer_id`，天然装不下群。

**为什么这是架构问题而不是实现问题**：无中心网络里的群聊，**难点不在 UI，在于"消息可达性"**。单聊时你知道对端是谁、连不连得上；群聊时 A 发一条消息，B 离线、C 换了 IP、D 刚加入不知道群存在——没有中心服务器仲裁，这些全靠协议约定。这个决策必须现在做，因为它会反向决定：

- 数据模型（`conversations` 主键要从 `peer_id` 改为 `conv_id`）
- 消息表（要加 `msg_id` 用于去重，因为多路径扇出必然重复到达）
- 协议（要加群消息类型、成员同步类型）
- 传输层（要支持多目标扇出，不再是"一个会话一条连接"）

**四种候选策略**（详细对比见 4.6）：

| 方案             | 机制                                      | 适用规模         | 代价                                                                   |
| ---------------- | ----------------------------------------- | ---------------- | ---------------------------------------------------------------------- |
| A 全互联单播扇出 | 每个成员对每个成员直连单播                | ≤ 20 人          | 连接数 O(N²)，群主变更需重新同步                                       |
| B 洪泛 Gossip    | 消息带 conv_id + msg_id + TTL，收到即转发 | 20~200 人        | 需去重表，无严格有序                                                   |
| C 软中继         | 群内选一个稳定节点做转发（可切换）        | **本项目不存在** | 全局仅 200 节点，群上限即 200，用 B 即可覆盖；引入弱中心与核心卖点冲突 |
| D 混合           | 小群 A / 大群 B / 超大群 C                | 全覆盖           | 实现复杂度最高                                                         |

**建议：v1.0 走 A 并硬编码 20 人上限，但数据模型和协议按 B 的形状预留**（即现在就引入 `msg_id`、`conv_id`、`group_epoch`）。这样切到 B 时是"加一条转发路径"，而不是"重做数据层"。

**规模约束确认后的简化（全局 ≤ 200 节点）**：群成员的现实天花板就是 200。这意味着

- **方案 C（软中继）永远不需要** —— 它的触发条件是"群 > 200 人"，而全公司/全厂总共只有 200 个节点。这从路线图里彻底移除，也避免了"为了极端场景引入弱中心"的架构妥协
- 方案 A（全互联扇出）的可用区间扩展到 ≤ 50 人（连接数 2,450，仍在按需拨号模型下可控，见 3.7）
- 方案 B（Gossip）覆盖 50~200 人，是本项目群聊能力的**完整闭环**

也就是说：**200 节点这个约束让群聊的三档策略退化成两档，且第二档就够用到底。**

### 1.2 G2 — 握手不认证，身份体系是装饰品

**现状**：4.3 节 HELLO "交换 NodeID 和显示名"。NodeID 是公钥指纹，但**连接时并不验证对方持有对应私钥**。任何人读取广播包拿到你的 NodeID，就能以你的身份连上第三方。

**为什么严重**：项目把"Ed25519 密钥对持久化 + 公钥指纹作为唯一标识"列为 P0，把"端到端加密"列为 P1。但**身份认证不是加密的子集，它是加密的前提**——没有认证，E2E 加密只是"和一个自称是张三的陌生人加密聊天"。而且签名基础设施已经具备（`identity.Sign()/Verify()` 已在模块表里），加认证几乎是零边际成本。

**修复**：HELLO 后插入挑战-应答（4.2 节给完整时序），双方对**握手全量转录**签名，签名验证通过才进入 ESTABLISHED。这样同时防冒充和防重放。

### 1.3 G3 — 接收文件名未清洗，可写任意路径

**现状**：4.6 节"文件接收路径逻辑"直接 `filepath.Join(default_dir, file_name)` 加后缀去重；4.4 节用 `os.OpenFile(tmpPath, ...)`。`file_name` 来自 4.3 的 `FILE_META` 载荷，**完全由对端控制**。

**攻击面**：

- `file_name = "../../../../Users/x/.ssh/authorized_keys"` → 写到目标目录外
- `file_name = "/etc/launchd.conf"` → 绝对路径覆盖
- `file_name = "a\u0000b"` → 空字节截断
- Windows: `file_name = "..\\..\\Startup\\evil.exe"` → 开机自启
- `file_name = "CON"` / `"NUL"` → Windows 保留设备名

**修复**（接收侧强制，不可绕过）：

1. 取 `filepath.Base(fileName)`，丢弃所有目录成分
2. 拒绝空、`.`、`..`、含 `/` `\` `\x00` 的输入
3. Windows 平台额外过滤保留名（`CON PRN AUX NUL COM1-9 LPT1-9`）与尾随点/空格
4. 长度截断到 255 字节（按 UTF-8 边界切，不要切坏多字节字符）
5. `filepath.Clean` 后校验结果仍在 `default_dir` 前缀内（`strings.HasPrefix(abs, base+string(os.PathSeparator))`）
6. 临时文件用 `job_id` 命名（如 `.swarmlink.<job_id>.part`）而非 `<filename>.tmp`，天然免冲突、免路径注入

### 1.4 G4 — 消息不可靠、进度事件会淹没事件桥

**现状**：`ChatService.SendMessage` 直接 `store.SaveMessage` + `transport.SendChat`，fire-and-forget。TCP 只保证**当前连接内**不丢不重不乱序；连接断开期间对方发的消息全部丢失，且重连后无任何补齐机制。同时 `messages` 表没有唯一键，重复到达会重复入库。

另外 4.4 节"每完成一个块，发送方和接收方分别推送进度"——1 GB 文件 = 2048 个事件，千兆网下约 0.2 秒内全部涌向 WebView 事件桥。前端每个事件都 `store.commit('updateTransfer')`，会把渲染主线程打满。

**修复**：

- 消息加 `msg_id`（UUIDv7，时间有序）+ `conv_id`，`UNIQUE INDEX(msg_id)` 做去重
- 加 `CHAT_ACK` 消息类型；发送侧 `outbox` 表记录未确认消息，指数退避重发；对端上线后 flush
- 语义为 **at-least-once + 接收端幂等去重 ≈ exactly-once**
- 进度事件**按 job 节流**：合并为 4~10 Hz，或百分比整数变化才发；速度值用滑动窗口计算（不要瞬时值，抖动大）

### 1.5 G5 — 双向拨号没有胜负规则

**现状**：发现是双向的（A 广播、B 也广播），双方都会尝试互拨。方案未定义"谁该拨号"。

**后果**：两端各有一条 TCP 连接，HELLO 通过两次 → 同一 peer 两个 session → 心跳重复、文件块从两条通道到达、`transfer_jobs` 状态被两个 writer 争抢。

**修复**（确定性规则，双方独立算出同一结论）：

- 若 `self.NodeID < peer.NodeID`，本方负责拨号；否则只接受、不主动拨
- 兜底：HELLO 完成后检查是否已存在同 nodeID 的 ESTABLISHED 连接；若存在，按同一规则保留胜者、关闭负者，**且关闭前不发任何业务帧**

### 1.6 P1 / P2 清单

| #     | 问题                                                 | 级别 | 影响                                              | 修复                                                                                                                            |
| ----- | ---------------------------------------------------- | ---- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| P1-1  | **分层依赖倒置**：transport 直接调 store             | P1   | 依赖成网状，无法单测，加功能持续恶化              | 引入端口接口 + 事件总线（第 3 章）                                                                                              |
| P1-2  | **协议无版本/能力协商**：Version 字节只是"当前值"    | P1   | 加字段就无法兼容旧节点                            | HELLO 协商 `proto_min/max` + caps 位掩码                                                                                        |
| P1-3  | **帧头无 Flags、无扩展位**                           | P1   | 后续加加密/压缩/分片必须改版本号                  | 帧头扩到 Magic2+Ver1+Flags1+Type1+Len4（4.1）                                                                                   |
| P1-4  | **传输是 stop-and-wait**：每块等 ACK                 | P1   | 跨网段 RTT 20 ms 时吞吐断崖；无法并发             | 滑动窗口 + 周期性全量位图回传（4.8）                                                                                            |
| P1-5  | **发现无 TTL / 无列表 epoch**                        | P1   | 陈旧节点永久驻留；节点交换产生风暴                | 条目带 `last_seen` + TTL 300 s；交换请求带 `list_epoch`；节点总数与单包条目数设上限                                             |
| P1-6  | **无多网卡策略**：WiFi/Ethernet/VPN 会互相打架       | P1   | 向错误网卡广播；发现自己；VPN 场景全乱            | 接口优先级 + 排除虚拟网卡 + 按接口独立广播 + 自发现过滤（4.5）                                                                  |
| P1-7  | **Store 是上帝对象**：transfer 直接依赖 SQLite       | P1   | 状态机无法脱离数据库测试                          | 定义 `MessageRepo`/`TransferRepo`/`PeerRepo` 端口，SQLite 只是实现                                                              |
| P1-8  | **无背压/无带宽预算**                                | P1   | 多对端同时传大文件打满网卡，发送 goroutine 无上限 | 全局并发传输上限 + 单对端限速 + 窗口自适应                                                                                      |
| P1-9  | **每块都写 SQLite**                                  | P1   | 10 GB 文件 = 20480 次写事务，WAL 也扛不住         | 位图在内存维护，**每 2 s 或每 256 块批量落盘**一次                                                                              |
| P1-10 | **进度事件无节流**（见 1.4）                         | P1   | 前端渲染卡死                                      | 节流至 4~10 Hz                                                                                                                  |
| P2-1  | 无结构化日志与 trace                                 | P2   | P2P 问题无法定位                                  | `log/slog` + `node_id`/`conn_id`/`job_id` 三级上下文                                                                            |
| P2-2  | 无迁移规范（`migrations.go` 只有文件名）             | P2   | 表结构变更会毁用户数据                            | `schema_migrations` 表 + 版本化迁移 + 启动时事务执行                                                                            |
| P2-3  | **端口 2425 与飞秋冲突**                             | P2   | 与目标用户已装软件抢端口                          | 启动探测占用 → 自动回退到 2425+1..+9 → 端口写进 ANNOUNCE 供对端学习                                                             |
| P2-4  | 无测试策略                                           | P2   | P2P 逻辑无法回归                                  | 端口接口 + 内存实现 + `net.Pipe()` 双节点 + 可注入 `Clock`                                                                      |
| P2-5  | 配置是可变全局状态                                   | P2   | 运行期改动生效点不明确                            | 快照只读 + `config.changed` 事件                                                                                                |
| P2-6  | 常量分散（UDP 魔数 5 字节 "MYAPP"，TCP 魔数 2 字节） | P2   | 协议常量不一致，易出错                            | 常量**集中**到 `protocol/const.go`。注意 **集中 ≠ 相同**：UDP 与 TCP 各用一套魔数与编号空间（见 4.4），不要为了"统一"把它们合并 |

---

## 2. 架构目标

### 2.1 质量属性与目标 SLO

架构不是为了好看，是为了守住下面这张表。**后续任何设计变更，都应先问"它对哪个指标负责"。**

| 质量属性 | 目标                                                 | 架构手段                                                                                                     |
| -------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 可维护性 | 新增一种消息类型 ≤ 3 个文件改动                      | 消息注册表 + 领域层零 I/O                                                                                    |
| 可测试性 | 领域层单测覆盖率 ≥ 80%，无需真实网络                 | 端口接口 + 内存适配器 + 可注入 Clock                                                                         |
| 可扩展性 | 新增"发现方式"（如 mDNS）不改传输/存储               | 发现策略接口 + 事件总线                                                                                      |
| 性能     | 千兆 LAN 单文件 ≥ 80 MB/s（磁盘为瓶颈而非协议）      | 滑动窗口 + 512 KB 块 + 批量落盘                                                                              |
| 可靠性   | 消息 at-least-once + 去重；传输断点恢复精度 = 512 KB | outbox + ACK + 位图持久化                                                                                    |
| 安全     | 身份不可冒充；接收文件不逃逸目标目录                 | 握手签名 + 路径清洗（G2/G3）                                                                                 |
| 可观测性 | 任一传输失败可从日志还原完整因果链                   | 结构化日志 + 统一 job_id trace                                                                               |
| 规模     | 全局 ≤ 200 节点、3–5 个网段                          | 在线状态走 UDP announce；TCP 按需拨号（3.7）；全量目录同步 + 条目 TTL 300 s；网段间**种子单播拉取**（4.5.1） |
| 可部署性 | 无需任何中心组件即可跨 3–5 网段运行                  | 种子 = 配置里的地址列表（零特权）；P-1/P-2 两条前置条件在 M1 即可自检                                        |

### 2.2 明确不做的（Non-Goals）

写在架构里，是为了防止以后被裹挟着做：

- **不做 NAT 穿透 / 公网打洞**（约束已明确，不为它设计任何路径）
- **不做中心服务器 / 云账号体系**（会让"无中心"这个核心卖点失效）
- **不做引导节点 / 种子分发服务**（3–5 网段手工配 6–10 条 IP 即闭环，见 ADR-011 —— **这是最容易被做成"事实中心"的位置，直接不做**）
- **v1.0 不做文件夹传输**（约束已明确，但数据模型不排斥：`transfer_jobs` 天然支持多 job 属于同一 batch）
- **永久不做转发 / 中继**（**注意：本次修订把它从"v2.0 可选增强"上调为"永久不做"** —— 只要 0.5 节的 P-1 成立，跨网段直接按需拨号即可，零跳数；中继的引入条件只剩"P-1 不成立"，而那是**网络侧该修的问题**，不该由应用架构牺牲"无中心"去买单）
- **不做跨设备身份合并**（一个用户多台机器共用身份，是身份体系的大改，v2.0 再议）

---

## 3. 目标架构

### 3.1 架构风格决策（ADR-001）

**决策：六边形架构（Ports & Adapters）+ 进程内事件驱动核心。**

**上下文**：这个系统的形状已经被约束死了——所有真实行为都发生在 I/O 边界上（UDP 收包、TCP 收帧、SQLite 读写、WebView 调用）。领域逻辑本身不复杂，复杂的是"边界上同时发生的事"。现设计的按技术分层（service / transport / store）会让依赖横穿，且无法在没有网络和数据库的情况下测试状态机。

**四层与依赖方向**：

```
┌─ L4 入站适配器 ───────────────────────────────────────────┐
│  service/  SettingsService  ChatService  TransferService  │
│            PeerService      GroupService                  │
│  eventbridge/  Go 领域事件 → Wails Event.Emit             │
└───────────────────────────┬───────────────────────────────┘
                            │ 只能向下调用
┌─ L3 应用层（用例编排，无 I/O 细节）────────────────────────┐
│  app/  ChatApp  TransferApp  DiscoveryApp  PeerApp  GroupApp│
│  职责：编排领域对象 + 端口，事务边界，事件发布              │
└───────────────────────────┬───────────────────────────────┘
                            │ 只能向下调用
┌─ L2 领域核心（纯 Go，零外部依赖）──────────────────────────┐
│  domain/identity  密钥 · NodeID · 签名验证                 │
│  domain/peer      Peer 实体 · 生命周期 · 多网卡地址         │
│  domain/message   Message 实体 · 去重规则 · 顺序规则        │
│  domain/group     Group 聚合 · 成员 epoch · 扇出计划        │
│  domain/transfer  Job 聚合 · ChunkBitmap · 状态机 · 窗口    │
│  domain/ports     所有接口定义（依赖倒置锚点）              │
└───────────────────────────┬───────────────────────────────┘
                            │ 实现（implements，箭头反转）
┌─ L1 出站适配器 ───────────────────────────────────────────┐
│  adapters/net/tcp   adapters/net/udp   adapters/net/noise  │
│  adapters/store/sqlite   adapters/store/mem                │
│  adapters/config    adapters/wails                          │
└───────────────────────────────────────────────────────────┘

横切：infra/eventbus（进程内 pub/sub）· infra/clock · infra/log
组合根：main.go —— 唯一允许 new 出具体适配器的地方
```

**四个必须做对的细节**：

1. **`domain/ports` 是独立包，不是散落在各领域包里**。它的存在意义是：`app` 依赖 `ports`，`adapters` 实现 `ports`，双方都不知道对方存在。Go 的隐式接口让这一步几乎零成本。
2. **`infra/eventbus` 是同步的**。不要引入 channel + goroutine 池——P2P 场景下事件量不大，同步分发让"消息已入库"这类顺序假设可推理。需要异步的（如磁盘 IO）由消费者自己开 goroutine。
3. **事件总线不能用于需要返回值/需要事务的路径**。`SendMessage` 这类需要"入库成功才算发送"的顺序，应由 `ChatApp` 显式编排；事件只用于**通知**（有新消息了、进度变了、节点上线了）。
4. **`main.go` 是唯一的组合根**。所有 `new` 具体适配器、注入依赖、启动 goroutine 都在这里。业务代码里不允许出现 `sqlite.New(...)`。

**代价（必须承认）**：

- 比"直接调用"多约 20% 的胶水代码（接口定义 + 依赖注入）
- 事件驱动让调用链在 IDE 里不能直接跳转，靠"主题常量 + 订阅点"导航
- 团队需要纪律：**一旦有人图省事在 `transport` 里直接 `import store`，整套约束就退化了**。建议加 CI 检查（见 7.5）

**为什么不是微服务**：本项目是**单机进程**，所有模块共享内存、生命周期完全一致。拆进程只会把函数调用变成 IPC，把编译期错误变成运行时错误。微服务解决的是"团队独立部署"问题，我们只有一个小团队和一个可执行文件。

### 3.2 三条依赖红线

| 红线 | 内容                                                                                          | 违例示例                                          | 检查方式            |
| ---- | --------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------- |
| R1   | `domain/**` 禁止 import `net`、`database/sql`、`os`（`io` 与 `time` 允许）、Wails、其他适配器 | 在 `domain/transfer` 里 `import "database/sql"`   | CI 静态检查（7.5）  |
| R2   | 跨模块只走 `ports` 接口或事件总线，禁止跨包直接 `import` 兄弟模块                             | `transport` import `store`                        | CI 静态检查         |
| R3   | 适配器之间禁止互相依赖，只允许依赖 `ports` + `domain`                                         | `adapters/store/sqlite` import `adapters/net/tcp` | 代码评审 + 目录规则 |

**R1 的实际收益**：`domain/transfer` 的状态机可以在没有磁盘、没有网络的情况下跑完整测试。这是本项目最需要测试的部分（断点续传逻辑最容易错）。

### 3.3 目录结构

```
swarmlink/
├── main.go                     # 组合根：装配 + 启动
├── go.mod
├── wails.json
├── internal/
│   ├── domain/                 # L2 纯领域（红线 R1）
│   │   ├── identity/           # KeyPair, NodeID, Sign, Verify, Fingerprint
│   │   ├── peer/               # Peer 实体, PeerState 迁移, Interface 选择策略
│   │   │                       # + SeedAddr 值对象（ADR-011：只是"值得先问一声的地址"）
│   │   ├── message/            # Message 实体, 去重规则, 排序规则, MsgID 生成
│   │   ├── group/              # Group 聚合, Member, Epoch, 扇出计划(FanoutPlan)
│   │   ├── transfer/           # Job 聚合, ChunkBitmap, StateMachine, Window
│   │   └── ports/              # 全部接口（见 3.4）
│   ├── app/                    # L3 用例编排
│   │   ├── chat_app.go
│   │   ├── transfer_app.go
│   │   ├── discovery_app.go
│   │   ├── peer_app.go
│   │   └── group_app.go
│   ├── adapters/               # L1 出站 / L4 入站
│   │   ├── net/
│   │   │   ├── protocol/       # 帧编解码（纯函数，无 I/O）+ const.go 统一魔数
│   │   │   ├── tcp/            # ConnManager, Session, 握手, 心跳
│   │   │   ├── udp/            # Broadcast, Multicast, 接口枚举
│   │   │   │                   # + seed.go：单播探测(SEED_PROBE)与 PEER_LIST 拉取（ADR-011）
│   │   │   └── noise/          # v1.1 加密通道（预留接口）
│   │   ├── store/
│   │   │   ├── sqlite/         # 仓储实现 + migrations/
│   │   │   └── mem/            # 测试用内存实现
│   │   ├── config/             # TOML 读写
│   │   └── wails/              # 服务外壳 + 事件桥
│   ├── infra/
│   │   ├── eventbus/           # 同步 pub/sub + 主题常量
│   │   ├── clock/              # Clock 接口 + 真实/虚拟实现
│   │   ├── log/                # slog 封装 + 上下文注入
│   │   └── fsutil/             # 路径清洗、原子替换、安全文件名
│   └── sdk/                    # v2.0 预留：对外插件接口
├── frontend/
│   ├── src/
│   │   ├── api/                # 包一层 bindings，隔离 Wails 生成代码
│   │   ├── stores/             # Pinia：chat / transfer / peers / group
│   │   ├── views/ components/
│   │   └── events/             # 事件订阅集中注册 + 节流
│   └── bindings/               # Wails 自动生成（不手改）
├── docs/adr/                   # 架构决策记录（第 6 章各条落成文件）
└── test/
    ├── e2e/                    # 多节点集成测试（同进程），含【跨网段收敛】用例
    ├── scale/                  # 20~200 虚拟节点规模验证（见 7.2）
    └── chaos/                  # 丢包/断连注入
```

**与现设计目录的关键差异**：

- 拆出 `domain/`（现设计和 `internal/` 平铺，领域和 I/O 混在一起）
- 拆出 `infra/eventbus`（现设计没有解耦机制）
- 桌面 GUI 是**唯一**交付形态（不再保留命令行前端；部署自检做进设置页）
- `service/` 改为 `adapters/wails/`（明确它是适配器，不是"业务服务层"）
- 加 `test/e2e`、`test/scale`、`test/chaos`（P2P 必须有多节点可重复测试；`test/scale` 用于证明 200 节点不爆，`test/e2e` 内含跨网段收敛用例）
- 跨网段**只在 `adapters/net/udp/seed.go` 多一条单播路径**，不新开包、不新开层、不新增特权角色 —— **这是 ADR-011"零特权"在目录结构上的体现**

### 3.4 端口（Ports）定义

这是本架构最核心的产出。**接口一旦定错，底层怎么写都白搭。**

```go
package ports

// ---------- 节点目录 ----------
type PeerDirectory interface {
    Upsert(p peer.Peer) error
    Get(id identity.NodeID) (peer.Peer, bool)
    List(filter PeerFilter) []peer.Peer
    MarkOffline(id identity.NodeID, at time.Time) error
    // 供发现层做重复抑制
    SeenRecently(id identity.NodeID, window time.Duration) bool
}

// ---------- 消息仓储 ----------
type MessageRepo interface {
    Append(m message.Message) error
    // 幂等写入：msg_id 已存在则返回 ErrDuplicate，调用方据此决定是否重发 ACK
    AppendIfAbsent(m message.Message) (inserted bool, err error)
    Latest(convID string, limit int, before int64) ([]message.Message, error)
    MarkDelivered(msgID string) error
}

// ---------- 传输任务仓储 ----------
type TransferRepo interface {
    UpsertJob(j transfer.Job) error
    GetJob(jobID string) (transfer.Job, error)
    FindResumable(peerID, fileHash string) (transfer.Job, bool)
    // 批量落盘，避免每块一次事务（P1-9）
    SaveBitmap(jobID string, bm transfer.ChunkBitmap, completed int64) error
    ListActive() ([]transfer.Job, error)
}

// ---------- 连接管理（可被内存实现替换）----------
type ConnManager interface {
    Dial(ctx context.Context, id identity.NodeID, addr string) (Session, error)
    Accept(ctx context.Context) (Session, error)
    SessionOf(id identity.NodeID) (Session, bool)
    Broadcast(f protocol.Frame, ids ...identity.NodeID) error
    Close(id identity.NodeID) error
}

type Session interface {
    PeerID() identity.NodeID
    Send(f protocol.Frame) error
    Recv() (protocol.Frame, error)   // 阻塞；ctx 取消即返回
    RTT() time.Duration              // 心跳测得，供窗口自适应
    Close() error
}

// ---------- 发现策略（可插拔，见 2.1 可扩展性）----------
type DiscoveryStrategy interface {
    Name() string
    Start(ctx context.Context, sink func(peer.Announcement)) error
    Stop() error
}

// ---------- 种子注册表（ADR-011 / 4.5.1）----------
// 关键：种子不拥有任何特权接口。它产出的就是普通的 peer.Announcement，
// 与广播产出的结构完全同构 → 走同一条 Upsert 路径进入 PeerDirectory。
// 若这里出现 "SeedClient.Relay" / "SeedServer.Authoritative" 之类的方法，
// 说明设计已经走形（见 ADR-011 的"不采纳的替代"）。
type SeedRegistry interface {
    // 本轮该问的 N 颗：随机 + 失败指数退避，避免 200 节点集中打同一颗
    Pick(n int) []SeedAddr
    // 回填探测/拉取结果：learned == nil 表示失败，据此累计 fail_cnt
    // 种子的实际 tcp_port / node_id 由回传的 ANNOUNCE 填进 learned
    Report(addr SeedAddr, learned *peer.Announcement, err error)
}

// ---------- 文件落盘（接收侧，含路径清洗）----------
type FileSink interface {
    // 返回临时文件句柄 + 最终路径；实现内做 G3 全部清洗
    Create(jobID, rawName string) (SinkHandle, error)
    Commit(h SinkHandle) (finalPath string, err error)
    Abort(h SinkHandle) error
}

// ---------- 事件总线 ----------
type EventBus interface {
    Publish(topic string, payload any)
    Subscribe(topic string, fn func(any)) (unsubscribe func())
}

// ---------- 时钟（测试可控）----------
type Clock interface {
    Now() time.Time
    After(d time.Duration) <-chan time.Time
    NewTicker(d time.Duration) Ticker
}
```

**设计要点**：

- `Session.Recv()` 用阻塞式而非 channel。理由：TCP 天然阻塞；阻塞式更容易在 `select { case <-ctx.Done(): }` 中统一取消，且不会产生无人消费的 channel 泄漏。
- `FileSink` 把 G3 的路径清洗**收敛到一个接口实现里**，而不是散落在接收逻辑中。这样清洗规则只有一个测试点。
- `Clock` 让心跳超时、TTL 过期、重传退避全部可测，不用 `time.Sleep` 拖慢测试（现设计完全没考虑这点）。
- `ConnManager` 是最值钱的接口——测试里换成内存实现，就能在单进程内跑 5 个虚拟节点。

### 3.5 事件总线与主题

| 主题                | 载荷                           | 生产者                       | 消费者                          |
| ------------------- | ------------------------------ | ---------------------------- | ------------------------------- |
| `peer.discovered`   | `peer.Peer`                    | udp/tcp 适配器               | PeerApp → UI、PeerRepo          |
| `peer.online`       | `{nodeID, addr, rtt}`          | tcp 适配器（握手成功）       | UI、ChatApp（flush outbox）     |
| `peer.offline`      | `{nodeID, reason}`             | tcp 适配器（心跳超时）       | UI、TransferApp（标记暂停）     |
| `chat.received`     | `message.Message`              | tcp 适配器                   | ChatApp → MessageRepo + UI      |
| `chat.delivered`    | `{msgID}`                      | tcp 适配器                   | ChatApp → outbox 删除 + UI 状态 |
| `group.updated`     | `group.Group`                  | GroupApp                     | UI、GroupRepo                   |
| `transfer.state`    | `{jobID, from, to}`            | domain/transfer              | TransferRepo、UI                |
| `transfer.progress` | `{jobID, percent, speed, eta}` | TransferApp（**节流 4 Hz**） | UI                              |
| `transfer.done`     | `{jobID, path}`                | TransferApp                  | TransferRepo、UI                |
| `transfer.error`    | `{jobID, err}`                 | TransferApp                  | TransferRepo、UI                |
| `net.error`         | `{connID, err}`                | tcp/udp 适配器               | infra/log、UI 诊断面板          |
| `config.changed`    | `config.Config`                | SettingsService              | 各 App 更新快照                 |

**给 UI 的额外礼物**：因为所有内部事件都过总线，可以加一个 **调试面板**，把事件流实时打到前端。P2P 排障极难，这个面板的投入产出比极高，建议 M1 就做。

### 3.6 三个状态机

**Peer 生命周期**：

```
UNKNOWN ──announce──> DISCOVERED ──dial/accept──> CONNECTING
                            ▲                          │
                            │                          ▼
                        (TTL 过期)                 HANDSHAKING ──失败──> OFFLINE
                            │                          │ 成功
                            │                          ▼
                        OFFLINE <──heartbeat timeout── ONLINE <──> DEGRADED
                                                              (RTT 高/丢包)
```

关键：`DISCOVERED → OFFLINE` 和 `ONLINE → OFFLINE` 是两条不同路径。前者仅表示"从邻居列表里消失了"（TTL 过期），后者是"连接真的断了"。UI 要区分展示（灰色 vs 红色），否则用户会误判。

**Session 状态机**（新增，现设计没有）：

```
TCP_OPEN → HELLO_SENT/RECV → AUTH_VERIFIED → ESTABLISHED → DRAINING → CLOSED
                │                  │
             超时/失败          签名不符
                ▼                  ▼
             CLOSED(拒绝)      CLOSED(冒充告警+加入黑名单)
```

**Transfer 状态机**（在现设计基础上修正）：

```
IDLE → META_EXCHANGE → TRANSFERRING ⇄ PAUSED
                            │
                            ▼
                        VERIFYING → DONE
                            │
                          失败 → FAILED（可重试，保留位图）
```

修正点：现设计 `PAUSED` 只能从 `TRANSFERRING` 进入且没有出口。补：`PAUSED → TRANSFERRING` 需重新走 META_EXCHANGE 交换位图（因为暂停期间对端可能已从别处拿到部分块）；`VERIFYING` 失败时**不清空位图**，应标记可疑块（通过逐块 hash 定位坏块）后回到 `TRANSFERRING` 重传坏块，而非整文件重来。

### 3.7 连接拓扑（ADR-009）

**问题**：原设计没说明"节点之间是否维护持久连接"。这个决策被节点规模直接决定，而两种答案的代价差两个数量级。

**算一笔账**——若每个节点与所有其他节点维护持久 TCP 连接：

| 全局节点数 | 全网连接数 | 单节点连接数 | 单节点保活开销                  |
| ---------- | ---------- | ------------ | ------------------------------- |
| 20         | 190        | 19           | 约 40 KB，38 个 goroutine       |
| **200**    | **19,900** | **199**      | **约 400 KB，398 个 goroutine** |
| 500        | 124,750    | 499          | 约 1 MB，998 个 goroutine       |

单看资源，200 节点其实扛得住（398 个 goroutine 对一个桌面应用不算致命）。**真正的问题在心跳**：

- 199 条连接 × 每 30 s 一次心跳 ≈ 6.6 pkt/s 出站
- 对端也在发 → 单节点约 13 pkt/s 纯保活流量
- 全网 200 个节点 → **约 1,300 pkt/s 的纯保活流量，零用户价值**
- 更麻烦的是**任何一次全网抖动都变成 199 条连接同时重建** → SYN 风暴

**决策：把「在线状态」和「传输连接」彻底解耦。**

```
在线状态（谁在线、叫什么、地址是什么、支持哪些能力）
  ← UDP ANNOUNCE 维护：无连接、无状态、成本趋零
  ← 每个节点持有【全量节点目录】：200 条 × 约 64 B ≈ 12.8 KB，一次同步就够

传输连接（TCP）
  ← 按需拨号（lazy dial）：只在"要发消息 / 发文件 / 群聊扇出"时才建连
  ← 会话级保活：仅对当前有活跃会话的 peer 维持心跳
  ← 空闲回收：连续 5 min 无业务帧 → 正常关闭（DRAINING → CLOSED）
  ← 预计单节点常驻连接数：≤ 10（活跃聊天 + 活跃传输 + 群聊扇出）
```

**这个解耦带来的连锁简化**（都是 200 节点这个规模才成立的）：

1. **全量目录同步可行**：200 条条目约 12.8 KB，一次响应发完，**不需要增量 / 分页 / 分片**。原设计担心的"节点交换风暴"在 200 节点下不存在——用 `list_epoch` 做"内容没变就不回"即可。
2. **不需要分层 / 分区发现**：网段内广播 + 网段间种子单播拉取（4.5.1）足够覆盖 200 节点 / 3–5 网段。上一版列为"未来风险"的一项，现在可以永久划掉。
3. **软中继永久移出路线图**：它的触发条件是"群 > 200 人"，而全局只有 200 个节点，这个场景不存在。群聊的现实天花板就是 200 人，用 Gossip（方案 B）足以覆盖，不需要引入弱中心。
4. **心跳退化为会话级**：`Session.RTT()` 只在有连接时才有意义——而这正是滑动窗口自适应需要的那个 RTT，刚好够用。
5. **发现层不需要知道连接状态**：`PeerDirectory` 只回答"我听说过谁"，`ConnManager` 只回答"我现在连着谁"，两者互不依赖（正好落在 `ports` 接口的拆分上）。
6. **跨网段连接同样按需**：跨网段目录（4.5.1 / ADR-011）只解决"看得见"，发消息仍走按需拨号 —— **跨网段不引入任何常驻连接**。"无中心"与"低开销"这两条性质在 3–5 网段下保持不变。

**代价（必须承认）**：

- 给一个"已发现但未连接"的 peer 发首条消息，需要先拨号 + 三段握手，**首条消息延迟增加约 1 个 RTT + 握手往返**（同网段 < 5 ms；3–5 网段若是园区内的路由网段，RTT 通常也在 1–5 ms 量级 → 实际增量约 10 ms 以内；**只有跨厂区 VPN 互联时才可能到 +60 ms 以上**）
- 必须区分两种状态：**"节点在线"**（announce 说它在线）vs **"连接可用"**（真的能连上）。UI 要分别展示，否则用户看到"在线"却发不出消息会困惑
- 群聊扇出时可能 20 个成员一个都没连，需要**并发拨号**且控制并发数（默认 8），避免瞬间 20 个 SYN

**为什么不采用混合方案（热点节点保持长连 + 冷点按需）**：那需要"热点判定"启发式，出问题时难以向用户解释。200 节点的规模下，**纯按需连接已经足够**——简单胜过聪明。

---

## 4. 协议设计 v2

### 4.1 帧头 v2（一次性到位）

**现设计**：`Magic(2) | Version(1) | Type(1) | Length(4) | Payload`

**问题**：没有扩展位。加加密、压缩、分片都得改版本号，而版本号一改就要做兼容分支。

**v2**：

```
┌────────┬────────┬────────┬────────┬────────┬──────────────┐
│ Magic  │  Ver   │ Flags  │  Type  │ Length │   Payload    │
│  2 B   │  1 B   │  1 B   │  1 B   │  4 B   │    变长       │
│ 0x53 0x4C │ 0x02 │ 位掩码 │        │ 大端    │              │
└────────┴────────┴────────┴────────┴────────┴──────────────┘
  固定 9 字节头部

Flags:
  bit0 ENCRYPTED   载荷已加密（v1.1 Noise 启用）
  bit1 COMPRESSED  载荷已压缩（zstd，仅对可压缩类型）
  bit2 FRAGMENT    分片帧，非独立消息
  bit3 FRAG_END    分片结束帧
  bit4-7           保留（必须置 0，接收方遇非 0 时按未知标志忽略而非报错）
```

**兼容规则（写进协议，避免以后吵架）**：

1. 接收方**必须先按 `Length` 读完载荷再判断能否处理**，未知 Type 直接丢弃并计数，不得断连
2. `Length` 上限 16 MB（`max_frame_size`），超过即协议违规 → 关连接（防内存炸弹）
3. `Ver` 不匹配时，不走"关连接"，而是由 HELLO 阶段的版本协商提前解决

**同时统一常量**：现设计 UDP 魔数是 5 字节 `"MYAPP"`、TCP 魔数是 2 字节 `0x4D 0x59`，两套。建议统一到 `adapters/net/protocol/const.go`，UDP 用一个 4 字节 `0x53 0x4C 0x41 0x4E`（"SLAN"），TCP 用 `0x53 0x4C`（"SL"），并在常量注释里写明"这两个值属于协议 ABI，改即破坏兼容"。

### 4.2 握手与认证（ADR-002）

**目标**：证明"你确实持有该 NodeID 对应的私钥"，且防重放。

**时序**（TCP 建连后立即执行）：

```
A                                                    B
│ ── HELLO ────────────────────────────────────────> │
│    proto_min, proto_max, caps, nodeID_A,           │
│    pubKey_A, nonce_A(32B), displayName, tcpPort    │
│                                                    │
│ <──────────────────────────────── HELLO_ACK ────── │
│    proto_chosen, caps_common, nodeID_B, pubKey_B,  │
│    nonce_B(32B),                                   │
│    sig_B = Sign(H("swarmlink-hello" || nonce_A ||  │
│                  nonce_B || pubKey_A || pubKey_B)) │
│                                                    │
│  ① 校验 nodeID_B == Fingerprint(pubKey_B)          │
│  ② 用 pubKey_B 验 sig_B                            │
│                                                    │
│ ── AUTH ─────────────────────────────────────────> │
│    sig_A = Sign(H("swarmlink-hello" || nonce_A ||  │
│                  nonce_B || pubKey_A || pubKey_B)) │
│                                                    │
│                        ③ 校验 nodeID_A 与 sig_A    │
│ <────────────────────────────────── AUTH_OK ────── │
│                                                    │
│            ESTABLISHED（此后才允许业务帧）          │
```

**为什么这样设计**：

- **转录绑定（transcript binding）**：签名覆盖 `nonce_A || nonce_B || pubKey_A || pubKey_B`，任何一方篡改字段都会导致验签失败；`nonce_A` 由 A 生成、`nonce_B` 由 B 生成，**任一方都无法单方面构造可重放的转录**（这正是防重放的关键——只签自己发的 nonce 是不够的，攻击者可原样重放整个握手）
- **三段而非两段**：`HELLO_ACK` 先给 sig_B，让 A 立刻知道"对面确实持有私钥"，可以提前拒绝冒充者，避免继续交换数据
- **域名分隔哈希**（`"swarmlink-hello"` 前缀）：防止签名被挪用到其他协议上下文（比如被当作文件块签名重放）
- **握手期间不允许业务帧**：现设计没有明确这点，实现时容易出现"HELLO 还没发完就发 CHAT"的竞态

**NodeID 派生的域分隔**（顺带修正）：现设计 `NodeID = SHA-256(pubkey)[:8]`。建议改为 `SHA-256("swarmlink-node-id-v1" || pubkey)[:8]`，避免这个哈希被复用到其他用途时产生交叉协议攻击面。8 字节在 200 节点规模下碰撞概率约 1×10⁻¹⁵，可忽略——**不需要为了抗碰撞把 NodeID 加长**（加长会让 UI 上的指纹更难读，收益为负）。

**加密演进路径（三选一，见 9.1）**：

- 方案 A：手写挑战-应答 + 会话内不加密（v1.0 就够）
- 方案 B：直接集成 `flynn/noise`（Noise_XX / IK），一步拿到认证 + 前向保密 + 身份隐藏
- 方案 C：**先 A，并用 Flags.ENCRYPTED + 预留 `KEY_EXCHANGE` 消息类型占住位置，v1.1 切 B**

**建议 C**。理由：本项目的威胁模型是"内网可路由环境"，同网段的攻击者本来就能嗅探；前向保密价值有限。而 Noise 会引入新的调试面（握手失败难定位）。**先把 A 做对（认证），协议留好接缝，v1.1 再上 B**——这符合"可逆性优先"（专家规则 4）。

若走 Noise，需注意：`flynn/noise` 的 `DH25519` 需要 X25519 密钥，而 Ed25519 密钥可通过 `edwards25519` boot 到 Montgomery 形式转换，但**更干净的做法是节点额外持有一个独立的 X25519 静态密钥**（避免同一密钥跨算法复用）。

### 4.3 版本与能力协商

`HELLO` 携带：

```json
{
  "proto_min": 2,
  "proto_max": 2,
  "caps": 0b0000_0011,
  "max_frame_size": 16777216,
  "max_chunk_size": 524288,
  "window_size": 8
}
```

`caps` 位掩码定义（**这是 v1.0 最重要的一张表，它决定了未来加功能要不要改架构**）：

| 位  | 能力              | 说明                                                |
| --- | ----------------- | --------------------------------------------------- |
| 0   | `CHAT_RELIABLE`   | 支持 msg_id + ACK（不支持则降级为 fire-and-forget） |
| 1   | `GROUP_CHAT`      | 支持群消息与成员同步                                |
| 2   | `RESUME_BITMAP`   | 支持位图续传（不支持则整文件重传）                  |
| 3   | `SLIDING_WINDOW`  | 支持多块在途                                        |
| 4   | `COMPRESSION`     | 支持 zstd                                           |
| 5   | `ENCRYPTED`       | 支持加密通道                                        |
| 6   | `FOLDER_TRANSFER` | 支持目录传输                                        |
| 7   | `RELAY`           | 支持中继转发                                        |

协商结果 = `proto_max = min(A.proto_max, B.proto_max)`，`caps_common = A.caps & B.caps`。**任何新能力的启用都必须先查 `caps_common`**，这保证老节点不会被新节点的帧类型搞崩。

### 4.4 消息类型总表（扩展后）

保留现设计的 0x01–0x21，新增：

| Type | 名称                 | 方向          | 说明                                             |
| ---- | -------------------- | ------------- | ------------------------------------------------ |
| 0x05 | `AUTH`               | 双向          | 握手第三段签名                                   |
| 0x06 | `AUTH_OK`            | 双向          | 握手完成                                         |
| 0x07 | `ERROR`              | 双向          | 协议错误（带 code + 可读消息）                   |
| 0x08 | `PING_ADDR`          | 双向          | 请求对端报告其观测到的本机地址（NAT/多网卡场景） |
| 0x22 | `CHAT_ACK`           | 接收方→发送方 | 消息已落库确认（`msg_id`）                       |
| 0x30 | `GROUP_META`         | 双向          | 群元数据（group_id, name, epoch, owner, 签名）   |
| 0x31 | `GROUP_MEMBERS_REQ`  | 双向          | 拉取成员列表（带 known_epoch）                   |
| 0x32 | `GROUP_MEMBERS_RESP` | 双向          | 返回成员列表（epoch 更高时）                     |
| 0x33 | `GROUP_MSG`          | 双向          | 群消息（conv_id = group_id）                     |
| 0x40 | `KEY_EXCHANGE`       | 双向          | v1.1 加密通道预留                                |
| 0x41 | `KEY_EXCHANGE_ACK`   | 双向          | 同上                                             |

**Type 空间分配约定**（写进文档，防止以后撞号）：

- `0x00–0x0F` 会话控制
- `0x10–0x1F` 文件传输
- `0x20–0x2F` 消息可靠性
- `0x30–0x3F` 群组
- `0x40–0x4F` 安全
- `0x50–0x7F` 预留（扩展/实验）
- `0x80–0xFF` 应用层扩展（v2.0 插件）

**UDP 发现报文使用独立的编号空间**（不与上表争用）：`ANNOUNCE` / `PEER_LIST_REQ` / `PEER_LIST_RESP` / `SEED_PROBE` 承载在 UDP 上，采用**独立的 4 字节魔数 + 独立 Type 号**。理由：UDP 报文必须能被"不认识它的其他 UDP 服务"以最快速度丢弃，所以前面要有强势魔数；而 TCP 帧不需要这层保护（连接已建立，对端必是本协议）。**两套编号空间不要混用** —— 否则以后想调整 UDP 魔数时会牵动 TCP 协议的版本兼容。`SEED_PROBE` 的语义见 4.5.1。

### 4.5 发现协议 v2

**修正点汇总**：

| 问题                           | 修正                                                                                                          |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------- | --- | --- | --- | ------ | --- | ------- | --- | ----- | --- | --------- | --- | ----------------------------------------------- |
| 无 TTL，陈旧节点永久驻留       | 邻居条目带 `last_seen`，TTL 300 s（可配）；过期即从目录移除；本地缓存 600 s 硬上限                            |
| 节点交换无版本，产生风暴       | `PEER_LIST_REQ` 带 `list_epoch`；`RESP` 带 `epoch`；epoch 相同则返回空响应（省流）                            |
| 交换无上限                     | 单响应条目 ≤ 256（**200 节点规模下一次响应就能发完全量目录**）；本地目录上限 512；超过按 `last_seen` LRU 淘汰 |
| 广播可被伪造                   | ANNOUNCE 带 Ed25519 签名（对 `magic                                                                           |     | ver |     | nodeID |     | tcpPort |     | nonce |     | timestamp |     | epoch` 签名）。验签成本约 50 µs，5 s 一次可忽略 |
| 广播无退避，多节点开机同步风暴 | 广播间隔 5 s，**加 ±20% 随机抖动**；节点交换对新节点立即做一次，之后指数退避 30 s → 60 s → … → 上限 600 s     |
| 多网卡混乱                     | 见下方接口策略                                                                                                |
| 会发现自己                     | 收到 `nodeID == self` 一律丢弃（UDP 广播默认回环到自己）                                                      |
| 端口冲突（与飞秋等抢 2425）    | 启动探测：2425 被占则试 2426…2434；最终端口写进 ANNOUNCE，**对端从 announce 学习实际端口**，不依赖约定端口    |
| 广播包被其他 UDP 服务误触      | 4 字节魔数 + 严格长度校验 + 尾部校验位（非魔数包直接丢，不解析）                                              |

**多网卡接口策略**（P1-6，现设计完全缺失，但笔记本上必然出问题）：

```
1. 枚举所有接口 → 排除 loopback、link-local(169.254/16)、未 UP 的
2. 排除虚拟网卡：VMware(vmnet)、VirtualBox(vboxnet)、Docker(bridge/veth)、
   Tailscale/ZeroTier(WireGuard)、Hyper-V 的 vEthernet —— 按名称模式 + 无物理 MAC 判定
3. 剩余接口按默认路由优先级排序（读路由表 / 或按接口 flags 与 MTU 启发式）
4. 每个接口独立构造子网广播地址（由 IP + netmask 算出，不用 255.255.255.255）
5. 每个接口独立 socket 绑定该接口（SO_BINDTODEVICE on Linux / IP_BOUND_IF on macOS），
   而不是一个 0.0.0.0 socket 全收 —— 便于给用户展示"通过哪张网卡连接"
6. 允许配置 allow_interfaces / deny_interfaces 覆盖（VPN 用户刚需）
```

**注意**：第 5 点意味着需要平台特定代码（build tag：`iface_linux.go` / `iface_darwin.go` / `iface_windows.go`），因为 Go 标准库没有跨平台的接口绑定 socket option。这是本项目**唯一需要平台特定代码的地方**，建议用 `github.com/libp2p/go-netroute` 或 `golang.org/x/net/ipv4` 简化。

**建议的分层策略（交给用户配置，不自动猜）**：设置页提供三档

- 「自动」（默认，走上面 6 步）
- 「指定网卡」（下拉列出物理网卡，用户选）
- 「仅显式种子」（关闭自动发现，只用种子 IP，适合 VPN/安全敏感环境）

#### 4.5.1 跨网段发现：3–5 网段 / 手动种子（ADR-011）

**问题**：UDP 广播止步于路由器。3–5 个网段意味着 3–5 个互不可见的"广播岛"——岛内节点能互相发现，**岛与岛之间在对方目录里完全不存在**。

**第一个判断：种子不是"服务器"，种子是"配了更多种子地址的普通节点"。**

常见做法是给种子加特殊角色（转发消息、维护权威目录），那会立刻长出中心节点，与"无中心"的核心卖点冲突。本方案不做：

| 常规做法                                | 本方案                                          | 好处                                                             |
| --------------------------------------- | ----------------------------------------------- | ---------------------------------------------------------------- |
| 种子是特权角色，转发消息 / 维护权威目录 | 种子**只是**配置里多列了几个 IP 的普通节点      | **零特权、零状态、可随时替换**；种子宕机不影响已建立的消息与传输 |
| 种子之间跑一套独立的目录同步协议        | 种子与普通节点跑**同一套** `PEER_LIST_REQ/RESP` | 只维护一条代码路径；种子的"特殊性"完全外置到配置文件             |

**第二个判断：跨网段是"报文的形状"，不是"以后要加的功能"。**（这也是它必须进 M1 的原因）

**机制（三句话）**：

1. **网段内**：`ANNOUNCE` 广播（5 s 周期 + ±20% 抖动），同网段节点自动互相发现 —— 与单网段场景**完全一致，零改动**。
2. **网段间**：节点按 `[discovery.seeds]` 清单，向**可达的种子**单播 `PEER_LIST_REQ`；种子的 `PEER_LIST_RESP` 返回**它手里的全量目录**（含它从别的网段学来的条目）。
3. **种子自身**：种子用的是同一份清单，所以种子之间用同一个机制互相拉取 —— **"种子间目录交换"不需要任何专门代码**。

```
  ┌──────── 网段 A ────────┐    ┌──────── 网段 B ────────┐    ┌──────── 网段 C ────────┐
  │  A1   A2  [S_A][S_A2] │    │  B1   B2  [S_B][S_B2] │    │  C1   C2  [S_C][S_C2] │
  │       ▲ 广播 5s        │    │       ▲ 广播 5s        │    │       ▲ 广播 5s        │
  └───────────┬───────────┘    └───────────┬───────────┘    └───────────┬───────────┘
              │                            │                            │
              └────── 种子两两单播 PEER_LIST_REQ/RESP（共用一份清单）──────┘
                     每网段 ≥ 2 种子 → 单点失效降级为"目录刷新变慢"
```

**为什么每网段要 ≥ 2 个种子**：种子只是配置里的一个 IP。该机器关机或换 IP，**整个网段对其他网段就消失了**（网段内不受影响，但网段外看不到它）。每网段 2 个种子把它降级为"刷新变慢"。3–5 网段 × 2 = 6–10 条 IP，手工维护完全可控。

**一处硬约束：种子必须固定 UDP 端口。** ADR-008 为普通节点设计了"端口被占则自动回退"，但**被选为种子的机器必须关掉回退、固定 `udp_port`** —— 否则跨网段节点拿着配置里的端口发第一个报文就石沉大海，而它没有任何其他途径知道种子换了端口（种子的广播同样不跨路由器）。第一跳怎么走，是个容易写错的细节：

```
第 1 跳：UDP  →  节点向种子的 [ip:udp_port] 发带魔数的探测包
                 种子【单播】回一份自己的 ANNOUNCE（签名完整）
第 2 跳：TCP  →  节点从这份 ANNOUNCE 学到种子的实际 tcp_port / node_id / pub_key
                 据此 TCP 拨号做 PEER_LIST_REQ
```

这样**只有一个端口需要固定**，`tcp_port` 照样可以回退；而且回传的 ANNOUNCE 带 Ed25519 签名，**种子身份从第一跳起就可验证**，不给中间人留口子。

**节点该拉哪些种子（v1.0 取简法）**：**每次随机选 2 个可达种子，成功则合并响应**。理由：让 200 个节点轮询同一颗种子会造成无谓热点；随机 2 个既保证了"单颗种子宕机仍能拿到跨网段目录"，又天然分散了负载。**不要**让节点去拉全部 6–10 个种子——那是 6–10 倍的无效流量，换来的是 0 的额外可靠性。

**收敛性与防环 —— 不需要 `via` 字段，也不需要来源标记**：

这是本方案能保持简洁的关键。跨网段目录天然会重复到达（A 网段节点既从广播听到 A1，也可能从某颗种子的响应里再收到 A1），但只要遵守两条规则就无需任何防环逻辑：

1. **条目按 `node_id` 去重**：同一节点无论从哪条路径学来，只占一条记录。
2. **TTL 用「本地到达时间」计，不用对端时间戳**：节点收到条目时记 `last_heard_local = now`（本地单调时钟），TTL 判断只跟这个值比。**跨网段节点的时钟偏差因此完全不影响正确性**。

因为条目的新鲜度由接收方本地维护、**不会被中转"刷新"**，所以不存在"陈旧条目被反复回灌"的问题 —— `via` / `hop_count` / `source` 标记一概不需要。**这是一个必须写进代码注释的细节**，否则实现时极易顺手拿对端 `timestamp` 去比，那样一旦两台机器时钟不同步就会出现"活着的节点被判过期"或"死掉的节点永不消失"。

**成本核算（复用了 ADR-010 的 epoch 优化后，流量趋近于零）**：

| 项             | 量                                  | 说明                                                                                                   |
| -------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 单次拉取响应   | 12.8 KB（目录未变时 **≈ 0**）       | `PEER_LIST_REQ` 携带本地缓存的 `list_epoch`；epoch 未变则返回**空响应**。目录稳定时几乎不走数据        |
| 种子被拉取频率 | 200 节点 × (1 / 300 s) ≈ 0.67 req/s | 分摊到 6–10 颗种子、且每节点只拉 2 颗，单颗种子 < 0.1 req/s                                            |
| 单种子出站     | **< 2 KB/s**（仅目录变更时上量）    | 对现代网络可忽略；**种子不需要任何特殊资源规格**                                                       |
| 跨网段收敛延迟 | ≤ 1 个刷新周期（默认 5 min）        | 新节点加入 → 本网段种子 5 s 内学到 → 其他网段 ≤ 5 min 可见。如需更快，v1.2 再加"种子目录变更主动 PUSH" |

**跨网段首条消息**：目录里已有对端 IP，直接 TCP 拨号即可，**不需要任何中继**。这正是 P-1（网段间三层可达）是硬前提的原因。

**部署前置条件**：P-1 / P-2 见 0.5 节。其中 **P-2（地址段不得重叠）在"多厂区/多门店 VPN 互联"场景里是高频真实风险**——建议 **M1 就把 `subnet` 字段放进 ANNOUNCE**，这样 P-2 的检测在第一个原型里就跑通，而不是等现场部署才发现。

### 4.6 群聊设计（ADR-003）

**数据模型**：

```go
type Group struct {
    ID         string          // 群主生成：SHA-256(ownerNodeID || createdAt || name)[:16]
    Name       string
    OwnerID    identity.NodeID
    Epoch      int64           // 成员表版本，任何成员变更 +1
    StateSig   []byte          // Owner 对 H(group_id|epoch|members) 的签名
    Members    []Member
    CreatedAt  time.Time
}

type Member struct {
    NodeID      identity.NodeID
    DisplayName string
    Role        MemberRole   // owner | admin | member
    JoinedAt    time.Time
    State       MemberState  // active | left | kicked
}
```

**成员同步协议**（v1.0 的简化版）：

1. 群主变更成员 → `Epoch++` → 用私钥签 `H(group_id|epoch|members)` → 向所有在线成员推 `GROUP_META`
2. 成员收到后：验签（必须用 `OwnerID` 对应公钥）→ `epoch > local.epoch` 则接受并覆盖本地成员表
3. 新成员加入前，先 `GROUP_MEMBERS_REQ{known_epoch: 0}` 从任一成员拉最新快照
4. 成员发现 `epoch` 冲突（两个不同成员表同 epoch）→ **以 owner 签名为准**；owner 长期离线（> 24 h）→ 群进入"只读"状态并在 UI 提示，不自动选举（v1.0 简化，把问题暴露给用户而非悄悄出错）

**消息扇出（v1.0 = 方案 A）**：

```
发送方 A 发消息 M(conv_id=group_id, msg_id=uuidv7)
  → 持久化到本地 messages 表
  → 查 Group.Members 中 state=active 且在线的成员
  → 对每个成员：Session.Send(GROUP_MSG)，独立 goroutine
  → 每个成员返回 CHAT_ACK{msg_id}
  → 全部 ACK 或超时后，更新 UI 送达状态（"3/5 已送达"）
```

**20 人上限的依据（在 ADR-009 的按需连接模型下重新核算）**：因为不维护全互联持久连接（3.7），群聊扇出时是**临时并发拨号**——20 人 = 最多 19 次并发拨号。按并发上限 8（`max_dial_concurrency`）分批，实际占用 8 条连接 × 2 goroutine = 16 goroutine，内存约 0.3 MB。因此：

- v1.0 上限 20 人：保守取值，UI 明示
- **v1.1 可提到 50 人**：50 人 = 49 次扇出拨号，分批后峰值连接数可控（约 0.8 MB）；这也是方案 A 在本项目约束下的实际上限
- **50~200 人必须切方案 B（Gossip）**：不是因为连接扛不住，而是因为单播扇出的总发送次数是 O(N²)（50 人群发一条消息 = 2,450 次单播），而 Gossip 只需向邻居转发
- **200 人是绝对天花板**：等于全局节点数，不存在"更大群"的场景。这也是方案 C（软中继）被永久移出的原因

**离线成员处理**：v1.0 **不做**离线消息暂存转发（需要可靠存储转发节点，等价于引入弱中心）。UI 明确标注"X 人未送达"，并提供"重发"按钮。这是诚实的取舍。

**切换到方案 B（Gossip）的接缝**（现在就要留）：

- 消息里已有 `msg_id` + `conv_id`（去重的前提）
- 加一个 `Hop` 字段和 `seen_cache`（LRU，去重表）
- 加 `GROUP_MSG` 的转发逻辑：收到非自己的群消息 → 转发给"除来源外、且不在 seen_cache 里"的邻居
- 加一个 `FANOUT_MODE` capability，让新旧节点混合组网时不出错

### 4.7 消息可靠性与去重（ADR-004）

**语义**：at-least-once 投递 + 接收端幂等去重 → 对用户等效 exactly-once。

**发送侧**：

```
1. msg_id = UUIDv7()（时间有序，便于 UI 排序与游标分页）
2. 单事务写入：messages(state='pending') + outbox(msg_id, conv_id, next_try_at=now)
3. Session.Send(CHAT{msg_id, conv_id, content, sent_at})
4. 收到 CHAT_ACK{msg_id} → messages.state='delivered' + outbox 删行
5. 未收到 → 由 outbox 扫描器重发（退避：1s, 2s, 4s…上限 5min）
6. 对端离线 → outbox 保留；收到 peer.online 事件 → 立即 flush 该 peer 的所有 pending
```

**为什么用 outbox 表而不是内存队列**：进程崩溃后未送达消息不能丢。这是"聊天工具"最基本的承诺。

**接收侧（幂等）**：

```
1. 收到 CHAT → AppendIfAbsent(msg)
   - 若 inserted=true：触发 chat.received 事件（更新 UI）
   - 若 ErrDuplicate：**不**触发 UI 事件，但仍回 CHAT_ACK
2. 无论是否重复，都要回 CHAT_ACK（否则发送方会无限重发）
3. 顺序：单会话内按 sent_at 排序展示；同 sent_at 按 msg_id 二次排序（UUIDv7 时间有序，结果稳定）
```

**关键细节**：重复消息**必须回 ACK**。这是很多实现的经典 bug——去重后直接 return，导致发送方永远收不到确认、无限重发。

**群消息去重**：`msg_id` 全局唯一，所以群消息也走同一张表、同一个 `UNIQUE INDEX`。这就是为什么 G1 的"提前引入 msg_id"是划算的——它同时解决了单聊可靠性和群聊去重两个问题。

**不做的**：已读回执（v1.0）、消息编辑/撤回、端到端消息排序（跨会话全局序不需要）。

### 4.8 文件传输：滑动窗口 + 周期性位图（ADR-005）

**现设计的问题**：stop-and-wait（每块等 ACK），跨网段 RTT 20 ms 时吞吐被限制在 `512KB / 20ms = 25 MB/s`，而千兆网是 125 MB/s。且无法利用 TCP 本身的窗口机制。

**v2 设计**：

```
发送方                                    接收方
  │ ── FILE_META{job_id,file_id,size,┐      │
  │      chunk_size,total_chunks}    │      │
  │ ────────────────────────────────>│      │
  │ <──────────── FILE_META_ACK{     │      │
  │      completed_bitmap(全量)}     │      │
  │                                  │      │
  │ ┌── 窗口 W = 8 ──┐               │      │
  │ │ 连续发 8 块，不等 ACK          │      │
  │ └────────────────┘               │      │
  │ ── FILE_CHUNK[0..7] ──────────────────> │  写临时文件（WriteAt，offset 已知，天然支持乱序）
  │                                          │  位图置位（内存）
  │ <── FILE_CHUNK_ACK{bitmap 全量} ──────── │  每 500 ms 或每 32 块回一次
  │                                          │
  │ 按 ACK 位图补发未完成的块（丢包重传）      │
  │ ...                                      │
  │ ── FILE_DONE ─────────────────────────>  │
  │                                          │  整体 SHA-256 校验
  │ <── FILE_DONE_ACK{ok: true} ───────────  │
  │                                          │  os.Rename(tmp, final) 原子替换
```

**四个设计决策及其理由**：

1. **ACK 回传全量位图而非逐块序号**
   位图大小 = `total_chunks / 8` 字节。1 GB 文件 = 256 B，50 GB 文件 = 12.5 KB。回传成本可忽略，但**换来极致的鲁棒性**：不需要处理累积 ACK + 选择性 ACK 的复杂语义，乱序、丢包、重复到达全都自动被位图吸收。**这是本设计里"用带宽换复杂度"的最佳一笔交易。**

2. **块乱序到达可直接写入**
   每个块自带 `offset`，接收方用 `file.WriteAt(buf, offset)`（或 `ReadAt` 语义对称），无需等前序块。这让重传只需补洞，大幅减少"一个块丢了要等一整轮"的延迟。

3. **窗口大小自适应**
   `W = clamp(BDP / chunk_size, 1, 64)`，其中 `BDP = RTT × 实测吞吐`。RTT 从心跳帧测得（`Session.RTT()`），吞吐从最近 1 s 的实际写入速率算。**初始 W=8，每 2 个 RTT 调整一次**。简单版：LAN（RTT < 1 ms）用 W=16，跨网段用 W=32。**不要**自己实现 TCP 拥塞控制——我们跑在 TCP 之上，这里的窗口只是为了填满管道，不需要处理拥塞（TCP 已经在处理了）。

4. **位图批量落盘（修正 P1-9）**
   内存维护位图 + 计数器，**每 2 s 或每 256 块**触发一次 `SaveBitmap`（单事务）。崩溃最多丢 2 s 的进度（重传 256 × 512 KB = 128 MB），可接受。相比每块一次事务，DB 写次数下降 256 倍。

**分块大小的再审视**：现设计固定 512 KB。建议：

| 场景                  | 建议块大小 | 理由                                                |
| --------------------- | ---------- | --------------------------------------------------- |
| LAN（RTT < 1 ms）     | 1 MB       | 减少帧开销与 ACK 频率                               |
| 跨网段（RTT > 10 ms） | 256 KB     | 降低单块重传成本，提高丢包恢复速度                  |
| 默认                  | 512 KB     | 现设计值，作为 `hello` 协商的 `max_chunk_size` 缺省 |

**块大小在 `FILE_META` 中传递**（已有 `chunk_size` 字段），所以两端可以不同——只要发送方按自己声明的块大小切，接收方按元数据解析即可。**但要写入协议约定：`chunk_size` 必须是 2 的幂**，便于位图索引与 offset 计算。

**并发与带宽预算（修正 P1-8）**：

- 全局最多 `N` 个活跃传输（默认 3），`N` 可配置
- 单对端最多 1 个活跃传输（v1.0 简化；v1.2 放开）
- 全局令牌桶限速（默认不限，可配 `max_upload_mbps`）
- 超出的传输进队列（`transfer_jobs.status = 'queued'`），UI 显示排队位置

**失败恢复**：

- `VERIFYING` 阶段整体哈希不匹配 → 逐块用 `chunk_hashes` 定位坏块（`FILE_META` 已含完整 `chunk_hashes`）→ 只重传坏块 → 重新校验
- **注意 `chunk_hashes` 的内存开销**：50 GB / 512 KB = 102400 块 × 32 B = 3.3 MB，可接受。但现设计说"续传时只发缺失块哈希"——建议**始终发全量 chunk_hashes**（3 MB 对 TCP 是小数目），换取接收方能在任意时刻独立定位坏块。简化 > 省流。

---

## 5. 数据模型 v2

```sql
-- ① 迁移基础设施（P2-2）
CREATE TABLE IF NOT EXISTS schema_migrations (
    version    INTEGER PRIMARY KEY,
    applied_at INTEGER NOT NULL,
    checksum   TEXT NOT NULL
);

-- ② 会话表：从 peer_id 主键改为 conv_id，承载单聊 + 群聊
CREATE TABLE IF NOT EXISTS conversations (
    conv_id      TEXT PRIMARY KEY,          -- 单聊 = peer_id；群聊 = group_id
    kind         TEXT NOT NULL CHECK(kind IN ('direct','group')),
    title        TEXT NOT NULL,
    peer_addr    TEXT,
    last_msg_id  TEXT,
    last_time    INTEGER,
    unread       INTEGER DEFAULT 0,
    is_online    INTEGER DEFAULT 0,
    pinned       INTEGER DEFAULT 0
);

-- ③ 消息表：加 msg_id 去重 + 承载状态
CREATE TABLE IF NOT EXISTS messages (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    msg_id     TEXT NOT NULL,               -- UUIDv7
    conv_id    TEXT NOT NULL,
    sender_id  TEXT NOT NULL,               -- 群聊需要；单聊等于对端
    direction  TEXT NOT NULL CHECK(direction IN ('in','out')),
    content    TEXT NOT NULL,
    msg_type   TEXT NOT NULL DEFAULT 'text',
    file_id    TEXT,
    sent_at    INTEGER NOT NULL,
    recv_at    INTEGER,
    state      TEXT NOT NULL DEFAULT 'pending'
               CHECK(state IN ('pending','sent','delivered','failed')),
    FOREIGN KEY (conv_id) REFERENCES conversations(conv_id)
);
CREATE UNIQUE INDEX IF NOT EXISTS ux_msg_id       ON messages(msg_id);
CREATE INDEX        IF NOT EXISTS ix_msg_conv_time ON messages(conv_id, sent_at DESC, id DESC);

-- ④ outbox：未确认消息（G4）
CREATE TABLE IF NOT EXISTS outbox (
    msg_id      TEXT PRIMARY KEY,
    conv_id     TEXT NOT NULL,
    attempts    INTEGER NOT NULL DEFAULT 0,
    next_try_at INTEGER NOT NULL,
    created_at  INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS ix_outbox_next ON outbox(next_try_at);

-- ⑤ 群组（G1）
CREATE TABLE IF NOT EXISTS groups (
    group_id   TEXT PRIMARY KEY,
    name       TEXT NOT NULL,
    owner_id   TEXT NOT NULL,
    epoch      INTEGER NOT NULL DEFAULT 0,
    state_sig  TEXT,
    created_at INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS group_members (
    group_id     TEXT NOT NULL,
    node_id      TEXT NOT NULL,
    display_name TEXT,
    role         TEXT NOT NULL DEFAULT 'member',
    state        TEXT NOT NULL DEFAULT 'active',
    joined_at    INTEGER NOT NULL,
    PRIMARY KEY (group_id, node_id)
);

-- ⑥ 节点目录（替代把在线状态塞在 conversations 里）
CREATE TABLE IF NOT EXISTS peers (
    node_id      TEXT PRIMARY KEY,
    display_name TEXT,
    pub_key      TEXT,                       -- base64 Ed25519 公钥（用于验签）
    last_addr    TEXT,
    caps         INTEGER DEFAULT 0,
    proto_ver    INTEGER DEFAULT 1,
    first_seen   INTEGER,
    last_seen    INTEGER,                    -- 【本地时钟的到达时刻】ADR-011 明确语义：
                                             -- TTL 只与它比较，绝不用对端 timestamp（跨网段时钟偏差）
    subnet       TEXT,                       -- 该节点所在子网（ANNOUNCE 携带）→ P-2 地址重叠检测
    state        TEXT DEFAULT 'unknown'
                 CHECK(state IN ('unknown','discovered','online','offline','blocked')),
    source       TEXT DEFAULT 'broadcast'    -- broadcast|multicast|seed|manual|gossip
);
CREATE INDEX IF NOT EXISTS ix_peers_seen ON peers(last_seen DESC);

-- ⑦ 传输任务：补充分块与窗口元数据
CREATE TABLE IF NOT EXISTS transfer_jobs (
    job_id        TEXT PRIMARY KEY,
    peer_id       TEXT NOT NULL,
    file_name     TEXT NOT NULL,
    file_size     INTEGER NOT NULL,
    file_hash     TEXT NOT NULL,
    chunk_size    INTEGER NOT NULL DEFAULT 524288,
    total_chunks  INTEGER NOT NULL DEFAULT 0,
    direction     TEXT NOT NULL CHECK(direction IN ('send','recv')),
    local_path    TEXT NOT NULL,
    temp_path     TEXT,
    completed     INTEGER DEFAULT 0,
    chunk_bitmap  BLOB,
    status        TEXT NOT NULL DEFAULT 'active'
                  CHECK(status IN ('queued','active','paused','verifying','done','failed','cancelled')),
    window_size   INTEGER NOT NULL DEFAULT 8,
    retry_count   INTEGER NOT NULL DEFAULT 0,
    error         TEXT,
    created_at    INTEGER NOT NULL,
    updated_at    INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS ix_transfer_peer_hash ON transfer_jobs(peer_id, file_hash);
CREATE INDEX IF NOT EXISTS ix_transfer_status    ON transfer_jobs(status, updated_at DESC);

-- ⑧ 种子节点（保留；ADR-011 明确了它的语义：种子只是"值得先问一声的地址"）
CREATE TABLE IF NOT EXISTS seed_nodes (
    addr       TEXT PRIMARY KEY,             -- "ip:udp_port"，直接来自配置文件；主键是它因为
                                             -- 探测之前根本不知道 node_id（无环外键指向 peers）
    node_id    TEXT,                         -- 探通后由回传的 ANNOUNCE 填充（NULL = 尚未探通）
    subnet     TEXT,                         -- 该种子所在子网 → P-2 地址重叠检测
    tcp_port   INTEGER,                      -- ANNOUNCE 学到的实际端口（ADR-008 回退后可能非 2425）
    last_seen  INTEGER,                      -- 本机最近一次探通时刻（本地时钟）
    last_epoch INTEGER,                      -- 最近一次目录响应的 list_epoch → 相同则不发请求
    is_manual  INTEGER DEFAULT 1,            -- v1.0 全部来自配置文件
    fail_cnt   INTEGER DEFAULT 0             -- 连续失败计数 → 指数退避，避免反复探测已下线的种子
);
```

**几处修正说明**：

| 修正                                                     | 原因                                                                                      |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `conversations` 主键 `peer_id → conv_id`                 | 群聊必需（G1）。**这是唯一一处破坏性变更，越早做越便宜**                                  |
| `messages` 加 `UNIQUE(msg_id)`                           | 幂等去重（G4）+ 群消息共享同一套去重逻辑                                                  |
| `messages` 加 `state`                                    | 送达状态 UI（"已发送/已送达/失败"）                                                       |
| 新增 `outbox`                                            | 崩溃后可恢复重发（G4）                                                                    |
| 新增 `peers`                                             | `conversations` 同时装"会话"和"节点在线状态"是职责混淆；群聊节点也要有 `pub_key` 用于验签 |
| `transfer_jobs` 加 `chunk_size/total_chunks/window_size` | 续传时需重建位图长度；窗口自适应需要                                                      |
| `transfer_jobs.status` 加 `queued/verifying/cancelled`   | 与 3.6 状态机对齐（现设计只有 `active`，无法表达排队与校验中）                            |
| `completed` 字段保留                                     | 虽然可由位图推导，但高频 UI 查询（进度百分比）走整数比解位图快，属于合理冗余              |

**WAL 与连接池**（现设计已对，补充说明）：

- `_journal_mode=WAL&_busy_timeout=5000&_synchronous=NORMAL` 正确。补充 `_foreign_keys=on`（Go 驱动默认不开外键约束）
- `SetMaxOpenConns(5)` 合理。但要注意 **modernc 驱动下写操作仍是串行的**（SQLite 单写者），5 个连接里同时只有 1 个能写。因此批量落盘（4.8 第 4 点）比调连接数更有效
- 建议加一个**单写 goroutine**（写队列）统一处理所有写操作，避免多连接争锁 + `busy_timeout` 等待。这是一个常见的 SQLite 性能陷阱

---

## 6. 架构决策记录（ADR）

> 建议把每一条拆成 `docs/adr/ADR-00X-<slug>.md` 独立文件，纳入版本管理。下面是正文。

### ADR-001：采用六边形架构 + 事件驱动核心

**Status**：Proposed
**Context**：现设计按技术分层（service/transport/store），依赖横穿（transport 直接调 store）。系统本质是多个 I/O 适配器围绕一个领域核心。需要可测试性（状态机必须能脱离网络与数据库单测）与可扩展性（新增发现方式、新增消息类型不应波及全栈）。
**Decision**：采用四层结构（入站适配器 / 应用层 / 领域核心 / 出站适配器），`domain/**` 零外部 I/O 依赖，跨模块通信只走 `ports` 接口或 `infra/eventbus`，`main.go` 为唯一组合根。
**Consequences**

- 更好：`domain/transfer` 状态机可 100% 单测；换存储/换传输不动业务代码；依赖方向可用工具强制
- 更差：多约 20% 胶水代码；IDE 跳转失效（事件驱动）；**需要团队纪律，一旦有人违规 import 就退化**
- 不采纳的替代：微服务（单进程应用，引入 IPC 与运维复杂度无收益）；纯分层（无法解决测试与横穿依赖）

### ADR-002：握手引入 Ed25519 挑战-应答认证

**Status**：Proposed
**Context**：项目已投入 Ed25519 密钥对做节点身份（P0），但握手仅明文交换 NodeID，身份可被任意冒充。E2E 加密列 P1，但缺少认证时加密无意义。
**Decision**：三段式握手（HELLO / HELLO_ACK / AUTH），双方对 `H(域分隔串 || nonce_A || nonce_B || pubKey_A || pubKey_B)` 签名，验签通过方进入 ESTABLISHED，握手期不允许业务帧。
**Consequences**

- 更好：身份不可冒充；防重放（双方 nonce 共同参与签名）；为 v1.1 加密预留干净接入点
- 更差：握手增加 1 个 RTT（LAN < 1 ms，可忽略）；需维护"握手未完成即拒绝业务帧"的状态机（如果不做，会出现竞态）
- 不采纳的替代：直接上 Noise（推迟 v1.0 交付，调试成本高）；不做认证（身份体系失去意义）

### ADR-003：群聊采用"全互联单播扇出 + 群状态 epoch 签名"

**Status**：Proposed
**Context**：群聊是 P0 但零设计。无中心网络下群聊的核心难点是消息可达性与成员一致性。
**Decision**：v1.0 采用全互联单播扇出，群成员上限 20；群状态由 owner 签名并以 `epoch` 单调递增传播；数据模型与协议按未来切换 Gossip 的形状预留（`msg_id`、`conv_id`、capability 位）。
**Consequences**

- 更好：与"无中心"约束完全一致；实现简单；成员一致性有明确的仲裁规则（owner 签名）
- 更差：20 人上限（写入 UI 约束）；群主长期离线则群只读；**不做离线消息暂存**，UI 必须诚实标注"X 人未送达"
- 升级路径：切 Gossip 只需加 `Hop` 字段 + `seen_cache` + 转发逻辑，数据层不动
- 不采纳的替代：软中继（**在全局 ≤ 200 节点的约束下该方案永远不需要**——群上限即 200，用 Gossip 即可覆盖）；洪泛（v1.0 规模下不必要，v1.1 群 > 50 人时启用）

### ADR-004：消息可靠性采用 "msg_id + ACK + outbox"

**Status**：Proposed
**Context**：现设计 CHAT 为 fire-and-forget，连接断开即丢消息，且无去重导致重复入库。TCP 只保证单连接内的可靠有序。
**Decision**：发送方生成 UUIDv7 `msg_id`，入库同时写 `outbox`；接收方幂等写入并**无条件回 ACK**；未确认消息由 outbox 退避重发，对端上线时 flush。语义为 at-least-once + 幂等去重。
**Consequences**

- 更好：断线不丢消息；重复到达被吸收；顺带解决群聊去重；送达状态可展示
- 更差：多一张表与一个后台扫描器；`msg_id` 生成需引入 UUID 库（用 `google/uuid` 即得 V7）
- 关键坑：**重复消息也必须回 ACK**，否则发送方无限重发（这是本设计最容易被实现错的点）

### ADR-005：文件传输采用滑动窗口 + 周期性全量位图回传

**Status**：Proposed
**Context**：现设计 stop-and-wait（每块等 ACK），跨网段 RTT 下吞吐受限；且块乱序无法利用；位图每块落盘会拖垮 SQLite。
**Decision**：窗口 W 块在途（初始 8，按 RTT 自适应至 64）；接收方每 500 ms 或每 32 块回传**全量位图**（非累积 ACK）；位图内存维护、每 2 s 或每 256 块批量落盘一次；`FILE_META` 始终携带全量 `chunk_hashes` 以支持坏块定位。
**Consequences**

- 更好：吞吐不再受 RTT 限制；乱序/丢包/重复由位图自动吸收，ACK 语义极简；DB 写次数下降 256 倍
- 更差：ACK 流量略增（1 GB 文件约 256 B × ACK 次数，可忽略）；需要窗口管理代码
- 不采纳的替代：累积 ACK + SACK（语义复杂，收益不明显）；实现自有拥塞控制（跑在 TCP 之上，TCP 已在处理拥塞）

### ADR-006：协议帧头一次性扩展为 Magic/Ver/Flags/Type/Length + 能力协商

**Status**：Proposed
**Context**：现帧头无 Flags，加加密/压缩/分片必须改版本号；无版本协商，加字段即破坏兼容。
**Decision**：帧头固定 9 字节并引入 Flags 位掩码与 16 MB 帧上限；HELLO 协商 `proto_min/max` 与 8 位 `caps`；未知 Type 必须按 Length 跳过后丢弃，不得断连。
**Consequences**

- 更好：后续加加密（Flags.ENCRYPTED）、压缩、分片零协议破坏；新老节点混网可用（能力降级）
- 更差：头部从 8 B 增至 9 B（可忽略）；所有解析代码必须严格遵守"先读长度、未知即跳过"

### ADR-007：依赖倒置 —— domain 零 I/O，ports 集中定义

**Status**：Proposed
**Context**：现设计中 transfer 状态机直接依赖 SQLite，导致无法单测；`Store` 为大对象，chat/transfer/seed 全塞一起。
**Decision**：`domain/ports` 独立包集中定义全部接口；`adapters/store/sqlite` 与 `adapters/store/mem` 为对等实现；引入 `Clock` 接口使时间可控。
**Consequences**

- 更好：状态机可脱离 I/O 测试；`test/e2e` 可用内存适配器在单进程跑多节点；时间相关逻辑（心跳超时、TTL、退避）可确定性测试
- 更差：接口数量增加（约 10 个）；需要防止"为每个方法建一个接口"的过度设计

### ADR-008：端口与网卡策略交给用户显式选择

**Status**：Proposed
**Context**：端口 2425 与飞秋等同类工具冲突；笔记本多网卡（WiFi/Ethernet/VPN）下自动选择必然出错。
**Decision**：TCP/UDP 端口启动时探测占用并自动回退（2425→2434），实际端口写入 ANNOUNCE 供对端学习；网卡提供三档策略（自动 / 指定网卡 / 仅种子），不自动猜测 VPN 场景。
**Consequences**

- 更好：与飞秋共存；VPN 用户可用；用户能知道"走的哪张网卡"
- 更差：需要平台特定代码（接口绑定 socket option）；UI 增加配置项

### ADR-009：在线状态与传输连接解耦（不建立全互联持久连接）

**Status**：Proposed
**Context**：全局节点数确认为 ≤ 200。原设计未说明节点间是否维持持久 TCP 连接。若采用全互联持久连接，全网 19,900 条连接会产生约 1,300 pkt/s 的纯保活流量，且网络抖动或重启会触发 199 条连接同时重建的 SYN 风暴。桌面应用不应承担这种无收益的常驻开销。
**Decision**：在线状态由 UDP ANNOUNCE 维护（无连接、成本趋零），每个节点持有全量节点目录（200 条目 ≈ 12.8 KB，一次同步发完）；TCP 连接**按需拨号 + 会话级保活 + 5 min 空闲回收**，单节点常驻连接数目标 ≤ 10。
**Consequences**

- 更好：零保活流量；无 SYN 风暴风险；发现层与连接层彻底解耦（`PeerDirectory` 与 `ConnManager` 互不依赖，正好落在 `ports` 接口拆分上）；`Session.RTT()` 天然只在有连接时存在，正好服务滑动窗口自适应
- 更差：给"已发现但未连接"的节点发首条消息需先建连 + 握手，**首条消息延迟 +1 RTT 与一次握手**（LAN < 5 ms，跨网段约 +60 ms）；必须严格区分 UI 上的"节点在线"与"连接可用"两种状态；群聊扇出需并发拨号（并发上限 8），避免瞬间 SYN 密集
- 不采纳的替代：全互联持久连接（1,300 pkt/s 零收益开销 + SYN 风暴）；混合方案"热点长连 + 冷点按需"（依赖热点判定启发式，出问题难以向用户解释，200 节点规模下不值得）

### ADR-010：全量节点目录同步，不做增量/分页

**Status**：Proposed
**Context**：全局 200 节点的约束下，节点目录体积是可精确计算的：200 条 × 约 64 B ≈ 12.8 KB。而"增量同步""分页拉取""目录分片"这些机制会带来版本向量、断点续传、乱序合并等一系列复杂度。
**Decision**：节点目录以**全量快照**方式同步。`PEER_LIST_REQ` 携带本地 `list_epoch`，`PEER_LIST_RESP` 在 epoch 不同时返回**完整目录**；单响应条目上限 256（覆盖全量 + 余量）。本地目录上限 512，超出按 `last_seen` LRU 淘汰。
**Consequences**

- 更好：实现极简（"内容没变就不回，变了就全发"）；无需版本向量与增量合并；12.8 KB 的传输成本在整个节点生命周期里可忽略；调试时"拉一次目录就能看到全局状态"，排障成本极低
- 更差：目录每次变更都要全量传播（但 200 节点下 12.8 KB 是可以忽略的代价）；若未来节点数增长到数千，此决策需推翻——**这正是一条"随规模而定"的决策，规模前提变了必须重新评审**

### ADR-011：跨网段采用"每网段多种子 + 单跳目录拉取"，不设引导节点

**Status**：Proposed
**Context**：已确认 3–5 个网段、种子手动维护。UDP 广播不跨路由器，网段之间必须有额外机制才能互相可见。候选方案三类：(A) 每网段配种子做单播目录拉取；(B) 设一个只读引导节点统一下发种子列表；(C) 引入中继节点转发消息。**B 与 C 都会长出"事实上的中心节点"**，与"无中心"这个核心卖点直接冲突。
**Decision**：

- 采用方案 A —— **网段内广播 + 网段间种子单播 `PEER_LIST_REQ/RESP`**；每网段配 **≥ 2 颗**种子；种子**不具任何特权**，与普通节点跑**同一套**目录交换协议，"特殊性"完全外置到配置文件。
- 节点每次**随机拉 2 颗可达种子**并合并响应（而非拉全部），避免种子热点。
- 条目按 `node_id` 去重、TTL 用**本地到达时间**计 → 跨网段目录无需 `via` / `hop_count` / 来源标记即可防环。
- **不设引导节点、不设中继。**
  **Consequences**
- 更好：零特权、零状态，种子可随时替换增删；只维护一条目录交换代码路径；复用 ADR-010 的 `list_epoch` 后单颗种子出站 **< 2 KB/s**，**种子机器不需要任何特殊规格**；跨网段首条消息直接拨号，无转发跳数
- 更差：**依赖两条部署前置条件** —— **P-1** 网段间三层可达（若为 NAT 隔离则方案失效，只能引入中继）、**P-2** 各网段地址段不重叠（否则寻址崩溃且现象隐蔽、极难排查）；新节点跨网段可见有 ≤ 1 个刷新周期（默认 5 min）的延迟；单颗种子失效会让该网段"对网段外暂时不可见"（靠 ≥ 2 种子缓解）
- **不可逆性提示**：P-2 若不满足，补救成本在网络侧而非代码侧（统一改址或加地址映射层），**越晚发现越贵** —— 因此 `subnet` 字段应在 **M1** 就进 ANNOUNCE
- 不采纳的替代：**只读引导节点**（B，最容易被做成"事实中心"，且 3–5 网段手工配种子根本不需要它）；**中继转发**（C，与"无中心"直接冲突，且 P-1 满足时完全不需要）；**种子广播转发跨网段目录**（12.8 KB 超 MTU，UDP 广播分片丢一片即全丢，不可控）

---

## 7. 工程化基线

### 7.1 目录与红线检查（CI）

```makefile
# 建议加入 CI，让架构约束可执行而非靠自觉
lint-arch:
	# R1: domain 层禁止 I/O 依赖
	! grep -rn '"net"\|"database/sql"\|"os"\|wails' internal/domain/ --include=*.go \
	    | grep -v '_test.go' | grep -v 'domain/ports'
	# R2: 适配器之间禁止互引
	! grep -rn 'internal/adapters/store' internal/adapters/net/ --include=*.go
	# R3: 业务代码禁止直接 new 适配器
	! grep -rn 'sqlite.New\|tcp.NewManager' internal/app/ internal/domain/ --include=*.go
```

### 7.2 测试策略

| 层级         | 范围                          | 手段                                                 | 目标                                                                |
| ------------ | ----------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------- |
| 单元         | `domain/**`                   | 纯函数测试 + 内存 ports + `FakeClock`                | 覆盖率 ≥ 80%                                                        |
| 组件         | `adapters/net/protocol`       | 表驱动编解码测试 + 畸形帧 fuzz                       | 无 panic                                                            |
| 组件         | `adapters/store/sqlite`       | 临时库 + 迁移往返测试                                | 迁移可重入                                                          |
| 集成         | 2 节点握手/聊天/传输          | `net.Pipe()` + 内存适配器                            | 主链路通过                                                          |
| E2E          | 3~5 虚拟节点                  | 单进程内跑发现 + 群聊 + 传输                         | 主场景通过                                                          |
| 连接模型     | 4~20 虚拟节点                 | 内存 `ConnManager` + `FakeClock`                     | 按需拨号、空闲回收（5 min）、`max_dial_concurrency` 生效            |
| **跨网段**   | **2 个隔离发现域 + 2 颗种子** | 单进程内构造两个互不广播的 UDP 端口域 + 共享种子清单 | **目录经种子双向收敛；随机拉 2 颗时单颗种子失效仍能取到跨网段目录** |
| **规模验证** | **20~200 虚拟节点**           | 单进程批量起节点（内存适配器）                       | **目录同步正确；常驻连接数 ≤ `max_active_conns`；无保活流量**       |
| 混沌         | 丢包/乱序/断连注入            | `test/chaos`，随机 kill 连接                         | 位图恢复正确、无数据损坏                                            |

**「规模验证」这一行是 200 节点约束的直接产物**：既然全量目录只有 12.8 KB、连接数有硬上限，就可以在单进程里真的起 200 个虚拟节点跑一遍发现与目录同步——**在上线前就证明"200 节点不会打爆"**，而不是等到现场。

**「跨网段」这一行是 3–5 网段约束的直接产物**，也是本节唯一一个**必须在 M1 就通过的用例**：把两个"广播互不可达"的域用端口隔离模拟出来，比等真实多网段环境便宜得多。它同时验证三件事——目录能双向收敛、单跳交换不产生回灌、`subnet` 字段（P-2 检测）能被正确填充。**这个用例跑不通，就说明 4.5.1 的实现走了形**，此时改代码还来得及。

**关键：`FakeClock` 是必需品，不是奢侈品。** 心跳超时（30 s）、TTL 过期（300 s）、重传退避（1s→5min）如果用真实时间测试，一个用例要跑几分钟。有了 `Clock` 接口，这些全部变成微秒级。

### 7.3 可观测性

- **日志**：`log/slog` + JSON handler。每条日志注入 `node_id`；连接相关注入 `conn_id`；传输相关注入 `job_id`。**一条日志能还原一次传输的完整因果链**（这是 P2P 排障的生命线）
- **调试面板**（前端）：实时展示事件总线事件流、在线节点、连接状态、位图进度。M1 就做，投入产出比极高
- **计数器**：`peers_online`、`frames_sent/recv`、`bytes_sent/recv`、`transfer_retries`、`discovery_dups_dropped`、`auth_failures`。暴露在设置页的"诊断"标签
- **不做**：Prometheus/OpenTelemetry。单机桌面应用（无服务端聚合）上这些是负担

### 7.4 配置管理

- 加载时构造**不可变快照**，通过 `config.changed` 事件通知各 App 更新
- 配置变更需要重启的部分（端口、网卡）在 UI 明确标注"需重启生效"
- TOML 结构在现设计基础上补充：

```toml
[network]
tcp_port = 0            # 0 = 自动探测（推荐）
udp_port = 0
port_fallback_range = 10
interface_mode = "auto" # auto | manual | seed_only
allow_interfaces = []   # interface_mode = manual 时生效
deny_interfaces = ["utun*", "vmnet*", "vboxnet*", "docker*"]
enable_multicast = false # 默认关闭（企业网多不支持，且易触发告警）

[discovery]
announce_interval = "5s"
peer_ttl = "300s"
max_peers = 512            # 全局约 200 节点，留 2.5× 余量
max_peer_list_size = 256   # 单次响应即可覆盖全量 200 条目（ADR-010）

[discovery.seeds]          # ADR-011 / 4.5.1：3–5 网段，手动维护，随安装包分发默认清单
refresh_interval = "300s"  # 跨网段目录刷新周期（同时也是跨网段可见性的收敛上界）
seeds_per_refresh = 2      # 每轮随机拉 2 颗可达种子并合并；不要调大，见 4.5.1
probe_timeout = "2s"       # 单颗种子的 TCP 探测超时
# 格式为 IP:UDP_PORT。**被选为种子的机器必须固定 udp_port（不允许自动回退）**，
# 否则跨网段节点无法在第一个报文上找到它；种子实际的 tcp_port 由种子回传的
# ANNOUNCE 学到，无需在此配置（ADR-008 的端口回退因此不受影响）。
# 每网段 ≥2 颗，共 6–10 条；条目不带任何角色语义，只是"值得先问一声的地址"。
list = [
  "192.168.1.10:2425",     # 网段 A 主
  "192.168.1.11:2425",     # 网段 A 备
  "192.168.2.10:2425",     # 网段 B 主
  "192.168.2.11:2425",     # 网段 B 备
  "10.20.3.10:2425",       # 网段 C 主
  "10.20.3.11:2425",       # 网段 C 备
]

[connection]               # ADR-009：在线状态与传输连接解耦
idle_conn_timeout = "5m"   # 空闲连接回收
max_dial_concurrency = 8   # 群聊扇出时的并发拨号上限，避免 SYN 密集
max_active_conns = 32      # 单节点连接硬上限（常态目标 ≤ 10）
heartbeat_only_when_active = true

[transfer]
max_concurrent = 3
window_size = 8
chunk_size = 524288
max_upload_mbps = 0     # 0 = 不限
bitmap_flush_interval = "2s"

[security]
require_auth = true     # 强烈建议保持 true
encryption = "off"      # off | prefer | require（v1.1 启用）
```

### 7.5 依赖策略

- 锁定 Wails `v3.0.0-beta.24`（beta 期 API 会变；升级作为独立任务，附带回归测试）
- 新增依赖白名单化：每加一个依赖需要说明"标准库为何不够"
- 预期新增：`google/uuid`（UUIDv7）、`BurntSushi/toml`、`modernc.org/sqlite`、`log/slog`（标准库）、`flynn/noise`（v1.1）、`klauspost/compress`（zstd，可选）
- **不要引入**：`gorilla/websocket`（不用 WebSocket）、任何消息队列客户端、任何 ORM

---

## 8. 演进路线图

### 8.1 M1 — 核心链路原型（3 周，CLI）

**目标**：`identity → discovery → transport → 发 hello → store → 读回` 全链路跑通。

**关键调整**：现设计说"建议先写命令行原型"。**我强烈支持，但要纠正一点：原型不是丢弃品，它就是 v1.0 的骨架。** 因此 M1 必须按 ADR-001 的四层结构写，而不是写一个 `main.go` 里堆逻辑的脚本。否则 M1 结束时会面临"推翻重写"。

M1 交付：

- `domain/identity`、`domain/peer`、`domain/ports`（框架全定下来）
- `adapters/net/protocol`（帧编解码 v2）
- `adapters/net/tcp`（含三段握手认证 + **按需拨号 / 会话级保活 / 空闲回收**，见 3.7）
- `adapters/net/udp`（广播发现 + 接口策略 + **全量目录维护**）
- **种子单播通道（ADR-011 / 4.5.1）**：UDP 探测 → 学回 ANNOUNCE → TCP `PEER_LIST_REQ/RESP`；`ANNOUNCE` 携带 `subnet` 字段（供 P-2 检测）
- `adapters/store/mem`（先用内存，验证领域逻辑）
- `infra/eventbus`、`infra/clock`、`infra/log`
- `main_wails.go`（GUI 外壳）：两台机器各起一个实例，能互相发现、握手、发消息
- `test/e2e`：3 节点发现 + 握手 + 消息往返
- `test/e2e`（**跨网段用例**）：**两个互不可达的 UDP 隔离域 + 一颗种子，验证目录经种子双向收敛**（单进程内用两个端口域即可模拟，不需要第二张真实网卡）
- `test/scale`：**20 节点无真实网络跑通发现 + 目录同步**（证明 3.7 的连接模型成立）

**M1 完成标准**：`go test ./test/e2e/... ./test/scale/...` 全绿，且**没有任何真实网络以外的手工步骤**；`test/scale` 在 20 节点下的常驻连接数符合预期（≤ 10）；**跨网段用例必须通过 —— 它就是"跨网段能力进 v1.0"这个决定的验收物。**

> **为什么把跨网段塞进 M1**：不是为了功能，而是为了**形状**。若 M1 的目录交换报文被写死成"广播响应"（例如隐含假设"请求方一定在同一广播域"），v1.1 想加种子时就得改协议与报文格式，而那时已经背上真实数据与版本兼容的问题。现在做，成本只有 **M1 +2 天**（一个探测定时器 + 一条单播路径），还顺带把 `subnet` 字段（P-2）与"种子身份从第一跳即可验签"一并落地。

### 8.2 M2 — 持久化与文件传输（3 周）

- `adapters/store/sqlite`（含 migrations + 单写 goroutine）
- `domain/transfer`（Job 聚合、位图、状态机、窗口）+ 完整单测
- `adapters/net/` 文件传输消息类型
- `adapters` 的 `FileSink`（含 G3 路径清洗 + 原子替换）
- 完成标准：两个 GUI 节点互传 10 GB 文件，中途 `kill -9` 发送方，重启后位图恢复、续传成功、哈希一致

### 8.3 M3 — UI 闭环（3 周，可并行于 M2）

- `adapters/wails`（5 个服务 + 事件桥）
- 前端：节点列表 / 聊天窗口 / 传输管理 / 设置 / **调试面板**
- 事件订阅集中化 + 节流（P1-10）
- 完成标准：GUI 双机互传 + 聊天，无卡顿

### 8.4 M4 — 可靠性硬化与 v1.0 GA（2 周）

- 消息可靠性完整落地（ADR-004）+ outbox 扫描器
- 群聊（ADR-003，20 人上限）
- 端口自动回退（ADR-008）
- 混沌测试接入 CI
- 结构化日志 + 诊断面板完善
- 用户文档（防火墙例外、**种子 IP 清单配置**、**P-1/P-2 部署前置条件自检步骤**、VPN 场景说明）

**v1.0 发布内容**：单聊 + 群聊（≤20 人）+ 单文件续传 + **跨网段发现（网段内广播 + 种子拉取；M1 起即具备）** + 身份认证 + 聊天记录。

### 8.5 v1.1 — 加密与扩容（4 周）

- Noise 通道（ADR-002 方案 B）：`adapters/net/noise`，Flags.ENCRYPTED 生效
- 密钥轮换 + 节点指纹校验 UI（首次连接让用户确认指纹，防 MITM）
- Gossip 节点交换成熟化（退避、上限、epoch）
- UDP 组播作为可选加速
- 群聊支持 50 人（扇出部分改并行 + 连接复用）

> 跨网段已**提前到 v1.0**（见 4.5.1 与 8.1），此处不再作为独立主题。v1.1 只加"加密"与"扩容"两件事，范围比上一版更干净。

### 8.6 v1.2 — 吞吐与批量（3 周）

- 目录/多文件传输（batch 概念：一个 batch 含多个 job）
- 多对端并发传输 + 全局带宽预算（放开 3.4 的"单对端 1 任务"限制）
- zstd 块级压缩（Flags.COMPRESSED）
- 全局断点恢复（重启后自动恢复所有 `active` 任务）
- 传输历史与统计

### 8.7 v2.0 — 扩展与多网络（6–8 周，按需）

此处才考虑架构级扩展。**前提是单机架构已到瓶颈**（如需要语音/屏幕共享、多设备同一身份）。

- **插件/能力扩展**：`sdk/` 包定义扩展点（注册消息类型 + UI 面板），支撑剪贴板同步、语音、屏幕共享（参考 BLIP）
- ~~**软中继**（方案 C）~~：**已从路线图移除**。原触发条件是"群聊 > 200 人"，但全局节点数已确认为 ≤ 200，该场景不存在；200 人群体量用 Gossip（方案 B）足以覆盖。**这是规模约束确认带来的直接简化——避免了一次对"无中心"原则的妥协**
- **多设备同一身份**：一个用户多台机器共享 NodeID，需要子设备 ID + 密钥派生
- **跨网络**：QUIC 传输（多路复用 + 更好移动性）；若要做公网，需引入 rendezvous 服务（破坏"无中心"，应作为可选模块）
- **明确不做**：中心化消息服务器。若未来必须（如企业版需求），应作为独立产品而非本项目演化

**同时永久移除的未来风险**（都是"≤ 200 节点 + 3–5 网段"这两条约束带来的）：

| 原列入的风险                    | 移除原因                                                                                                |
| ------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 分层/分区发现协议               | 200 节点用广播 + 全量目录交换即可覆盖                                                                   |
| 软中继 / 弱中心                 | 群上限 = 全局节点数 = 200，Gossip 即闭环                                                                |
| 目录增量同步 / 分片拉取         | 全量目录仅 12.8 KB，全量传播成本可忽略（ADR-010）                                                       |
| NodeID 长度不足                 | 200 节点下 8 字节碰撞概率 1×10⁻¹⁵                                                                       |
| **种子分发机制 / 只读引导节点** | 3–5 网段手工配种子完全可控（6–10 条 IP）→ **不需要引导节点，也就无需在"无中心"上做任何妥协**（ADR-011） |
| **跨网段中继 / 转发节点**       | 只要 P-1（网段间三层可达）成立，跨网段直接按需拨号即可，**零跳数**（4.5.1）                             |

### 8.8 路线图总览

| 版本 | 时间           | 主题            | 关键架构动作                                                                |
| ---- | -------------- | --------------- | --------------------------------------------------------------------------- |
| M1   | 3 周 **+2 天** | 核心链路        | 确立四层结构 + 端口 + 事件总线 + 按需连接模型 + **种子单播通道（ADR-011）** |
| M2   | 3 周           | 持久化与传输    | FileSink 抽象 + 状态机                                                      |
| M3   | 3 周           | UI 闭环         | Wails 适配器 + 事件桥                                                       |
| M4   | 2 周           | 硬化 → **v1.0** | 认证 + 可靠性 + 群聊 + 端口回退 + **部署前置条件（P-1/P-2）自检**           |
| v1.1 | 4 周           | 加密与扩容      | Noise 适配器（接口已预留）+ 群聊 50 人                                      |
| v1.2 | 3 周           | 吞吐与批量      | 多任务调度器                                                                |
| v2.0 | 6–8 周         | 扩展与多网络    | sdk/ 插件接口（**无中继**）                                                 |

**总计到 v1.0 约 11.5 周**（M1+M2+M3 部分并行+M4；跨网段进 M1 的增量约 2 天），与现设计的 11–15 周估算基本吻合，但**交付物质量不同**：现设计的 11 周产出的是"功能能跑的 v1.0"，本路线产出的是"功能能跑、且 v1.1/v1.2 不需要重构的 v1.0"。多出的成本集中在 M1，而 M1 的回报贯穿整个后续生命周期 —— **跨网段从 v1.1 提前到 v1.0，就是这笔回报的第一笔兑现。**

### 8.9 一句话路线图

> **v1.0 只做"能跑且留好接缝"，把加密 / 并发 / 批量都放到有真实反馈之后。** 现在多花 2 周立架构 + 2 天定跨网段报文形状，比 v1.1 时推翻数据层或改协议便宜一个数量级。

---

## 9. 待决策项

**两个前提（9.4 规模 / 9.5 拓扑）均已确认**，以下是**剩余的三处分叉** —— 没有客观最优解，取决于你们的取舍。请按 A/B/C 给个结论，我据此收敛为可执行的设计终稿。

### 9.1 加密与认证的推进节奏

| 选项  | 内容                                                                                      | 优势                                               | 代价                                                               | 我的倾向 |
| ----- | ----------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------ | -------- |
| **A** | v1.0 只做 Ed25519 挑战-应答认证，暂不加密；协议留 `Flags.ENCRYPTED` + `KEY_EXCHANGE` 接缝 | 交付快；调试简单；认证已解决"冒充"这个最实际的威胁 | 内网同网段可嗅探（但攻击者本来就在内网）                           |          |
| **B** | v1.0 直接集成 `flynn/noise`（Noise_XX），一步拿到认证 + 前向保密 + 身份隐藏               | 一次到位；Noise 是成熟标准；免手写密码学           | 推迟 v1.0（+2~3 周）；握手失败难排查；需额外的 X25519 静态密钥管理 |          |
| **C** | 先 A 后 B：v1.0 = A，v1.1 = B，中间用 capability `ENCRYPTED` 做灰度                       | 兼顾交付与安全演进；可先让用户跑起来再升级         | 需要维护两套握手路径一段时间                                       | ★        |

**影响**：选 A/C 则 4.2 节的手写握手是 v1.0 交付内容；选 B 则 M1 工期 +2 周，且 4.2 节改为 Noise 集成说明。

### 9.2 群聊的 v1.0 交付范围

| 选项  | 内容                                                                             | 优势                           | 代价                                     | 我的倾向 |
| ----- | -------------------------------------------------------------------------------- | ------------------------------ | ---------------------------------------- | -------- |
| **A** | v1.0 就上群聊，全互联扇出，硬上限 20 人，不做离线暂存                            | 兑现 P0 承诺；数据模型一次定对 | v1.0 工期 +2 周；20 人上限需在 UI 明说   | ★        |
| **B** | v1.0 只做单聊，群聊推到 v1.1；但数据模型（`conv_id`/`msg_id`）现在就按群聊形状做 | v1.0 更快、更稳；数据层不返工  | v1.0 功能表要下调"支持群聊"的优先级      |          |
| **C** | v1.0 做"广播式伪群"（无成员管理，仅向所有在线节点群发）                          | 实现最省                       | 不是真群聊；用户预期会落空；反而更伤体验 |          |

**影响**：选 A 则第 4.6 节与 ADR-003 全部进 v1.0；选 B 则 4.6 节标记为"v1.1 设计已就绪"，但 5 章数据模型仍按群聊形状落地（**这一条无论选什么都建议现在做**）。

**规模确认后的变化**：既然全局只有 200 个节点，群聊的能力边界已经完全封闭（20 → 50 → 200，到顶），**不存在"以后可能要做万人群"的不确定性**。这让选 A 的风险显著低于上一版评估——你现在定下的方案 A + Gossip 接缝，就是终态架构，不需要留额外余地。代价只是 v1.0 工期 +2 周。

### 9.3 传输协议的并发模型

| 选项  | 内容                                                          | 优势                                                                            | 代价                                               | 我的倾向 |
| ----- | ------------------------------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------- | -------- |
| **A** | v1.0 先保留 stop-and-wait（每块等 ACK），只把位图批量落盘修掉 | 实现最简；LAN 下吞吐其实够（512KB / 0.2ms ≈ 2.5 GB/s 理论上限，实际受磁盘限制） | 跨网段（RTT > 10 ms）吞吐断崖；v1.2 要重写发送逻辑 |          |
| **B** | v1.0 直接上滑动窗口 + 周期性全量位图（ADR-005）               | 一次做对；跨网段可用；ACK 语义反而更简单                                        | M2 工期 +1 周；窗口管理代码需测试                  | ★        |
| **C** | v1.0 用固定窗口 W=4 的简化版（不做 RTT 自适应）               | 折中：拿到绝大部分收益，省掉自适应的复杂度                                      | 极端网络下不够优；v1.2 补自适应                    |          |

**影响**：选 A 则 4.8 节降级为"位图批量落盘改造"，其余进 v1.2；选 B/C 则 4.8 节与 ADR-005 进 v1.0。

### 9.4 规模前提（**已确认：全局节点数 ≤ 200**）

确认后触发的架构结论（已全部回写进正文）：

| 原假设                                 | 确认后                   | 影响章节                           |
| -------------------------------------- | ------------------------ | ---------------------------------- |
| 全局 ≤ 500 节点                        | **≤ 200 节点**           | 0.5                                |
| 可能需要分层/分区发现                  | **不需要**，永久移除     | 3.7、8.7                           |
| 群聊可能超 200 人，需软中继            | **不需要**，永久移除     | 1.1、4.6、8.7                      |
| 节点间是否全互联持久连接（原文未讨论） | **不建立**，改为按需拨号 | **3.7（新增）、ADR-009（新增）**   |
| 目录是否需增量同步                     | **不需要**，全量快照     | ADR-010（新增）                    |
| 网段数（原文未讨论）                   | **3–5 个**，种子手动维护 | **4.5.1（新增）、ADR-011（新增）** |

### 9.5 拓扑前提（**已确认：3–5 个网段、种子手动维护、暂不考虑运维负担**）

确认后触发的架构结论（已全部回写进正文）：

| 确认项               | 架构结论                                                               | 影响章节                           |
| -------------------- | ---------------------------------------------------------------------- | ---------------------------------- |
| **3–5 个网段**       | 网段内广播 + **网段间种子单播拉取**；每网段 ≥2 颗种子，共 6–10 条 IP   | **4.5.1（新增）、ADR-011（新增）** |
| **种子手动维护**     | **不需要**种子分发机制 / 只读引导节点 → **无需在"无中心"上做任何妥协** | 0.5、8.7                           |
| **暂不考虑运维负担** | 不为此开发自动化工具；但 **P-1 / P-2 两条前置条件仍必须写进安装文档**  | 0.5、8.4                           |
| 跨网段能力的落点     | **提前到 v1.0（M1）** —— 它是报文的形状约束，不是可选功能              | 4.5.1、8.1、8.5                    |

**为什么"暂不考虑运维负担"这句话要打个折**：它免掉的是**工具**（自动分发、集中管控面板），免不掉的是**前置条件**。3–5 网段真正容易踩的坑不是"手工配 6 个 IP 麻烦"，而是下面两条里任意一条不成立：

- **P-1 网段间三层不可达** → 跨网段直接失效，只能上中继（架构级返工）
- **P-2 各网段地址段重叠**（尤其"多厂区都是 `192.168.1.0/24` 再走 VPN 互联"）→ 寻址崩溃，且现象隐蔽、极难排查

这两条都是**网络侧的既定事实，代码救不了**。建议 **M1 就把 `subnet` 字段放进 ANNOUNCE**，让 P-2 的检测在第一个原型里跑通 —— 这样万一不满足，你是在**第 3 周**发现，而不是在部署当天。

---

## 附录 A：与现设计书的差异清单

供快速对照，**加粗为破坏性变更（越早做越便宜）**。

| 章节               | 现设计                                                                | 本设计                                                                                                               | 类型                    |
| ------------------ | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 3.2 模块划分       | service/discovery/transport/transfer/store/config 平铺                | 四层：入站适配器/应用/领域/出站适配器 + infra                                                                        | 结构调整                |
| 3.2 模块划分       | —                                                                     | **新增 `domain/ports` 集中定义接口**                                                                                 | 新增                    |
| 3.2 模块划分       | transport 直接调 store                                                | 事件总线 + 端口接口，依赖单向                                                                                        | 修正                    |
| 3.3 数据流         | ChatService → store → transport 硬编码调用链                          | ChatApp 编排 + 事件通知 UI                                                                                           | 修正                    |
| **3.7 连接拓扑**   | 未提及（隐含"全互联持久连接"）                                        | **在线状态走 UDP announce（全量目录 12.8 KB）；TCP 按需拨号 + 5 min 空闲回收；单节点常驻连接 ≤ 10**                  | **新增决策（ADR-009）** |
| **4.6 群聊**       | 功能表标 P0，但零设计                                                 | **全互联单播扇出；v1.0 上限 20 人，v1.1 提至 50 人；> 50 切 Gossip；绝对天花板 = 200（全局节点数），软中继永久移除** | **新增（ADR-003）**     |
| 4.2 发现协议       | 明文 ANNOUNCE，无 TTL/epoch，无接口策略                               | 签名 ANNOUNCE + TTL 300s + list_epoch + 条目上限 + 三档网卡策略 + 端口自动回退                                       | 增强                    |
| 4.2 目录同步       | 节点交换（无范围界定）                                                | 全量快照同步，单响应 ≤256 条目即可覆盖全局（ADR-010）                                                                | 简化                    |
| **4.2 跨网段发现** | **完全缺失**（原文未讨论多网段场景）                                  | **网段内广播 + 网段间种子单播拉取；每网段 ≥2 颗种子；种子零特权；`subnet` 字段进 ANNOUNCE**                          | **新增决策（ADR-011）** |
| **4.2 种子语义**   | 仅有 `seed_nodes` 表，无机制定义                                      | **种子 = 配置里的一串 IP，与普通节点跑同一套目录协议；固定 UDP 端口，实际 TCP 端口由回传 ANNOUNCE 学到**             | **新增决策（ADR-011）** |
| 5 表结构           | `seed_nodes(node_id PK)`                                              | `seed_nodes(addr PK, node_id 可空, last_epoch)`；`peers.last_seen` 明确为**本地到达时间**语义                        | 修正                    |
| 4.3 帧格式         | Magic2+Ver1+Type1+Len4（8B）                                          | Magic2+Ver1+**Flags1**+Type1+Len4（9B）+ 16MB 上限                                                                   | **破坏性**              |
| 4.3 消息类型       | 0x01–0x21                                                             | 保留 + 新增 AUTH/AUTH*OK/ERROR/CHAT_ACK/GROUP*\*/KEY_EXCHANGE                                                        | 扩展                    |
| 4.3 握手           | HELLO 交换 NodeID + 显示名                                            | 三段式 HELLO/HELLO_ACK/AUTH，Ed25519 转录签名                                                                        | **新增**                |
| 4.3 协商           | 无                                                                    | proto_min/max + 8 位 caps 能力协商                                                                                   | 新增                    |
| 4.4 分块           | 固定 512KB，stop-and-wait，每块等 ACK                                 | 窗口 W 自适应，周期性全量位图 ACK，块大小可协商                                                                      | 修正                    |
| 4.4 位图落盘       | 每块更新                                                              | 内存维护 + 每 2s/每 256 块批量落盘                                                                                   | 修正                    |
| 4.4 临时文件       | `<filename>.tmp`                                                      | `.swarmlink.<job_id>.part`                                                                                           | 修正                    |
| 4.6 接收路径       | `filepath.Join(dir, file_name)` 直接使用                              | **FileSink 统一清洗（base/拒绝 ../绝对路径/保留名/长度/前缀校验）**                                                  | **安全修复**            |
| 4.7 Wails 集成     | 服务直接持有 store/transport                                          | 服务仅做适配与参数校验，业务在 app 层；加事件桥                                                                      | 修正                    |
| 5 目录结构         | internal/{identity,discovery,transport,transfer,store,config,service} | internal/{domain,app,adapters,infra} + test/{e2e,chaos}                                                    | **结构调整**            |
| 4.5 表结构         | conversations(peer_id PK)                                             | **conversations(conv_id PK, kind)**                                                                                  | **破坏性**              |
| 4.5 表结构         | messages 无唯一键                                                     | **messages.UNIQUE(msg_id)** + state 字段                                                                             | **破坏性**              |
| 4.5 表结构         | —                                                                     | 新增 outbox / peers / groups / group_members / schema_migrations                                                     | 新增                    |
| 4.5 表结构         | transfer_jobs.status 仅 active                                        | 加 queued/verifying/cancelled + chunk_size/total_chunks/window_size                                                  | 扩展                    |
| 6 风险表           | 8 条风险                                                              | 保留 + 新增：连接撕裂、目录穿越、进度事件洪泛、端口冲突、多网卡、SQLite 写竞争                                       | 增强                    |
| 7 实施计划         | 7 阶段 11–15 周                                                       | M1–M4 约 11.5 周到 v1.0，且每阶段含测试与架构约束验收                                                                | 重构                    |

## 附录 B：现设计中**保持不变**的部分（已评审通过）

避免过度推翻，以下内容判断正确、直接沿用：

1. **UDP 广播用 `net.InterfaceAddrs()` 构造子网广播地址，不用 `255.255.255.255`；绑定 `0.0.0.0` 而非 `127.0.0.1`** —— 完全正确，这是最容易踩的坑
2. **组播地址选 `224.0.1.0/24` 或 `239.0.0.0/8`，避开 `224.0.0.0/24`** —— 正确（Linux 内核特殊处理）
3. **`modernc.org/sqlite` 而非 `mattn/go-sqlite3`** —— 正确。无 CGO 带来的交叉编译便利性，对 Wails 打包是决定性的；并发场景更优也符合本项目负载
4. **WAL + `_busy_timeout=5000` + `_synchronous=NORMAL`** —— 正确（仅建议补 `_foreign_keys=on`）
5. **位图而非 JSON 数组存完成块** —— 正确，且 1 GB = 256 B 的测算准确
6. **`os.OpenFile` 一次打开 + `file.ReadAt(buf, offset)` 随机读** —— 正确，避免 `Seek` 的竞态
7. **`io.LimitReader` 处理尾块** —— 正确
8. **私钥 PKCS#8 PEM + `PRIVATE KEY` 块类型 + 0600/0700 权限** —— 正确（注意 Windows 不遵守 POSIX 权限位，需 ACL 或接受该平台限制）
9. **`os.Rename` 原子替换临时文件** —— 正确
10. **块三元组 `(file_id, offset, size)` 做唯一索引而非 `(file_id, chunk_index)`** —— 这个洞察很准，说明设计者理解网络重传语义
11. **先写命令行原型验证核心链路** —— 正确（仅需纠正"原型即骨架，不要写抛弃型代码"）
12. **`seed_nodes` 表与"种子"这个概念** —— 方向正确，本设计**保留并强化**了它，只补了缺失的机制（单播拉取 / 零特权 / 固定 UDP 端口 / 单跳防环），没有替换这个概念

---

## 附录 C：一句话总结

> 现方案把**技术细节做对了**（广播的坑、SQLite 选型、位图续传、原子替换都对），把**架构做缺了**（依赖方向、跨模块契约、P0 功能的设计预留、以及 5 个必修缺口）。补架构的成本集中在 M1，回报是整个后续生命周期不用重构数据层。
>
> 而 **"全局 ≤ 200 节点"与"3–5 个网段"这两条边界，是整份文档里最值钱的东西** —— 前者一次性划掉了分层发现、软中继、增量目录同步三块复杂度，并让"按需连接"明显优于全互联；后者让跨网段落成 **每网段 2 颗种子、共 6–10 条 IP 的手工配置**，从而**彻底不需要引导节点，也就不需要在"无中心"上做任何妥协**。
>
> **架构的价值一半来自约束。** 你给出的这两个数字，比新增任何设计都更有价值。

---

**文档结束** · 两个前提（规模 / 拓扑）已齐。下一步：请就第 9 章剩余的 **3 个分叉**（9.1 加密节奏 / 9.2 群聊范围 / 9.3 传输并发）给出结论，我据此收敛为可执行的设计终稿。
