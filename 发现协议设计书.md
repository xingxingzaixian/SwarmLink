# SwarmLink 发现协议设计书

**状态**：与代码同步（v3.0.0-beta.24 工具链 / 仓库当前 HEAD）
**代码基线**：本文所有结论都对着实现逐条核对过，引用格式为 `文件:行号`
**与既有文档的关系**：`架构设计书.md` §4.5 / §4.5.1 是**设计意图**，本文描述**实现事实**。两者的差异集中列在 §11，其中架构书里的若干参数（5 s 广播、±20% 抖动、LRU 淘汰上限、`SO_BINDTODEVICE`）在实现中已被修订或未落地。

---

## 目录

1. [目标与约束](#1-目标与约束)
2. [总体结构：三种来源汇入一条路径](#2-总体结构三种来源汇入一条路径)
3. [报文格式](#3-报文格式)
4. [签名与身份](#4-签名与身份)
5. [时序与参数总表](#5-时序与参数总表)
6. [状态机](#6-状态机)
7. [多网卡与地址选择](#7-多网卡与地址选择)
8. [端口策略](#8-端口策略)
9. [种子机制（跨网段）](#9-种子机制跨网段)
10. [目录交换与收敛](#10-目录交换与收敛)
11. [与架构书的差异](#11-与架构书的差异)
12. [安全模型与威胁分析](#12-安全模型与威胁分析)
13. [代码索引](#13-代码索引)
14. [测试与验收](#14-测试与验收)
15. [已知缺口与演进建议](#15-已知缺口与演进建议)

---

## 1. 目标与约束

| 维度 | 确认值 | 对发现协议的约束 |
|---|---|---|
| 全局节点数 | ≤ 200 | 全量目录同步可行（约 12.8 KB / 次），不需要分层、分页、增量发现 |
| 网段数 | 3–5 | 网段内广播 + 网段间种子单播，**不设引导节点、不设中继** |
| 种子维护 | 手工配置（6–10 条 IP） | 不做种子分发机制；种子地址是静态输入 |
| 连接模型 | 不维护全互联持久连接 | 发现只负责「看得见」，连接由需要时拨号（ADR-009） |
| 中心化程度 | 无中心 | 种子**零特权**：不与普通节点跑不同的协议 |

两条**代码救不了**的部署前置条件（`docs/adr/ADR-011-cross-subnet-seeds.md`、`架构设计书.md` §0.5）：

- **P-1**：任意两网段的种子 IP 之间可 TCP 直连（三层可达，非 NAT 隔离）。不满足 → 跨网段发现与消息全失效。
- **P-2**：各网段地址段不得重叠。不满足 → 目录里出现「node_id 不同、IP 相同」的条目，跨网段寻址崩溃且现象隐蔽。

P-1/P-2 在代码里有自检实现：`internal/app/selfcheck.go:31-35`（输入）、`:87-102`（P-2 重叠判定，只报警告不硬失败）。`subnet` 字段参与签名正是为了让 P-2 检测不可被伪造（`internal/domain/peer/peer.go:112-114`）。

---

## 2. 总体结构：三种来源汇入一条路径

发现层最核心的设计不是广播，而是**归一化**：无论通告来自网段内广播还是跨网段种子，产出的都是同一个 `peer.Announcement`，走同一条 `Upsert` 路径进入目录。因此「跨网段」没有引入第二条代码路径。

```
                         ┌──────────────────────────────────────┐
   网段内广播             │  process.Announcement(来自任意策略)     │
   ──────────────►       │        PeerApp.OnAnnouncement         │
   Broadcaster           │   · 自发现过滤  · BYE → MarkOffline    │
   (udp.UDPTypeAnnounce) │   · State 保持策略  · LastSeen=本地now │
                         │              ↓                        │
   跨网段种子两跳         │        PeerDirectory.Upsert            │
   ──────────────►       │   (mem.Peers / sqlite.Peers)          │
   Prober → Announcement │              ↓                        │
   (Source=seed)         │     Publish(TopicPeerDiscovered)      │
                         └──────────────────────────────────────┘
```

对应实现：

| 环节 | 位置 |
|---|---|
| 广播策略接口 | `internal/domain/ports/ports.go:197-201` |
| 广播实现 | `internal/adapters/net/udp/broadcast.go:107-144`（Start）、`:200-239`（recvLoop） |
| 种子探测接口 / 实现 | `ports.go:223-225` / `internal/adapters/net/udp/seed.go:33-88` |
| 统一汇合点 | `internal/app/peer_app.go:59-135` |
| 目录端口 / 实现 | `ports.go:64-72` / `internal/adapters/store/{mem/mem.go,sqlite/peers.go}` |

`Source` 字段标记条目来源（`peer.SourceBroadcast` / `peer.SourceSeed` / `peer.SourceManual` / `peer.SourceMulticast` / `peer.SourceGossip`，见 `peer.go:49-55`），仅用于诊断与自检过滤（`selfcheck.go:76-81`），**不参与任何决策**——这正是「不需要 `via` / `hop_count`」的体现。

> 预留未实现：`SourceMulticast` 与 `SourceGossip` 常量存在，但**没有任何组播/Gossip 实现**；`Config.EnableMulticast`（`broadcast.go:28-29`）与 `Config.MulticastAddr` 在 bootstrap 中被赋值（`internal/bootstrap/bootstrap.go:214`）却从未被读取。

---

## 3. 报文格式

### 3.1 UDP 封套

```
 0        4      5      6                      N
+--------+------+------+----------------------+
| Magic  | Ver  | Type |   JSON body          |
| "SLAN" | 0x01 |      |   (utf-8)            |
| 4 字节  | 1字节 | 1字节 |                      |
+--------+------+------+----------------------+
```

- 编码 / 解码：`internal/domain/protocol/udp.go:22-32` / `:37-45`；前缀长度常量 `UDPPrefixLen = 6`（`udp.go:13`）
- 魔数 `UDPMagic = 0x53 0x4C 0x41 0x4E`（"SLAN"，`const.go:92`）、`UDPVersion = 0x01`（`const.go:95`）
- **为什么要有强势魔数**：UDP 端口可能落到任何服务上，报文必须能被「不认识它的服务」以最快速度丢弃（`udp.go:20-21`、`const.go:87-88`）
- 校验失败（长度 < 6 或魔数不符）返回 `ErrBadUDPPacket`，接收方**静默丢弃**（`broadcast.go:214-217`）
- **UDP 与 TCP 是两套独立的魔数与编号空间**，调整 UDP 魔数不得牵动 TCP 版本兼容（`const.go:89`）

### 3.2 UDP 消息类型

| 值 | 名称 | 载荷 | 当前状态 |
|---|---|---|---|
| `0x01` | `UDPTypeAnnounce` | `AnnounceWire` | 在用（周期广播 + 种子单播回包） |
| `0x02` | `UDPTypePeerListReq` | — | **声明但全仓无使用**（目录交换实际走 TCP） |
| `0x03` | `UDPTypePeerListResp` | — | 同上 |
| `0x04` | `UDPTypeSeedProbe` | `SeedProbeWire`（实际发空体 `{}`） | 在用（第一跳） |
| `0x05` | `UDPTypeBye` | `AnnounceWire`（同构，同样签名） | 在用（主动下线） |

定义于 `internal/domain/protocol/const.go:98-109`。

BYE 单独立类型而不在 ANNOUNCE 里加标志位的原因写得很清楚（`const.go:103-107`）：旧版本客户端遇到未知类型会直接丢弃，**不会把一条告别误当成在线刷新**。同理 `Leaving` 不占线格式字段、不参与签名（`peer.go:98-104`）。

### 3.3 ANNOUNCE / BYE 载荷（JSON）

`AnnounceWire`（`udp.go:52-63`）字段与编码：

| 字段 | JSON | 编码 | 参与签名 |
|---|---|---|---|
| node_id | `node_id` | 16 位 hex（8 字节） | ✅ |
| display_name | `display_name` | 明文 UTF-8 | ❌ |
| public_key | `public_key` | base64（Ed25519 32 字节） | ❌ |
| tcp_port | `tcp_port` | uint16（实际监听端口） | ✅ |
| udp_port | `udp_port` | uint16（实际绑定端口） | ✅ |
| subnet | `subnet` | `"ip/prefix"` 字符串 | ✅ |
| nonce | `nonce` | 32 位 hex（16 字节） | ✅ |
| timestamp | `timestamp` | int64 秒（本地时钟） | ✅ |
| epoch | `epoch` | uint64（目录 epoch） | ✅ |
| sig | `sig` | base64（Ed25519 64 字节） | —（签名本身） |

反解入口 `AnnounceWire.Announcement()`（`udp.go:82-116`）**只做形状校验**（nodeID 可解析、公钥可解析、nonce 恰好 16 字节、签名可 base64 解码），**不做验签**——验签由调用方负责（`broadcast.go:259`、`seed.go:81`）。

### 3.4 签名规范字节

```
"swarmlink-announce-v1"          ← 域分隔串，21 字节
|| nodeID[8]                      ← 原始 8 字节，非 hex
|| TCPPort[2]  大端
|| UDPPort[2]  大端
|| nonce[16]
|| timestamp[8] 大端（有符号 int64 按位重解释为 uint64）
|| epoch[8]     大端
|| subnet[变长] ← 原始字节
```

实现：`internal/domain/peer/peer.go:115-135`；域分隔串常量 `peer.go:108`。

三点必须记住：

1. **`display_name` 与 `public_key` 不参与签名**。前者是有意为之（仅展示用，改动不应影响身份）；后者是因为它本身就是验证签名的输入（改了它签名必然失效）。
2. **`subnet` 参与签名是对架构书的追加**（`peer.go:112-114`）：subnet 用于 P-2 地址重叠检测，若不签名则可被伪造，检测就失去意义。
3. `ObservedIP` / `Leaving` 不参与签名：前者由**接收方**的协议栈给出、对端无法伪造（`peer.go:93-95`），后者由报文类型承载。

### 3.5 SEED_PROBE 载荷

结构体 `SeedProbeWire{node_id, tcp_port, udp_port}`（`udp.go:145-149`）**声明了字段但实际发送空体**：`protocol.EncodeUDP(protocol.UDPTypeSeedProbe, protocol.SeedProbeWire{})`（`seed.go:50`）；接收端 `replyAnnounce` 也不解析 body（`broadcast.go:234-236`）。即第一跳是一个**无内容的触发包**，设计上是「你回我一份你的 ANNOUNCE」的最小语义。

### 3.6 PEER_LIST（走 TCP，不走 UDP）

目录交换复用 TCP 连接，因此带 9 字节帧头（`const.go:16-26`）：

```
Magic(2)=0x53 0x4C | Ver(1)=0x02 | Flags(1) | Type(1) | Length(4, 大端) | Payload
```

| 帧类型 | 值 | 载荷 |
|---|---|---|
| `TypePeerListReq` | `0x03` | `PeerListReqWire{list_epoch}`（`udp.go:134-136`） |
| `TypePeerListResp` | `0x04` | `PeerListRespWire{epoch, entries[]}`（`udp.go:139-142`） |

条目 `PeerListEntry`（`udp.go:123-131`）：`node_id / display_name / public_key / addr / subnet / caps / proto_ver`。注意 `addr` 是**发送方记录的 `LastAddr`**，对方直接拿去拨号（跨网段首条消息零跳数）。

载荷上限：单帧 16 MB（`const.go:26`），但条目数被 `MaxEntries`（默认 256）限制（`discovery_app.go:219-224`）。

---

## 4. 签名与身份

| 项 | 值 | 位置 |
|---|---|---|
| 算法 | Ed25519 | `internal/domain/identity/identity.go`（`Sign`/`Verify`） |
| 签名长度 | 64 字节 → base64 88 字符 | `udp.go:77` |
| NodeID | `SHA-256("swarmlink-node-id-v1" \|\| pubkey)[:8]`，hex 16 字符 | `identity.go` |
| 公钥指纹校验 | `Fingerprint(pubKey) == nodeID` | **仅 TCP 握手**：`internal/adapters/net/tcp/handshake.go:139-140,221` |

**发现层与握手层的信任分工**（这是理解本协议安全性的关键）：

- **UDP 层**证明的是「这条通告出自持有该私钥的人」——验签用的是报文内自带的 `public_key`（`broadcast.go:259`）。
- **UDP 层不检查** `nodeID == Fingerprint(pub_key)` 是否自洽。也就是说，一个自签的「`node_id` 与 `pubkey` 不匹配」的条目**能进入目录**，直到真正 TCP 拨号时才会在握手阶段（`handshake.go:140`）被识破。
- 因此目录条目的定位是**线索而非权威**：身份最终由握手阶段的三段挑战-应答（ADR-002）确认。

这个缺口的具体影响与修法见 §15.1。

---

## 5. 时序与参数总表

### 5.1 常量表

| 参数 | 默认值 | 语义 | 位置 |
|---|---|---|---|
| `AnnounceInterval` | **45 s** | 广播周期基准 | `broadcast.go:55`；配置 `config/config.go:121` |
| `JitterRatio` | **0.33** | 周期抖动比例 → 实际 30–60 s | `broadcast.go:56` |
| 首发时机 | 等满一个抖动周期后才发 | **无立即首发** | `broadcast.go:187-198` |
| `byeRounds` | **3** 次，间隔 20 ms | 下线通告重发 | `broadcast.go:276,294-299` |
| 广播写超时 | 2 s | 每次 `DialUDP` 新建 socket | `broadcast.go:327` |
| `Stop` 收敛等待 | 2 s | 等 goroutine 退出 | `broadcast.go:163` |
| UDP 读缓冲 | 65535 B | 单包上限 | `broadcast.go:202` |
| `Probe.Timeout` | **2 s** | 种子探测读超时 | `seed.go:34-37`；`config.go:127` |
| 种子刷新 `RefreshInterval` | **300 s**（启动时立即做一轮） | 跨网段收敛上界 | `discovery_app.go:69,82-95` |
| `SeedsPerRefresh` | **2**（校验上限 8） | 每轮随机拉的种子数 | `discovery_app.go:70`；`config.go:255-261` |
| 退避基准 / 上限 | **1 s / 10 min** | `d = base × 2^min(failCnt,10)` 封顶 | `seed.go:137-138,196-207` |
| `PeerTTL` | **300 s** | 条目过期判据 | `config.go:122`；`peer.go:75-77` |
| 目录 TTL 扫描节拍 | **1 s** | 组合根统一 ticker | `bootstrap.go:270-289` |
| `MaxEntries` | **256** | 单次目录响应条目上限 | `config.go:124` → `discovery_app.go:71,219-224` |
| 端口回退范围 | **10** → `2425..2434` | TCP 与 UDP 共用 | `config.go:114`；`bootstrap.go:309-372` |
| `defaultUDPPort` | **2425** | `udp_port=0` 时的落点 | `bootstrap.go:341,346-348` |
| TCP 帧上限 | 16 MB | 超大帧即协议违规 | `const.go:26` |

### 5.2 为什么要 45 s 而不是 5 s

`broadcast.go:46-52` 把取舍写在了注释里：45 s ± 33% 让新节点一分钟内被看见，同时把纯保活流量压到可忽略；对比 5 s 一轮，200 节点约 40 pkt/s 的常驻噪声，换来的只是「早几秒被看到」。

代价是「对方异常掉线」感知变慢，因此**用 BYE 补足**：正常关闭立即通知（秒级），只有崩溃/断电才回退到 300 s TTL 兜底。

### 5.3 抖动算法

`broadcast.go:376-386`：`f ∈ [-1,1)` 均匀取自 `crypto/rand`，返回 `d × (1 + f × ratio)`；`ratio ≤ 0` 时原样返回。这是**乘法抖动**，不是简单的 `d ± ratio` 绝对值。目的：避免多节点同时开机造成的同步风暴。

注意随机源不统一：jitter 用 `crypto/rand`（`broadcast.go:380`），种子随机选择用 `math/rand`（`seed.go:136`，`time.Now().UnixNano()` 播种）。前者是对抗性场景，后者只是负载分散，区分合理。

### 5.4 时间来源

领域逻辑统一走 `ports.Clock`（`ports.go:36-40`）：`announceLoop` 用 `clk.After`、`buildAnnounce.Timestamp` 用 `clk.Now().Unix()`（`broadcast.go:194,367`）、`DiscoveryApp` 用 `clk.NewTicker`——因此测试可用 `clock.Fake` 精确推进时间。

但有几处**用了真实 `time`**，属于有意为之的实现细节：

- BYE 之间的 20 ms 间隔（`broadcast.go:297`）
- `Stop` 的 2 s 收敛等待（`broadcast.go:163`）
- 广播写截止时间（`broadcast.go:327`）
- 组合根的 1 s ticker（`bootstrap.go:274`）

---

## 6. 状态机

`peer.State` 的语义（`peer/peer.go:15-26`）：

```
              announce 到达                      TCP 握手成功
   (无) ──────────────────► discovered ──────────────────────► online
                              │   ▲                              │
              TTL 过期        │   │ idle_timeout 回退            │
                    ┌─────────┘   └──────────────┐               │
                    ▼                            │               │
                 offline ◄──── BYE / 连接断开 ◄─────────────────┘
```

| 触发源 | 状态变化 | 事件 | 位置 |
|---|---|---|---|
| 收到 ANNOUNCE（新节点） | → `discovered` | `TopicPeerDiscovered` | `peer_app.go:87,131-133` |
| 收到 ANNOUNCE（原为 online） | **保持 `online`** | `TopicPeerDiscovered` | `peer_app.go:88-90` |
| 收到 BYE | → `offline` | `TopicPeerOffline{reason:"leave"}` | `peer_app.go:67-81` |
| TCP 握手成功 | → `online`（并刷新 `LastAddr`） | — | `peer_app.go:143-164` |
| TCP 连接空闲回收 | `online` → `discovered` | `TopicPeerOffline{reason:"idle_timeout"}` | `peer_app.go:177-184` |
| TCP 连接断开（其他原因） | → `offline` | `TopicPeerOffline` | `peer_app.go:185-187` |
| TTL 扫描（仅 `discovered`） | → `offline` | `TopicPeerOffline{reason:"ttl_expired"}` | `peer_app.go:192-209` |

三条不可混淆的语义（这是 ADR-009「在线状态与连接解耦」的落地）：

1. **「节点在线」由 announce 维护，「连接可用」由 TCP 维护，两者互不覆盖**（`peer_app.go:86-90`）。因此一个 `online` 的节点可以没有连接（发消息时才拨号），一个 `discovered` 的节点也可能连接良好。
2. `idle_timeout` **只降级为 `discovered`，不判离线**（`peer_app.go:175-184`）——它可能仍在广播 ANNOUNCE。UI 必须区分这两种状态。
3. `Sweep` 只处理 `State == StateDiscovered` 的条目（`peer_app.go:195-197`）：`online` 条目的存活由连接层事件负责。

---

## 7. 多网卡与地址选择

### 7.1 枚举流程

`EnumerateInterfaces(allow, deny)`（`iface.go:71-127`）：

1. 排除 `FlagUp == 0`、`FlagLoopback != 0` 的接口（`:79-84`）
2. 命中 `deny` 的接口排除（`:85-87`）
3. `allow` 非空 → 只保留命中项；**`allow` 为空** → 用 `looksVirtual(name)` 排除（`:88-94`）
4. 地址只取 IPv4；排除 `IsLinkLocalUnicast()`（169.254/16）；16 字节掩码截断为 4 字节（`:105-117`）
5. `subnetBroadcast(ip, mask)` 计算**子网广播地址**：`ip[i] | ^mask[i]`（`:129-138`）

### 7.2 虚拟网卡黑名单

`virtualPrefixes`（`iface.go:29-33`）——注意这是**名称前缀**匹配：

```
lo  docker  br-  veth  virbr  vmnet  vboxnet  utun  tun  tap
tailscale  zerotier  wg  zt  hyper-v  vethernet  npcap  bridge  awdl
```

默认 deny 清单（`broadcast.go:57` / `config.go:116`）：`utun*, vmnet*, vboxnet*, docker*`。

`matchesList` 支持两种模式（`iface.go:45-63`）：`前缀*` 通配、以及精确匹配（大小写不敏感）。架构书提到的「无物理 MAC 判定」在实现中**没有做**，只有名称前缀判定。

### 7.3 为什么必须用子网广播地址

`iface.go:129-131` 的注释就是答案：`255.255.255.255` 会被路由器、启用 IGMP snooping 的交换机丢弃，macOS 10.15+ 也默认禁止非特权进程向它发送。`iface_test.go` 锁定了这条规则。

### 7.4 收发 socket 的取舍

| 方向 | 做法 | 位置 |
|---|---|---|
| 发送 | 每个接口 × 每个目标端口 `net.DialUDP`，`LocalAddr = iface.IP`，目标 = `iface.Broadcast:port` | `broadcast.go:318-330` |
| 接收 | **单个** `0.0.0.0:udp_port` socket 收全部广播 | `broadcast.go:121` |

架构书建议的 `SO_BINDTODEVICE`（Linux）/ `IP_BOUND_IF`（macOS）**没有采用**，改用「源地址绑定接口 IP」这一可移植等价手段，代价是无法强制「只从某网卡收」。这条取舍记录在 `docs/adr/ADR-008-port-and-interface-policy.md:34-44`，也是**本仓库没有任何 `iface_*.go` 平台特定文件**的原因。

### 7.5 `interface_mode` 三档的真实行为

| 模式 | 实现行为 |
|---|---|
| `auto`（默认） | 走上面 6 步；`allow` 为空时排除虚拟网卡 |
| `manual` | **与 `auto` 走完全相同的代码路径**——没有任何分支。差异仅在于用户是否填了 `allow_interfaces` |
| `seed_only` | 映射为 `bcCfg.SeedOnly`（`bootstrap.go:215`）：不启 `announceLoop`（`broadcast.go:131-134`）、不发 BYE（`broadcast.go:286`）、仍保留 `recvLoop` 与种子探测 |

`manual` 与 `auto` 无差异属于实现与配置文档的口径不一致（`config.go:65` 注释写的是三档语义），已列入 §15。

---

## 8. 端口策略

### 8.1 TCP：可以交给系统

`startTCP`（`bootstrap.go:309-333`）在 `base..base+fallback-1` 内逐个尝试；`base == 0` 交给系统随机分配。全部失败返回错误（TCP 起不来 = 节点不可用，属硬失败）。

之所以「0 = 系统分配」是安全的：对端从 ANNOUNCE 学到实际端口再拨号（`bootstrap.go:339-340`）。

### 8.2 UDP：必须落在基准端口上

`udp_port = 0` **不允许**落到系统随机端口，而是强制 `2425`（`bootstrap.go:341-348`）。原因写得很直白：广播发现要求收发双方落在**同一个**端口，而 ANNOUNCE 携带的端口只对单播有用——随机端口意味着两台机器几乎必然错开，广播就永远收不到对方的 ANNOUNCE。

### 8.3 广播目标是一个端口集合

`AnnouncePorts`（`broadcast.go:35-43`）是**目标端口集合**而非单一端口：`startBroadcast` 把整个回退区间（`2425..2434`）灌进去（`bootstrap.go:353-360`）。这样即便某台机器因 2425 被占而落在 2426，按 2425 广播的其他节点也到得了它——否则「对端从 announce 学习、无需配置端口」这个承诺在发现场景就不成立。

端口回退成功后会在 `Node.Notices` 追加一条面向用户的提示（`bootstrap.go:228-232`）。

---

## 9. 种子机制（跨网段）

### 9.1 设计立场

**种子不是服务器，是「配置里多列了几个 IP 的普通节点」**（`ports.go:209-211`、`docs/adr/ADR-011`）。落到代码上的表现：

- `ports.SeedRegistry` **只有** `Pick` 与 `Report` 两个方法，没有任何特权语义；注释明说「若这里出现 `Relay` / `Authoritative` 之类的方法，说明设计已经走形」（`ports.go:207-217`）
- 种子产出的就是普通 `peer.Announcement`（`seed.go:118-119`），走同一条 Upsert 路径（`discovery_app.go:116-121`）
- **数据源只有一个**：`[discovery.seeds].list`（`config/seeds`）→ `Config.SeedAddrs()`（`config.go:220-235`，非法条目跳过并收集错误）→ `udp.NewRegistry`（`seed.go:133-147`）

### 9.2 两跳流程

```
第 1 跳（UDP）                       第 2 跳（TCP）
节点 A                                 A
  │ DialUDP(种子ip:udp_port)  已连接     │ Dial(种子 node_id, ip:tcp_port)
  │ ──── SEED_PROBE（空体）──────────►   │ ──── PEER_LIST_REQ{list_epoch} ───►
  │ ◄─── ANNOUNCE（完整签名，单播）───    │ ◄─── PEER_LIST_RESP{epoch,entries} ─
  │  ①验签 ②学 tcp_port/node_id/subnet   │  合并进目录（Source=seed）
  │  ③ Report(成功)                      │
```

实现要点：

1. 探测用**已连接** UDP socket（`net.DialUDP`，`seed.go:43-48`）：只接受来自该种子地址的回复，天然过滤伪造源。副作用是**硬性要求种子 `udp_port` 与配置一致**——种子若回退端口，回包源端口不匹配，连接会直接拒收（这正是 ADR-011 要求种子固定 UDP 端口的原因，由 socket 语义强制而非靠约定）。
2. 只接受 `UDPTypeAnnounce` 类型并**逐个跳过**不合格的包（`seed.go:68-83`），直到超时。
3. 种子侧由 `recvLoop` 的 `UDPTypeSeedProbe` 分支触发 `replyAnnounce`（`broadcast.go:234-236`）——**任何节点都回探测**，不分角色。
4. `replyAnnounce` 的 `subnet` 选择规则（`broadcast.go:334-353`）：取**包含探测源 IP** 的接口；若都不包含（例如从回环探测），退化为首个接口的子网；**绝不留空**——空值会让对端失去 P-2 这一唯一冲突检测信号。
5. 第二跳在 goroutine 里异步执行（`discovery_app.go:123`），不阻塞刷新循环。

### 9.3 注册表与退避

`seedState`（`seed.go:94-103`）保存：`addr / failCnt / nextTry / lastSeen / nodeID / tcpPort / subnet / lastEpoch`。

- **`Pick(n)`**（`seed.go:158-184`）：先剔除「`failCnt > 0 && now < nextTry`」（退避中）的；若全在退避则返回 `nil`（本轮不拉，避免反复敲已下线的种子）；`math/rand` 洗牌后取前 n。随机而非固定顺序，避免 200 节点集中打同一颗。
- **`Report(addr, learned, err)`**（`seed.go:187-216`）：
  - 失败（`err != nil || learned == nil`）→ `failCnt++`，`nextTry = now + base×2^min(failCnt,10)` 封顶 10 min
  - 成功 → `failCnt = 0`、`nextTry` 清零、记录 `nodeID/tcpPort/subnet/lastSeen`
- **只对第一跳计数**：`pullFromSeed`（第二跳 TCP）失败**不会**回灌 `Report`（`discovery_app.go:127-151` 无 `registry.Report` 调用）。因此 P-1 不满足时（UDP 通、TCP 不通）种子**不会被退避**，每轮都会重试——从可用性看是好事（网络恢复即自愈），从流量看是浪费。已列入 §15。
- `Snapshot()` 供诊断面板（`seed.go:238-254`），经 `DiscoveryApp.SeedSnapshot()`（`discovery_app.go:260-265`）暴露到 UI。

---

## 10. 目录交换与收敛

### 10.1 请求 / 响应语义

| 场景 | 响应 | 位置 |
|---|---|---|
| `list_epoch == 0`（首次/未知） | 返回全量目录 | `discovery_app.go:162-165` |
| `list_epoch != local`（对端目录已变） | 返回全量目录 | 同上 |
| `list_epoch == local`（内容未变） | **空响应**（只带 epoch，省流） | `discovery_app.go:162-163` |

条目构造（`discovery_app.go:219-245`）：从目录取 `MaxEntries`（256）条，跳过 `StateBlocked`，并把公钥序列化带上——这样接收方拿到的条目是可拨号、可校验身份（握手时）的完整线索。

### 10.2 收敛与防环：两条规则

架构书 §4.5.1 明确要求「不需要 `via` 字段、不需要来源标记」，实现严格遵守，靠的是：

1. **条目按 `node_id` 去重**：`Upsert` 天然保证（mem 实现以 NodeID 为 map key；sqlite 实现用 `ON CONFLICT(node_id) DO UPDATE`，见 `internal/adapters/store/sqlite/peers.go:32-38`）
2. **TTL 用本地到达时间**：`LastSeen = now`（本地时钟，`peer_app.go:111`、`discovery_app.go:192`），**绝不使用对端 `timestamp`**（`peer.go:66-68` 有醒目注释）。因为新鲜度由接收方本地维护、不会被中转「刷新」，所以不存在「陈旧条目被反复回灌」，也就无需 hop 计数。

另外 `HandlePeerListResp` 会跳过 `id == self` 的条目（`discovery_app.go:182`）。

### 10.3 ⚠ 一个已确认的功能缺口：`BumpEpoch` 无人调用

这是本次代码核对中最重要的发现，直接影响跨网段刷新：

- 广播器的 epoch **只在 `Start` 时置 1**（`broadcast.go:110`），`Epoch()` 直接读它（`:101`），而 `LocalEpoch` 接线到它（`bootstrap.go:258`）。
- `BumpEpoch()`（`broadcast.go:103-104`）的注释是「本地目录变化时调用，使对端重新拉取」，但**全仓库没有任何调用点**（唯一引用是定义本身）。
- 客户端把每个对端的响应 epoch 记在 `lastEpoch map[NodeID]uint64`（`discovery_app.go:44,177`），请求时带上（`:145-146`）。

于是实际行为是：

```
(本机 → 该种子) 第 1 次拉取：list_epoch = 0  → 返回全量目录 ✅
               第 2 次起  ：list_epoch = 1  → 1 == local(1) → 返回空 ❌ 且永久如此
```

**后果**：同一对 `(本机, 种子)` 只会有一次全量同步；之后哪怕种子那边学到了一堆新节点，也不会再传过来（直到本机重启）。正常广播仍工作，所以**同网段发现不受影响**，但跨网段的「新节点 ≤ 1 个刷新周期可见」这条承诺目前不成立。ADR-011 的成本核算里「目录变更时上量 < 2 KB/s」也因此从未发生过。

`test/e2e.TestCrossSubnetConvergenceViaSeed`（`test/e2e/e2e_test.go:246-300`）之所以通过，是因为它每次都用**全新的** `(本机, 种子)` 组合去拉——恰好走的是「第 1 次」。

修法（§15.2 给了优先级）：让目录变化时真的 bump epoch，或干脆取消 `list_epoch` 优化——200 节点规模下一次全量响应约 12.8 KB，5 分钟一次，成本可以忽略。

---

## 11. 与架构书的差异

以下逐条是**实现与 `架构设计书.md` §4.5 / §4.5.1 的不一致**，以实现为准（表中左列为架构书原文口径）：

| # | 架构书 | 实现 | 影响 |
|---|---|---|---|
| 1 | 广播间隔 5 s，±20% 抖动 | **45 s**，±33%（`broadcast.go:55-56`） | 保活流量降一个数量级；掉线感知靠 BYE 补足 |
| 2 | 本地目录上限 512、按 `last_seen` LRU 淘汰、本地缓存 600 s 硬上限 | **未实现**。`MaxPeers=512` 配置项存在但**无任何代码读取**；目录无容量上限与淘汰 | 200 节点下无影响；缺上限意味着被污染时目录可无界增长（见 §15.4） |
| 3 | 单响应条目 ≤ 256 | ✅ 一致（`MaxEntries`/`MaxPeerListSize`） | — |
| 4 | 签名覆盖 `magic\|ver\|nodeID\|tcpPort\|nonce\|timestamp\|epoch` | 域分隔串改为 `"swarmlink-announce-v1"`，**追加 `udp_port` 与 `subnet`** | 必须同步：`udp_port` 参与签名才能防止端口被篡改；`subnet` 见 P-2 |
| 5 | 节点交换对新节点立即做一次，之后指数退避 30 s→…→600 s | 种子探测走**固定周期（300 s，启动时立即一次）+ 随机选 2 颗 + 失败指数退避 1 s→10 min**（退避对象是「种子」而非「任意新节点」） | 语义不同但更简单：不需要维护「已与谁交换过」的节点集合 |
| 6 | 接口绑定用 `SO_BINDTODEVICE` / `IP_BOUND_IF`，需平台特定代码 | 源地址绑定替代，**零平台特定文件**（ADR-008:34-44） | 无法强制「只从某网卡收」 |
| 7 | UDP 包「尾部校验位」 | **无尾部校验**：魔数 + 版本 + 长度校验即可（UDP 自身有校验和） | 无实质影响 |
| 8 | 「无物理 MAC 判定」辅助识别虚拟网卡 | 只按名称前缀 | 未在清单内的虚拟网卡（如自定义 `bridge0`）可能漏网 |
| 9 | 三档网卡策略「自动 / 指定网卡 / 仅显式种子」 | `seed_only` 有独立分支；**`manual` 与 `auto` 同路径** | 语义靠用户填 `allow_interfaces` 实现 |

---

## 12. 安全模型与威胁分析

### 12.1 已覆盖的威胁

| 威胁 | 对策 | 位置 |
|---|---|---|
| 伪造他人身份广播 | Ed25519 签名，验签不过静默丢弃 | `broadcast.go:259`、`seed.go:81` |
| 收到自己的广播（UDP 回环） | `NodeID == self` 丢弃（UDP 层与目录层各一道） | `broadcast.go:256`、`peer_app.go:60` |
| 伪造 BYE 把别人踢下线 | BYE 与 ANNOUNCE **同构共用同一条验签路径**（注释专门说明了这条：单独写一套最容易漏掉验签） | `broadcast.go:241-263` |
| 陌生 UDP 报文误触发 | 4 字节魔数 + 长度校验，非本协议静默丢弃 | `udp.go:37-45` |
| 中间人篡改种子身份 | 第一跳起就验签，种子身份不可伪造 | `seed.go:80-83` |
| 探测者被伪造的回包欺骗 | 探测用**已连接** socket，只接受来自目标地址的回复 | `seed.go:43-48` |
| 陈旧条目永久驻留 | `LastSeen` 本地时间 + TTL 300 s | `peer_app.go:192-209` |
| 多节点同步风暴 | 广播抖动 + 种子随机选择 + 失败退避 | `broadcast.go:376-386`、`seed.go:158-184` |
| 超大帧内存炸弹 | 单帧 16 MB 上限 | `const.go:26` |

### 12.2 尚未覆盖 / 需要知情接受

1. **`nodeID ↔ pubkey` 自洽性在 UDP 层未校验**（§4、§15.1）。攻击者可用自造密钥对广播任意 `node_id`，在目录里制造「身份与公钥不匹配」的条目。真正拨号时握手会拒绝，所以**不能冒充身份，但能污染目录**（占条目、显示假名字、造成指向错误 IP 的拨号尝试）。
2. **目录条目是广播明文**：`display_name`、`subnet`、两个端口对同网段任何主机可见（被动嗅探即可）。v1.0 不加密（`security.encryption = off`），这是既定取舍。
3. **无新鲜度检查**：`nonce` 与 `timestamp` 都参与签名，但接收方**不检查**时间窗。重放一条旧 ANNOUNCE 只会刷新 `LastSeen`（危害小）；重放一条旧 BYE 可让该节点在目录里显示离线，直到下一条 announce 到来（≤ 60 s）——危害有限但存在。
4. **探测回包存在放大效应**：请求是空体（8 字节），回包是完整签名 ANNOUNCE（数百字节）。`broadcast.go:235` 注释写「回包与请求同量级」，**这句注释与事实不符**，实际放大约几十倍。配合伪造源 IP，同网段攻击者可把每个 SwarmLink 节点当成反射器（规模上受限于「同网段」与「每个包一份签名回包」的开销，但仍是可用的放大面）。
5. **目录无容量上限**（§11 第 2 条）：攻击者可用大量自造 NodeID（每个都自签、成本极低，因为不要求 `nodeID == Fingerprint(pubkey)`）把目录与 SQLite 表撑大。缺少 `MaxPeers` 淘汰逻辑使这条路径没有刹车。
6. **探测无鉴权**：任何人都能触发一条回包（设计如此）。这意味着「本机是否在运行 SwarmLink」在同网段可被主动探测确认——与广播本身暴露的信息量相同，不额外增加暴露面。

---

## 13. 代码索引

| 文件 | 职责 |
|---|---|
| `internal/domain/protocol/const.go` | UDP/TCP 常量、魔数、消息类型编号 |
| `internal/domain/protocol/udp.go` | UDP 封套编解码 + ANNOUNCE / PEER_LIST / SEED_PROBE 线上结构 |
| `internal/domain/protocol/codec.go`、`wire.go` | TCP 帧编解码、JSON 载荷与 nonce 十六进制编解码 |
| `internal/domain/peer/peer.go` | `Announcement`、`SigningBytes`、`Peer`、`State`、`SeedAddr` |
| `internal/domain/ports/ports.go` | `DiscoveryStrategy`、`SeedRegistry`、`SeedProber`、`PeerDirectory`、`Clock`、`EventBus` 接口 |
| `internal/adapters/net/udp/broadcast.go` | 广播器：枚举、收发循环、抖动、BYE、验签、端口集合广播 |
| `internal/adapters/net/udp/iface.go` | 接口枚举、虚拟网卡过滤、子网广播地址计算 |
| `internal/adapters/net/udp/seed.go` | 种子探测（第一跳）与注册表（随机选择 + 指数退避） |
| `internal/app/peer_app.go` | `Announcement` → 目录的归一化、状态机、TTL 扫描 |
| `internal/app/discovery_app.go` | 跨网段编排：刷新循环、两跳流程、目录请求/响应 |
| `internal/app/router.go` | `TypePeerListReq/Resp` 分派到 `DiscoveryApp`（`router.go:56-59`） |
| `internal/app/selfcheck.go` | P-1 / P-2 部署前置条件自检 |
| `internal/adapters/config/config.go` | `[network]` / `[discovery]` / `[discovery.seeds]` 配置与默认值 |
| `internal/bootstrap/bootstrap.go` | 组合根：端口回退、装配、TTL ticker |
| `internal/adapters/store/{mem,sqlite}` | 目录实现（mem 用于试验/测试，sqlite 用于生产，`bootstrap.go:137-172`） |

---

## 14. 测试与验收

| 测试 | 验证的行为 | 位置 |
|---|---|---|
| `TestAnnounceReachesPeerOnDifferentPort` | 端口错开时 `AnnouncePorts` 让 ANNOUNCE 仍可达 + 验签有效 | `internal/adapters/net/udp/broadcast_test.go:23` |
| BYE 组（3 个用例） | Stop 广播带签名的 BYE、反复 Stop 幂等、默认周期落在 30–60 s | `internal/adapters/net/udp/bye_test.go:18,82,105` |
| 接口组（6 个用例） | 子网广播地址、`SubnetString`、`Contains`、`looksVirtual`、通配匹配、枚举排除 loopback/link-local | `internal/adapters/net/udp/iface_test.go:8-70` |
| 种子组（5 个用例） | `Pick` 的退避过滤与随机、成功清零、全退避返回 nil、跳过空种子、`Probe` 验签与超时 | `internal/adapters/net/udp/seed_test.go:15-123` |
| 协议往返组 | UDP 报文编解码、拒绝外来包、ANNOUNCE 往返含验签、nonce 长度约束、PEER_LIST_RESP 往返 | `internal/domain/protocol/udp_test.go` |
| `const_test.go` | ABI 常量（含 `UDPMagic`）稳定性 —— 改这些值会被测试拦住 | `internal/domain/protocol/const_test.go` |
| 目录/BYE 行为 | BYE 标离线、不因后续 announce 复活 online、未知节点 BYE 为空操作 | `internal/app/peer_app_test.go:17,71,106` |
| `TestCrossSubnetConvergenceViaSeed` | 两个互不广播的域 + 一颗种子 → 目录双向收敛、条目带可拨号地址且 `source=seed`、跨网段首条消息直连送达 | `test/e2e/e2e_test.go:246-300` |
| 规模用例 | 20 节点全量目录收敛、**零常驻连接** | `test/scale/scale_test.go:17` |

复现命令：

```bash
make test          # 全量单测
make test-e2e      # 端到端（含跨网段收敛）
make test-scale    # 规模
```

**未覆盖**：`DiscoveryApp` 没有独立单测（只由 e2e 间接覆盖）；`Registry.SetLastEpoch/LastEpoch` 无测试（因为生产链路未使用）。§10.3 的 epoch 缺口**现有测试无法发现**——这是它值得单独立一条测试的原因。

---

## 15. 已知缺口与演进建议

按「修了收益最大」排序。

### 15.1 P0 — UDP 层补 `nodeID == Fingerprint(pubkey)` 校验

**现状**：只有 TCP 握手校验（`handshake.go:140,221`）；`decodeAnnounce` 用报文自带的公钥验签，不检查它与 `node_id` 是否自洽（`broadcast.go:246-263`）。

**后果**：目录可被塞入身份自相矛盾的条目；配合 §11 第 2 条（目录无上限）构成低成本污染面。

**修法**：在 `decodeAnnounce` 验签**之后**加一行 `if identity.Fingerprint(a.PublicKey) != a.NodeID { return ..., false }`，与握手侧口径一致（一处 3 行改动 + 一个用例：自造 key 声明他人 node_id 的报文必须被丢弃）。种子探测路径同理（`seed.go:81` 处）。

### 15.2 P0 — 让 `list_epoch` 真正生效（或删掉它）

**现状**：`BumpEpoch` 无调用点 → epoch 恒为 1 → 每个 `(本机, 种子)` 对只同步一次（§10.3）。

**两条路**：

- **A（保留优化）**：目录发生实质变化时 bump。需要给 `DiscoveryApp`/`PeerApp` 一个 epoch 递增的端口（现在 `LocalEpoch func() uint64` 是只读的），在 `Upsert` 新条目、`MarkOffline`、BYE 处理处调用。
- **B（取简法）**：`HandlePeerListReq` 忽略 `list_epoch`，永远返回全量。200 节点下一次响应约 12.8 KB、每节点 5 分钟一次，代价是单种子出站约 `< 2 KB/s` —— 与 ADR-011 的核算结论一致，且**没有正确性风险**。

无论选哪条，都补一个测试：**同一对节点连续两轮拉取，第二轮必须能拿到新加入的节点**。现有 e2e 用例结构上覆盖不到这一点。

### 15.3 P1 — 目录容量上限与淘汰

落实架构书的 `MaxPeers=512`：在目录实现或 `PeerApp` 层加「超过上限按 `LastSeen` LRU 淘汰」，并给 `MaxPeers` 一个真实的读取点（现在是死配置）。这条同时把 §12.2.5 的污染面收口。

### 15.4 P1 — 回包放大的注释与实现对齐

`broadcast.go:235` 的「回包与请求同量级」不成立（请求 8 字节，回包数百字节）。二选一：把注释改成事实并接受该放大面（同网段前提下可接受），或让探测请求携带一个挑战值、回包只带必要字段以压缩体积。至少不应保留一句误导性的注释。

### 15.5 P2 — 语义/一致性收尾（改动都很小）

| 项 | 现状 | 建议 |
|---|---|---|
| `interface_mode = manual` | 与 `auto` 同路径 | 要么实现差异，要么在配置注释与 UI 上说明「manual 等价于 auto + allow_interfaces」 |
| `EnableMulticast` / `MulticastAddr` | 被赋值但从未读取 | 删除字段，或在文档里标注为未实现 |
| `SourceMulticast` / `SourceGossip` | 常量存在、无实现 | 同上 |
| `UDPTypePeerListReq/Resp` | 死常量（实际走 TCP） | 删掉，避免后来者误以为有一条 UDP 目录交换路径 |
| `SeedProbeWire` 的三个字段 | 实际发空体、接收端不解析 | 删除字段或补上内容（如携带自身 node_id 便于种子侧日志） |
| `Registry.SetLastEpoch/LastEpoch` | 生产链路未调用（`DiscoveryApp` 自建 map） | 删掉，或改由 registry 承担该职责 |
| `SeedTCPPortFallback` | `bootstrap.go:257-261` 未赋值 → 恒为 0 | 接上配置（种子未回 tcp_port 时的兜底），或删掉字段 |
| `PeerDirectory.SeenRecently` | 端口与两个实现都有，**发现层从未调用** | 接上（可作为「目录变化 → bump epoch」的信号），或删掉 |
| `Announcement.Epoch` | 参与签名，但接收方从不消费 | 明确其用途（如用于 §15.2 的目录版本），或从线格式移除 |

### 15.6 P2 — 测试补齐

- `DiscoveryApp` 的独立单测（刷新循环、两跳、epoch 语义）
- §15.1 的身份自洽性用例
- §15.2 的「连续两轮拉取」用例
- 一个「种子 `udp_port` 与配置不一致」的用例，锁定「种子必须固定 UDP 端口」这条硬约束的行为表现

---

## 附录 A：关键配置项

```toml
[network]
tcp_port = 0                 # 0 = 系统分配（安全：对端从 announce 学）
udp_port = 0                 # 0 → 强制落到 2425（不能用系统随机）
port_fallback_range = 10     # 2425..2434
interface_mode = "auto"      # auto | manual | seed_only
allow_interfaces = []        # 例如 ["en0", "eth*"]
deny_interfaces = ["utun*", "vmnet*", "vboxnet*", "docker*"]

[discovery]
announce_interval = "45s"    # 实际 30~60s（±33% 抖动）
peer_ttl = "300s"
max_peers = 512              # ⚠ 当前无读取点（见 §15.3）
max_peer_list_size = 256     # 单次目录响应条目上限

[discovery.seeds]
refresh_interval = "300s"
seeds_per_refresh = 2        # 上限 8，超过直接报错
probe_timeout = "2s"
list = ["10.1.1.5:2425", "10.2.1.5:2425"]   # ⚠ 种子机器必须固定 udp_port
```

## 附录 B：一次完整的「新节点加入」时序

```
T+0      新节点启动 → startTCP（可能回退）→ startBroadcast（UDP 回退或 2425）
                  → Start 后 recvLoop 立即可用，announceLoop 开始等第一个抖动周期
T+0..+60 同网段其他节点在各自的下一次广播中带上新节点；新节点也在自己的
          第一个周期发出 ANNOUNCE（含实际 tcp_port / udp_port / subnet）
T+≤60s   双方各自验签 → Upsert(State=discovered, LastSeen=本地now)
          → Publish(TopicPeerDiscovered) → UI 出现该节点
T+0      DiscoveryApp.Start 立即执行一次 RefreshSeeds（不等 300s）
          第 1 跳 UDP 探测种子 → 验签 → Report(成功) → 走同一条 Upsert
          第 2 跳 TCP PEER_LIST_REQ → 合并种子目录（Source=seed，带可拨号地址）
T+300s.. 后续每 300s 重复刷新 —— ⚠ 但见 §10.3：当前从第 2 次起返回空响应
发消息   目录里有 IP → 按需 TCP 拨号 + 三段握手（身份在此最终确认）→
          空闲 5 分钟回收；节点仍是 discovered，不判离线（ADR-009）
关闭     Stop() → 3×BYE（间隔 20ms）→ 对端立即 MarkOffline(reason=leave)
崩溃     无 BYE → 对端等下一条 announce 超时，最多 300s 后由 Sweep 判离线
```
