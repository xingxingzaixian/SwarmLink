# 内网P2P聊天与文件分享工具 — 技术研究及设计方案

**版本**: v1.0
**日期**: 2026-09-20
**技术栈**: Go + Wails v3.0.0-beta.23 + Vue3


## 一、项目概述

### 1.1 项目定位

构建一款类似飞秋、内网通、飞鸽传书的**无中心节点**内网聊天与文件分享工具。应用运行于可路由的局域网环境中（节点间网络可互相 ping 通），支持跨网段场景，但不依赖 NAT 穿透。所有节点对等，不存在中心服务器。

### 1.2 核心功能

| 功能 | 描述 | 优先级 |
|---|---|---|
| 节点自动发现 | 本网段 UDP 广播自动发现，跨网段通过种子 IP + 节点交换扩散 | P0 |
| 文本聊天 | 点对点实时文本消息，支持群聊 | P0 |
| 文件传输 | 点对点文件传输，支持断点续传、分块校验 | P0 |
| 节点身份 | Ed25519 密钥对持久化，公钥指纹作为节点唯一标识 | P0 |
| 聊天记录 | SQLite 本地持久化，支持历史查询 | P1 |
| 端到端加密 | 基于 Ed25519 密钥协商的对称加密 | P1 |
| 用户设置 | 文件保存路径、自动打开等配置项持久化 | P1 |

### 1.3 约束与前提

- **网络环境**：节点间网络可互相 ping 通（可路由内网），不要求同一广播域
- **无中心节点**：所有节点对等，不依赖任何中心服务器或引导节点
- **跨网段发现**：接受首次手动添加种子 IP，后续通过节点间交换自动扩散
- **文件传输**：暂不支持文件夹传输，仅支持单文件
- **UI 框架**：Wails v3.0.0-beta.23（最新 Beta 版本）
- **文件保存路径**：用户设置默认路径，接收时不弹框


## 二、技术调研

### 2.1 服务发现方案调研

局域网节点发现主要有四种技术路径，各自适用于不同的网络场景。

**UDP 广播**是实现零配置发现最简单直接的方式。发送端向子网广播地址（如 `192.168.1.255`）发送通告包，同广播域内所有节点均可接收。但需要注意：路由器、交换机（尤其启用了 IGMP snooping 的企业级设备）会丢弃目的地址为 `255.255.255.255` 的 UDP 包；macOS 10.15+ 默认禁止非特权进程向 `255.255.255.255` 发送；Windows 防火墙可能静默拦截 UDP 广播包。因此，应使用 `net.InterfaceAddrs()` 显式获取本机活跃 IPv4 地址，构造子网广播地址而非硬写 `255.255.255.255`，同时绑定监听 socket 时使用 `0.0.0.0:port` 而非 `127.0.0.1:port`。

**UDP 组播**可用于跨网段发现（前提是路由器开启 IGMP 和组播路由）。组播地址应选择 `224.0.1.0/24` 或 `239.0.0.0/8` 中的可配置地址（如 `239.255.255.250`），避开 `224.0.0.0/24`（Linux 内核有特殊处理，普通 socket 无法 bind）。

**mDNS（多播 DNS）** 是标准化的零配置发现协议，Go 生态中有 `hashicorp/mdns`、`zeroconf` 等成熟库。但 mDNS 默认只查本地链路（link-local）IPv4 多播，多网卡环境下可能发包到错误接口。在企业网络中，mDNS 跨网段需要 mDNS reflector 支持。

**主动网段扫描**是跨网段场景的兜底方案。程序启动时读取本机路由表获取直连网段，对目标端口做 TCP 连接探测（如 `/24` 网段，并发控制在合理范围）。扫描可能触发 IDS/IPS 告警，需要合规评估。

综合考虑项目约束（无中心节点、可 ping 通的可路由内网、接受种子 IP），最终采用混合发现策略。

### 2.2 Wails v3 集成方案调研

Wails v3 的核心优势在于提供了类型安全的 Go ↔ JavaScript 双向绑定系统。其绑定生成流程由 `wails3 generate bindings` 命令驱动，经过静态分析、代码生成和运行时桥接三个阶段。

在 Go 侧，开发者定义服务结构体并通过 `application.Options.Services` 注册。Wails v3 会静态分析 Go 源码，自动为导出的结构体和方法生成 TypeScript 绑定模块。在 TypeScript 侧，导入生成的绑定模块后即可直接调用 Go 方法，无需编写任何 IPC 桥接代码。

事件系统提供了统一的发布/订阅机制，支持 Go → JS、JS → Go、窗口间通信三种方向。Go 侧通过 `app.Event.Emit("event-name", data)` 发射事件，JS 侧通过 `Events.On("event-name", callback)` 监听。

对于本项目，Wails v3 的绑定系统将用于：前端调用 Go 后端的传输控制、配置读写、聊天记录查询等方法；事件系统将用于：Go 后端向前端推送节点上线/下线、传输进度、新消息到达等实时事件。

### 2.3 自定义 TCP 二进制协议调研

TCP 是面向字节流的协议，不保留消息边界。在 TCP 上实现消息传输需要自定义帧格式。标准做法是在消息头部增加长度字段，接收方先读取固定长度的头部，再根据长度字段读取完整载荷。

Go 标准库提供了 `encoding/binary` 包用于大端/小端整数编码，`io.ReadFull` 用于确保读取完整数据。一个典型的帧格式为：魔数字节（协议识别）+ 消息类型（1 字节）+ 载荷长度（4 字节大端）+ 载荷数据。接收方先用 `io.ReadFull` 读取固定长度的头部，解析出载荷长度后，再用 `io.ReadFull` 读取载荷。

对于可靠性要求较高的场景，可在载荷中增加 CRC32 校验字段，用于检测传输过程中的数据损坏。协议版本号字段建议在头部预留，便于后续协议升级。

### 2.4 断点续传方案调研

断点续传的核心是将文件切分为固定大小的块，每块独立校验，将已完成块的索引持久化到本地。当传输中断后，重启时读取状态文件，跳过已完成的块，从断点继续。

**分块策略**：Chunk 大小建议设为 512KB–1MB。太小会导致协议开销过大，太大则重传成本高。每块需计算 SHA-256 哈希，接收方校验通过后才标记为完成。使用 `os.OpenFile` 一次打开文件，配合 `file.ReadAt(buf, offset)` 实现线程安全的随机读取，避免 `os.Open` + `file.Seek` 在高并发小块场景下的系统调用开销。

**状态持久化**：推荐使用位图（Bitmap）而非 JSON 数组存储已完成块。位图以字节数组形式记录块完成状态，查询和更新都极快。一个 1GB 文件（512KB 块）仅需 2048 位 = 256 字节。位图可持久化到 SQLite 的 BLOB 字段中。

**文件指纹**：客户端预计算整个文件的 SHA-256 作为 `file_id`，每块请求携带 `file_id`、`offset`（字节偏移）、`size`（本块真实长度）。服务端用三元组做唯一索引，而非仅 `file_id:chunk_index`，防止网络重传导致相同序号多次到达。

**状态恢复**：用单个 `*os.File` 以 `O_APPEND|O_CREATE` 打开日志文件，每上传成功一块追加一行 JSON 记录；恢复时只需读取最后几 KB，反向扫描找最新成功记录。对于本项目，由于已有 SQLite 存储基础设施，建议直接利用 `transfer_jobs` 表的 `chunk_bitmap` 字段。

### 2.5 SQLite 驱动选型

Go 生态中主流的 SQLite 驱动有两种：`mattn/go-sqlite3`（CGO）和 `modernc.org/sqlite`（纯 Go）。

`modernc.org/sqlite` 基于 ccgo/v4 将 SQLite C 代码转译为 Go 代码，无需 CGO，交叉编译时不会因 C 依赖出问题。性能方面，TPC-H 基准测试显示：在 1GB 数据库场景下，mattn 驱动的 DB 生成耗时 50.06s，modernc 为 1m 05.66s；查询性能上 modernc 的 Query 2 为 5.36s（mattn 为 3.24s）。但在并发读写场景下，modernc 表现更优——简单插入约慢 3 倍，但并发（N=8）场景下反而快 25%。

对于本项目的负载特征（聊天消息写入频率中等、传输状态频繁更新、历史查询偶尔发生），modernc 的并发优势更为关键。此外，Wails v3 内置的 SQLiteService 也是基于 modernc 封装的，选择它有利于与 Wails 生态保持一致。

### 2.6 开源项目参考

**LANShare**（Go）：一款去中心化的局域网即时通信工具，P2P 架构，支持命令行和 Web 界面，消息端到端加密，使用 SQLite 存储历史消息。其架构与本项目高度相似，可作为核心参考。

**BLIP**（Electron + WebRTC）：现代技术栈的 P2P 局域网信使，支持文字、语音、视频和屏幕共享，使用 UDP 广播 + mDNS 发现，TCP 直连通信，Ed25519 Mesh 握手。其发现协议和握手设计值得借鉴。


## 三、系统架构设计

### 3.1 整体架构

应用采用分层架构，Go 后端负责所有网络通信和数据持久化，Vue3 前端仅负责 UI 渲染和用户交互。

```
┌─────────────────────────────────────────────────────┐
│                   Vue3 前端 (Wails WebView)          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 节点列表  │  │ 聊天窗口  │  │ 传输管理  │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│         ↕ Wails 绑定 (Go方法调用) / 事件系统 (推送)       │
├─────────────────────────────────────────────────────┤
│                   Go 后端 (Wails Application)        │
│  ┌──────────────────────────────────────────────┐   │
│  │              Service 层                       │   │
│  │  SettingsService │ ChatService │ TransferService│   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │discovery │  │transport │  │ transfer │          │
│  │  包       │  │  包       │  │   包      │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│  ┌──────────┐  ┌──────────┐                        │
│  │ identity │  │  store   │                        │
│  │   包      │  │   包     │                        │
│  └──────────┘  └──────────┘                        │
└─────────────────────────────────────────────────────┘
```

### 3.2 模块划分

| 模块 | 职责 | 关键接口 |
|---|---|---|
| **identity** | Ed25519 密钥对生成/加载、NodeID 派生、签名/验证 | `LoadOrCreate()`, `NodeID()`, `Sign()`, `Verify()` |
| **discovery** | 本网段 UDP 广播、组播尝试、种子 IP 管理、节点交换 | `Discover()`, `AddSeed()`, `OnPeerFound()` |
| **transport** | TCP 连接管理、心跳、消息收发、协议帧编解码 | `Listen()`, `Dial()`, `Send()`, `OnMessage()` |
| **transfer** | 文件分块、断点续传、哈希校验、进度事件 | `StartSend()`, `StartRecv()`, `Resume()`, `OnProgress()` |
| **store** | SQLite 聊天记录、传输状态、配置持久化 | `SaveMessage()`, `GetHistory()`, `UpdateTransfer()` |
| **config** | TOML 配置读写、默认值管理 | `Load()`, `Save()`, `GetDownloadDir()` |

### 3.3 数据流

**消息发送流程**：
1. Vue3 前端调用 `ChatService.SendMessage(peerID, text)`
2. ChatService 调用 `store.SaveMessage()` 持久化到 SQLite
3. ChatService 调用 `transport.Send(peerID, msg)` 发送到对端
4. 对端 transport 收到消息后，调用 `store.SaveMessage()` 保存
5. 对端通过 `app.Event.Emit("chat:new-message", data)` 推送事件到前端
6. 前端监听到事件后更新聊天窗口

**文件传输流程**：
1. Vue3 前端调用 `TransferService.StartSend(peerID, filePath)`
2. TransferService 计算文件 SHA-256，分块并计算每块哈希
3. 向对端发送文件元数据（文件名、大小、哈希、分块信息）
4. 对端检查本地是否有相同 `file_id` 的传输记录，返回已完成的块位图
5. 发送方只传输缺失的块，每块完成后接收方更新位图并 ACK
6. 进度通过 `app.Event.Emit("transfer:progress", data)` 实时推送到前端
7. 所有块传输完成，接收方校验整体哈希，原子替换临时文件


## 四、详细设计

### 4.1 节点身份设计

节点身份用 Ed25519 密钥对表示，NodeID 为公钥的 SHA-256 指纹前 8 字节的 hex 编码。私钥使用 PKCS#8 PEM 格式持久化到磁盘，标准 PEM 块类型为 `PRIVATE KEY`（而非非标准的 `ED25519 PRIVATE KEY`）。

**存储位置**：`os.UserConfigDir()/<app-name>/identity/node.key`

各平台路径映射：
- Linux: `~/.config/<app-name>/identity/node.key`
- macOS: `~/Library/Application Support/<app-name>/identity/node.key`
- Windows: `%APPDATA%\<app-name>\identity\node.key`

**文件权限**：私钥文件 `0600`（仅所有者可读写），目录 `0700`。

```go
func LoadOrCreate(configDir string) (*Identity, error) {
    keyPath := filepath.Join(configDir, "identity", "node.key")
    if data, err := os.ReadFile(keyPath); err == nil {
        block, _ := pem.Decode(data)
        priv, err := x509.ParsePKCS8PrivateKey(block.Bytes)
        // priv.(ed25519.PrivateKey)
    }
    // 首次运行，生成新密钥对
    pub, priv, _ := ed25519.GenerateKey(rand.Reader)
    pkcs8, _ := x509.MarshalPKCS8PrivateKey(priv)
    block := &pem.Block{Type: "PRIVATE KEY", Bytes: pkcs8}
    os.MkdirAll(filepath.Dir(keyPath), 0700)
    os.WriteFile(keyPath, pem.EncodeToMemory(block), 0600)
    return &Identity{NodeID: fingerprint(pub), PrivateKey: priv, PublicKey: pub}, nil
}
```

### 4.2 发现协议设计

**本网段发现（UDP 广播）** ：

广播消息采用二进制格式，开头为固定魔数字节 `0x4D 0x59 0x41 0x50 0x50`（“MYAPP”），随后是版本号（1 字节）、消息类型（1 字节）、NodeID（8 字节）、TCP 监听端口（2 字节大端）、显示名长度（1 字节）和显示名（变长）。

发送端使用 `net.InterfaceAddrs()` 获取本机活跃 IPv4 地址，构造子网广播地址（如 `192.168.1.255`），向固定端口（如 UDP 2425）发送通告包。监听端绑定 `0.0.0.0:2425`，接收所有接口的广播包。

为防止误触发和广播风暴，接收方用 `map[string]time.Time` 缓存最近 30 秒内见过的 NodeID，重复 ID 直接丢弃。广播消息开头包含魔数字节，避免被局域网内其他 UDP 服务（如 SSDP、mDNS、DHCP）误触发。

**跨网段发现（种子 IP + 节点交换）** ：

用户可在设置界面手动添加种子 IP。程序连接种子节点的 TCP 端口后，发送节点列表交换请求（包含本节点已知的所有节点信息），种子节点返回其已知节点列表。收到新节点后，逐一尝试连接，连接成功后再次交换节点列表，形成 Gossip 式扩散。

种子节点的连接状态和最后在线时间记录在本地 SQLite 中，启动时自动尝试连接最近活跃的种子节点。

**UDP 组播（可选）** ：

如果网络环境支持组播，可加入组播地址 `239.255.255.250`（端口 2426），定期发送通告包。组播地址的选择需避开 `224.0.0.0/24`（Linux 内核特殊处理）和 `239.255.255.255`（保留地址）。

### 4.3 TCP 传输协议设计

**帧格式**（大端字节序）：

```
┌────────┬────────┬────────┬────────┬─────────────┐
│ Magic  │ Version│  Type  │ Payload│   Payload   │
│ 2字节   │ 1字节   │ 1字节   │ Length │   Data      │
│        │        │        │ 4字节   │  变长        │
└────────┴────────┴────────┴────────┴─────────────┘
```

- **Magic**：`0x4D 0x59`，用于协议识别和流对齐
- **Version**：协议版本号，当前为 `0x01`
- **Type**：消息类型，定义如下：

| Type | 名称 | 方向 | 说明 |
|---|---|---|---|
| 0x01 | HELLO | 双向 | 连接建立后的握手，交换 NodeID 和显示名 |
| 0x02 | CHAT | 双向 | 文本消息 |
| 0x03 | PEER_LIST_REQ | 双向 | 请求节点列表 |
| 0x04 | PEER_LIST_RESP | 双向 | 返回节点列表 |
| 0x10 | FILE_META | 发送方→接收方 | 文件元数据 |
| 0x11 | FILE_META_ACK | 接收方→发送方 | 已完成的块位图 |
| 0x12 | FILE_CHUNK | 发送方→接收方 | 文件块数据 |
| 0x13 | FILE_CHUNK_ACK | 接收方→发送方 | 块确认 |
| 0x14 | FILE_DONE | 发送方→接收方 | 所有块发送完毕 |
| 0x15 | FILE_DONE_ACK | 接收方→发送方 | 文件校验通过 |
| 0x20 | HEARTBEAT | 双向 | 心跳保活 |
| 0x21 | HEARTBEAT_ACK | 双向 | 心跳确认 |

**FILE_META 载荷**（JSON 或二进制）：
```json
{
  "job_id": "uuid-v4",
  "file_id": "sha256-hex",
  "file_name": "example.zip",
  "file_size": 1073741824,
  "chunk_size": 524288,
  "total_chunks": 2048,
  "chunk_hashes": ["hash1", "hash2", ...]
}
```

`chunk_hashes` 仅在首次传输时发送完整列表。续传时发送方已知接收方已有的块，可选择性发送缺失块的哈希。

**FILE_META_ACK 载荷**：
```json
{
  "job_id": "uuid-v4",
  "completed_bitmap": "base64-encoded-bitmap",
  "completed_chunks": [0, 1, 2, 5, 6, ...]
}
```

`completed_bitmap` 为位图基64编码，接收方根据已有传输记录返回。

**读写实现**：发送方使用 `encoding/binary.Write` 写入头部（魔数、版本、类型、长度大端），再写入载荷。接收方先用 `io.ReadFull` 读取固定长度的头部，解析出载荷长度后，再用 `io.ReadFull` 读取载荷。连接设置读写超时（`conn.SetReadDeadline` / `conn.SetWriteDeadline`），防止慢客户端阻塞。

### 4.4 文件断点续传设计

**分块策略**：

Chunk 大小固定为 512KB。对最后一个不足 512KB 的块，使用 `io.LimitReader(file, remainingSize)` 自动截断，无需额外判断。

文件打开方式使用 `os.OpenFile(path, os.O_RDONLY, 0)` 一次打开，全程复用句柄。每个分块任务用 `file.ReadAt(buf, offset)` 读取，不改变文件内部偏移，线程安全。

**位图管理**：

```go
type ChunkBitmap struct {
    data []byte
}

func (b *ChunkBitmap) Set(index int) {
    b.data[index/8] |= 1 << (uint(index) % 8)
}

func (b *ChunkBitmap) IsSet(index int) bool {
    return b.data[index/8]&(1<<(uint(index)%8)) != 0
}

func (b *ChunkBitmap) CompletedCount() int { /* 统计置位数量 */ }
```

位图序列化后存入 SQLite 的 `chunk_bitmap` BLOB 字段。一个 1GB 文件（2048 块）的位图仅需 256 字节。

**传输状态机**：

```
IDLE → META_EXCHANGE → TRANSFERRING → VERIFYING → DONE
         ↑                ↓
         └── PAUSED ←─────┘
```

- **META_EXCHANGE**：发送文件元数据，接收方返回已完成块位图
- **TRANSFERRING**：按块顺序发送缺失块，每块完成后等待 ACK
- **PAUSED**：用户手动暂停或网络中断，状态持久化到 SQLite
- **VERIFYING**：所有块接收完毕，计算整体 SHA-256 与 `file_id` 比对
- **DONE**：校验通过，临时文件原子替换为最终文件

**临时文件与原子替换**：

接收方在目标目录下创建临时文件 `<filename>.tmp`，使用 `os.OpenFile(tmpPath, os.O_CREATE|os.O_WRONLY, 0644)` 打开。每个块写入前用 `file.Seek(offset, 0)` 定位到正确位置。所有块接收并校验通过后，用 `os.Rename(tmpPath, finalPath)` 原子替换，避免脏读。

**进度事件**：

每完成一个块，发送方和接收方分别通过 Wails 事件系统推送进度：

```go
app.Event.Emit("transfer:progress", map[string]interface{}{
    "job_id":   jobID,
    "peer_id":  peerID,
    "percent":  float64(completedChunks) / float64(totalChunks) * 100,
    "speed":    formatSpeed(bytesPerSecond),
    "status":   "active",
})
```

### 4.5 数据持久化设计

**数据库位置**：`os.UserConfigDir()/<app-name>/data/app.db`

**SQLite 配置**：启用 WAL 模式以支持并发读写，设置合理的连接池参数。DSN 参数：`?_journal_mode=WAL&_busy_timeout=5000&_synchronous=NORMAL`。`SetMaxOpenConns(5)`，`SetMaxIdleConns(2)`，禁用连接老化。

**表结构**：

```sql
-- 会话表
CREATE TABLE IF NOT EXISTS conversations (
    peer_id       TEXT PRIMARY KEY,
    peer_name     TEXT NOT NULL,
    peer_addr     TEXT,                    -- 最近已知 IP:Port
    last_message  TEXT,
    last_time     INTEGER,
    unread_count  INTEGER DEFAULT 0,
    is_online     INTEGER DEFAULT 0
);

-- 消息表（append-only）
CREATE TABLE IF NOT EXISTS messages (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    peer_id       TEXT NOT NULL,
    direction     TEXT NOT NULL CHECK(direction IN ('in','out')),
    content       TEXT NOT NULL,
    msg_type      TEXT DEFAULT 'text',
    file_id       TEXT,
    timestamp     INTEGER NOT NULL,
    FOREIGN KEY (peer_id) REFERENCES conversations(peer_id)
);
CREATE INDEX IF NOT EXISTS idx_messages_peer_time ON messages(peer_id, timestamp);

-- 传输任务表
CREATE TABLE IF NOT EXISTS transfer_jobs (
    job_id        TEXT PRIMARY KEY,
    peer_id       TEXT NOT NULL,
    file_name     TEXT NOT NULL,
    file_size     INTEGER NOT NULL,
    file_hash     TEXT NOT NULL,
    direction     TEXT NOT NULL CHECK(direction IN ('send','recv')),
    local_path    TEXT NOT NULL,
    temp_path     TEXT,
    completed     INTEGER DEFAULT 0,
    chunk_bitmap  BLOB,
    status        TEXT DEFAULT 'active',
    created_at    INTEGER NOT NULL,
    updated_at    INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_transfer_peer_hash ON transfer_jobs(peer_id, file_hash);

-- 种子节点表
CREATE TABLE IF NOT EXISTS seed_nodes (
    node_id       TEXT PRIMARY KEY,
    addr          TEXT NOT NULL,
    last_seen     INTEGER,
    is_manual     INTEGER DEFAULT 0
);
```

**聊天记录查询**：

```go
func (s *Store) GetHistory(peerID string, limit, offset int) ([]Message, error) {
    rows, err := s.db.Query(
        `SELECT id, peer_id, direction, content, msg_type, file_id, timestamp
         FROM messages WHERE peer_id = ? ORDER BY timestamp DESC LIMIT ? OFFSET ?`,
        peerID, limit, offset,
    )
    // ...
}
```

**传输状态更新**：

```go
func (s *Store) UpdateTransferProgress(jobID string, completed int64, bitmap []byte) error {
    _, err := s.db.Exec(
        `UPDATE transfer_jobs SET completed = ?, chunk_bitmap = ?, updated_at = ?
         WHERE job_id = ?`,
        completed, bitmap, time.Now().UnixMilli(), jobID,
    )
    return err
}
```

### 4.6 配置管理设计

**配置文件位置**：`os.UserConfigDir()/<app-name>/config.toml`

**配置结构**：

```toml
# config.toml
[general]
display_name = "张三"
auto_start = false

[download]
default_dir = "/Users/xxx/Downloads/MyApp"
auto_open = false

[network]
tcp_port = 2425
udp_port = 2425
enable_multicast = true
multicast_addr = "239.255.255.250"

[seeds]
addresses = ["10.10.20.15:2425", "10.10.30.22:2425"]
```

**加载逻辑**：代码中定义默认值结构体，读取 TOML 文件后逐字段覆盖。`os.UserConfigDir()` 是 Go 标准库中跨平台获取用户配置目录的关键函数，其行为高度依赖 GOOS 环境与底层系统约定。需注意 macOS 的 `os.UserConfigDir()` 硬编码为 `~/Library/Application Support`，不响应 `XDG_CONFIG_HOME`。

```go
type Config struct {
    General struct {
        DisplayName string `toml:"display_name"`
        AutoStart   bool   `toml:"auto_start"`
    } `toml:"general"`
    Download struct {
        DefaultDir string `toml:"default_dir"`
        AutoOpen   bool   `toml:"auto_open"`
    } `toml:"download"`
    Network struct {
        TCPPort         int    `toml:"tcp_port"`
        UDPPort         int    `toml:"udp_port"`
        EnableMulticast bool   `toml:"enable_multicast"`
        MulticastAddr   string `toml:"multicast_addr"`
    } `toml:"network"`
    Seeds struct {
        Addresses []string `toml:"addresses"`
    } `toml:"seeds"`
}

func Load(configDir string) (*Config, error) {
    cfg := defaultConfig()
    data, err := os.ReadFile(filepath.Join(configDir, "config.toml"))
    if err == nil {
        toml.Unmarshal(data, &cfg)
    }
    if cfg.Download.DefaultDir == "" {
        cfg.Download.DefaultDir = filepath.Join(os.UserHomeDir(), "Downloads", AppName)
    }
    os.MkdirAll(cfg.Download.DefaultDir, 0755)
    return &cfg, nil
}
```

**文件接收路径逻辑**：接收方收到文件元数据后，不弹框，直接保存到 `default_dir`。如果同名文件已存在，自动加后缀 `(1)`、`(2)`，直到找到不冲突的文件名。用户如需将某个文件存到别处，可在传输管理界面手动“另存为”。

### 4.7 Wails 集成设计

**服务注册**：

```go
// main.go
func main() {
    app := application.New(application.Options{
        Name:        "MyApp",
        Description: "内网P2P聊天与文件分享工具",
        Services: []application.Service{
            application.NewService(&SettingsService{}),
            application.NewService(&ChatService{}),
            application.NewService(&TransferService{}),
        },
    })
    // 启动网络服务（discovery、transport、transfer）
    // ...
    app.Run()
}
```

**Go 服务暴露**：

```go
type ChatService struct {
    store     *store.Store
    transport *transport.Manager
}

func (s *ChatService) SendMessage(peerID string, text string) error {
    msg := store.Message{
        PeerID:    peerID,
        Direction: "out",
        Content:   text,
        Timestamp: time.Now().UnixMilli(),
    }
    if err := s.store.SaveMessage(msg); err != nil {
        return err
    }
    return s.transport.SendChat(peerID, text)
}

func (s *ChatService) GetHistory(peerID string, limit, offset int) ([]store.Message, error) {
    return s.store.GetHistory(peerID, limit, offset)
}
```

**事件推送**：

```go
// transport 层收到消息时
func (m *Manager) onChatReceived(peerID, text string) {
    msg := store.Message{ /* ... */ }
    m.store.SaveMessage(msg)
    m.app.Event.Emit("chat:new-message", map[string]interface{}{
        "peerId":    peerID,
        "content":   text,
        "timestamp": time.Now().UnixMilli(),
    })
}

// transfer 层进度更新时
func (m *Manager) onProgress(jobID string, percent float64, speed string) {
    m.app.Event.Emit("transfer:progress", map[string]interface{}{
        "jobId":   jobID,
        "percent": percent,
        "speed":   speed,
    })
}
```

**Vue3 前端监听**：

```javascript
import { Events } from '@wailsio/runtime'

Events.On('chat:new-message', (event) => {
  store.commit('addMessage', event.data)
})

Events.On('transfer:progress', (event) => {
  store.commit('updateTransfer', event.data)
})

Events.On('peer:online', (event) => {
  store.commit('addPeer', event.data)
})
```


## 五、项目结构

```
myapp/
├── main.go                          # Wails 应用入口
├── go.mod
├── wails.json
├── internal/
│   ├── identity/
│   │   ├── identity.go              # Ed25519 密钥对管理
│   │   └── identity_test.go
│   ├── discovery/
│   │   ├── broadcast.go             # UDP 广播收发
│   │   ├── multicast.go             # UDP 组播收发
│   │   ├── seed.go                  # 种子 IP 管理
│   │   ├── gossip.go                # 节点列表交换
│   │   └── discovery_test.go
│   ├── transport/
│   │   ├── protocol.go              # 帧格式定义与编解码
│   │   ├── conn.go                  # TCP 连接管理
│   │   ├── manager.go               # 连接池与心跳
│   │   ├── heartbeat.go
│   │   └── transport_test.go
│   ├── transfer/
│   │   ├── chunk.go                 # 文件分块
│   │   ├── bitmap.go                # 位图管理
│   │   ├── sender.go                # 发送端状态机
│   │   ├── receiver.go              # 接收端状态机
│   │   ├── resume.go                # 断点恢复
│   │   └── transfer_test.go
│   ├── store/
│   │   ├── db.go                    # SQLite 初始化
│   │   ├── chat.go                  # 聊天记录 CRUD
│   │   ├── transfer.go              # 传输状态 CRUD
│   │   ├── seed.go                  # 种子节点 CRUD
│   │   └── migrations.go
│   ├── config/
│   │   ├── config.go                # TOML 配置加载
│   │   └── config_test.go
│   └── service/
│       ├── settings_service.go      # 配置服务（Wails 绑定）
│       ├── chat_service.go          # 聊天服务（Wails 绑定）
│       └── transfer_service.go      # 传输服务（Wails 绑定）
├── frontend/
│   ├── index.html
│   ├── package.json
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── views/
│   │   │   ├── ChatView.vue         # 聊天窗口
│   │   │   ├── TransferView.vue     # 传输管理
│   │   │   └── SettingsView.vue     # 设置页
│   │   ├── components/
│   │   │   ├── PeerList.vue         # 节点列表
│   │   │   ├── MessageBubble.vue    # 消息气泡
│   │   │   └── ProgressBar.vue      # 进度条
│   │   └── stores/
│   │       ├── chat.ts              # Pinia 聊天状态
│   │       ├── transfer.ts          # Pinia 传输状态
│   │       └── peers.ts             # Pinia 节点状态
│   └── bindings/                    # Wails 自动生成的绑定
└── build/                           # 构建产物
```


## 六、关键风险与应对措施

| 风险 | 影响 | 概率 | 应对措施 |
|---|---|---|---|
| **防火墙拦截 UDP 广播** | 本网段发现失效 | 高 | 文档引导用户添加防火墙例外；同时提供种子 IP 手动添加作为兜底 |
| **跨网段发现不可用** | 跨网段节点无法自动发现 | 中 | 种子 IP + 节点交换是主要机制；组播作为可选增强 |
| **Wails v3 Beta API 变动** | 升级时需适配 | 中 | 锁定 `v3.0.0-beta.23` 版本；关注 Wails 官方 Release Note |
| **modernc SQLite 写入性能** | 高频消息场景可能有瓶颈 | 低 | 批量写入 + WAL 模式；消息写入频率远低于纯插入基准测试负载 |
| **大文件传输内存占用** | 接收方 OOM | 低 | 流式写入，每块 512KB，不一次性读入内存 |
| **TCP 连接中断后状态不一致** | 传输失败或数据损坏 | 中 | 位图 + 文件哈希双重校验；重连后重新交换位图 |
| **端口冲突** | 程序无法启动 | 低 | 配置文件中可修改端口；启动时检测端口占用并提示 |
| **Ed25519 私钥泄露** | 节点身份被伪造 | 低 | 私钥文件权限 0600；不在日志中输出私钥内容 |


## 七、实施计划

| 阶段 | 内容 | 预估工期 | 依赖 |
|---|---|---|---|
| **第一阶段** | identity 包 + discovery 包（UDP 广播） | 1–2 周 | 无 |
| **第二阶段** | transport 包（TCP 连接 + 帧协议 + 心跳） | 1–2 周 | 第一阶段 |
| **第三阶段** | store 包（SQLite 表结构 + CRUD） + config 包 | 1 周 | 无 |
| **第四阶段** | transfer 包（分块 + 位图 + 断点续传） | 2–3 周 | 第二、三阶段 |
| **第五阶段** | Wails 服务层 + Vue3 前端（节点列表、聊天窗口、传输管理、设置页） | 2–3 周 | 第三阶段（可并行于第四阶段） |
| **第六阶段** | 种子 IP + 节点交换 + 跨网段发现 | 1 周 | 第一、二阶段 |
| **第七阶段** | 端到端加密 + 错误处理 + 用户文档 | 1–2 周 | 全部 |

**建议先写命令行原型**验证核心链路：identity 生成 → discovery 发现 → transport 连接 → 发送“hello” → store 写入 SQLite → 读回验证。这条链路跑通后再叠加 transfer 和 Wails 前端。